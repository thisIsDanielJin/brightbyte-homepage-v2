/**
 * components/sections/WorkSection.tsx — Work grid section (SEC-04)
 * STUB: Full implementation in Task 2 (Plan 04-02).
 */
import type { PROJECTS_QUERY_RESULT } from '@/sanity.types'

interface WorkSectionProps {
  projects: PROJECTS_QUERY_RESULT
  locale: string
}

export function WorkSection({ projects, locale }: WorkSectionProps) {
  // Task 2 implementation — stub renders nothing for now
  return null
}
