---
phase: 05-r3f-hero
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - components/hero/GlassMesh.tsx
  - components/hero/HeroScene.tsx
  - components/hero/constants.ts
autonomous: false
requirements: [HERO-01, HERO-02]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Single frosted-glass rounded solid, slow Y-axis rotation, brand-token lighting; PerformanceMonitor active; IntersectionObserver pauses the loop off-screen (UI-SPEC populated covered, D-01/D-03/D-04/D-05)"
    - "The glass sits roughly centered behind the hero text (text/CTA stay at z-10 above the Canvas) and is kept soft/faint behind the copy so the headline/subline/CTA remain legible — placement/scale tuned within the existing container contract (D-07; legibility hard-gated in Plan 03 via D-08)"
    - "Draw call count stays under 200 in a production build; PerformanceMonitor adaptive DPR scaling is active (ROADMAP SC #4)"
    - "Lighting + glass tint are driven by Phase-1 tokens (surface / surface-dark / accent) via named JS constants — the hero reads unmistakably BrightByte, never generic three.js chrome (D-09)"
    - "The render loop pauses (frameloop='never') when the hero is scrolled out of the viewport, without remounting the Canvas (D-05)"
    - "Gradient → glass cross-fade completes over ~400-500ms ease-out once onCreated fires; no pop-in, no spinner (D-11)"
    - "On PerformanceMonitor decline, the material degrades (samples/resolution reduced or non-transmissive fallback) so mobile stays within budget (D-12, D-02 Claude's Discretion mobile degradation locked here)"
  artifacts:
    - components/hero/GlassMesh.tsx
  key_links:
    - "HeroScene <Canvas frameloop={isVisible?'always':'never'}> ← IntersectionObserver state (D-05 offscreen pause, no remount — RESEARCH Pattern 2 / Pitfall 5)"
    - "PerformanceMonitor → AdaptiveDpr → setDpr(current*initialDpr); onDecline → GlassMesh degraded material path (RESEARCH Pattern 3/5)"
    - "GlassMesh MeshTransmissionMaterial color/lights ← constants.ts ACCENT_HEX/SURFACE_HEX/SURFACE_DARK_HEX (D-09 token-driven, no HDRI — RESEARCH Pattern 4)"
---

<objective>
Expand the proven tracer slice into the real frosted-glass centerpiece. Plan 01 proved the isolation chain, the reduced-motion gate, and the CLS-safe swap on a minimal mesh. This plan replaces that mesh with the actual scene: a single rounded solid wrapped in `MeshTransmissionMaterial`, lit by token-derived colored lights (no HDRI), slowly rotating on the Y axis, with the offscreen `frameloop` pause, `PerformanceMonitor` + `AdaptiveDpr`, and the two-tier mobile degradation path — plus the gradient→glass cross-fade timing.

Purpose: Deliver the "material catching light" appeal (D-01) that is the whole point of the hero, while keeping the render loop within the D-12 performance budget on mobile.
Output: `components/hero/GlassMesh.tsx` (the glass mesh + rotation + degradation), a fully-wired `HeroScene.tsx` (IntersectionObserver frameloop + PerformanceMonitor + AdaptiveDpr), and the fade-in confirmed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-r3f-hero/05-CONTEXT.md
@.planning/phases/05-r3f-hero/05-RESEARCH.md
@.planning/phases/05-r3f-hero/05-PATTERNS.md
@.planning/phases/05-r3f-hero/05-UI-SPEC.md
@.planning/phases/05-r3f-hero/05-01-SUMMARY.md
@components/hero/HeroScene.tsx
@components/hero/HeroCanvas.tsx
@components/hero/constants.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Lock mobile degradation strategy for the transmission material (D-02 costly)</name>
  <decision>Lock the mobile degradation strategy for MeshTransmissionMaterial (D-02 material choice is costly; D-12 mobile budget is the governing constraint).</decision>
  <context>D-02 (frosted glass / transmission) is a `costly` reversibility decision — the material drives lighting, environment, and the entire perf/degradation strategy. RESEARCH offers a two-tier path (Tier 1: reduce samples→2 + resolution→32 + transmissionSampler=true on decline; Tier 2: swap to non-transmissive meshPhysicalMaterial if Tier 1 still misses budget). CONTEXT D-02/discretion says "research recommends, planner locks." Locking here before implementation avoids reworking GlassMesh twice. This is presented because the material is the single largest perf risk in the phase; the executor should confirm the strategy before building the degradation branches.</context>
  <options>
    <option id="tier1-first">
      <name>Tier 1 default, Tier 2 only if the phase perf gate (Plan 03 Lighthouse/draw-call) fails</name>
      <pros>Keeps the richer transmission look everywhere; single material path; simplest GlassMesh; matches RESEARCH primary recommendation. Degradation is samples=2/resolution=32 + transmissionSampler on PerformanceMonitor.onDecline.</pros>
      <cons>Risk that Tier 1 still misses LCP < 2.5s on Moto G4 — discovered only at the Plan 03 gate, forcing a follow-up.</cons>
    </option>
    <option id="tier2-mobile">
      <name>Capability/width-gated: transmission on desktop, non-transmissive meshPhysicalMaterial on mobile from the start</name>
      <pros>Guarantees the mobile budget by construction (no extra render pass on mobile); lowest LCP risk.</pros>
      <cons>Two material code paths to maintain; mobile loses the true refraction look; more complex GlassMesh.</cons>
    </option>
  </options>
  <resume-signal>Select: tier1-first or tier2-mobile (then Task 2 implements the chosen degradation path)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build GlassMesh + wire HeroScene (token lighting, rotation, offscreen-pause, PerformanceMonitor, degradation, fade-in)</name>
  <files>components/hero/GlassMesh.tsx, components/hero/HeroScene.tsx, components/hero/constants.ts</files>
  <read_first>
    - components/hero/HeroScene.tsx (Plan 01 minimal Canvas — to be expanded with IntersectionObserver + PerformanceMonitor + AdaptiveDpr + GlassMesh)
    - components/hero/HeroCanvas.tsx (the fade-in wrapper + onReady already wired in Plan 01 — do not change its gate)
    - components/hero/constants.ts (ACCENT_HEX/SURFACE_HEX/SURFACE_DARK_HEX + glass params + ROTATION_SPEED + degradation thresholds; add GLASS_SCALE_DESKTOP/GLASS_SCALE_MOBILE if the chosen strategy needs them)
    - components/hero/GlassMesh.tsx (to be created — analog components/ui/MotionSection.tsx animation-loop pattern, PATTERNS.md §L180-230; RESEARCH GlassMesh code example §L520-588)
    - .planning/phases/05-r3f-hero/05-RESEARCH.md (Pattern 2 frameloop pause §L221-267; Pattern 3 PerformanceMonitor+AdaptiveDpr §L269-292; Pattern 4 token lighting no-HDRI §L294-329; Pattern 5 mobile degradation §L331-345; Pitfall 3 transmissionSampler §L394-400; Pitfall 5 frameloop no-remount §L408-412; Pitfall 6 roughness §L414-418)
    - .planning/phases/05-r3f-hero/05-UI-SPEC.md (Scene Motion Contract §L350-391; Responsive Behavior §L318-347; R3F scene color contract §L100-118; Flags — glass params as local constants §L509-522)
    - .claude/CLAUDE.md ("drei named imports only" — MeshTransmissionMaterial, PerformanceMonitor, AdaptiveDpr imported individually, never barrel)
  </read_first>
  <behavior>
    - Test (tests/sections/hero.spec.ts, still green): `#hero canvas` visible + aria-hidden in de/en at mobile-375 + desktop after the expansion (no regression from Plan 01).
    - Test (reduced-motion, still green): `canvas` count 0 under `reducedMotion: 'reduce'`.
    - Behavior: the mesh rotates on Y (visible frame-to-frame delta); the loop stops when the hero is scrolled out of view (frameloop toggles to 'never') and resumes on re-entry, with no Canvas flash/remount.
    - Behavior: lighting/tint colors come only from constants.ts (ACCENT_HEX/SURFACE_HEX/etc.) — no raw hex literals inside GlassMesh.tsx.
  </behavior>
  <action>
    Create `components/hero/GlassMesh.tsx` ('use client', named export `GlassMesh`, JSDoc header per PATTERNS.md §L210-228). Import named from drei: `MeshTransmissionMaterial` (NEVER barrel). Import constants (ACCENT_HEX, SURFACE_HEX, SURFACE_DARK_HEX, GLASS_IOR, GLASS_ROUGHNESS, GLASS_THICKNESS, GLASS_SAMPLES, GLASS_RESOLUTION, GLASS_SAMPLES_MOBILE, GLASS_RESOLUTION_MOBILE, ROTATION_SPEED). Render token-driven lights (ambientLight color={SURFACE_HEX}, directionalLight position={[2,3,4]} color={SURFACE_HEX}, pointLight position={[-3,-2,2]} color={ACCENT_HEX}) — NO drei Environment, NO HDRI (D-09, RESEARCH Pattern 4). Render one `<mesh ref>` with `<icosahedronGeometry args={[1,4]} />` and `<MeshTransmissionMaterial color={ACCENT_HEX} ior={GLASS_IOR} roughness={GLASS_ROUGHNESS} thickness={GLASS_THICKNESS} transmission={1} chromaticAberration={0.05} backside={false} anisotropicBlur={0} samples resolution transmissionSampler />` where samples/resolution/transmissionSampler switch on a `degraded` state prop per the LOCKED degradation strategy from the checkpoint. Keep roughness ≤ 0.05 (Pitfall 6). Drive Y rotation in `useFrame` by ROTATION_SPEED. Offset the focal mass right (~65-70% horizontal on desktop, centered ~50% on mobile per UI-SPEC Responsive) using the mesh position / scale — read canvas size via useThree to pick desktop vs mobile placement; add GLASS_SCALE_DESKTOP/GLASS_SCALE_MOBILE constants if needed (local constants, no new token).
    Expand `components/hero/HeroScene.tsx`: add the IntersectionObserver → `isVisible` state (RESEARCH Pattern 2) driving `frameloop={isVisible ? 'always' : 'never'}` on the SAME `<Canvas>` (prop change, no remount — Pitfall 5). Wrap the scene in `<PerformanceMonitor>` (named import from drei) with `<AdaptiveDpr />` and `<GlassMesh degraded={…} />`; wire onDecline/onIncline to a `degraded` state toggling the material path per the locked strategy. Keep `dpr={[1,2]}`, `gl={{ antialias:false }}`, `onCreated={onReady}`, `style={{position:'absolute',inset:0}}`, camera from constants. DPR floor stays 1.0 (UI-SPEC — never lower).
    Confirm the fade-in: HeroCanvas's `data-[ready=true]:opacity-100` wrapper + onReady from Plan 01 already produces the ~500ms ease-out cross-fade (HERO_FADE_MS = --duration-entrance); do NOT re-implement it — just verify the glass fades in over the still-painted gradient (Pitfall 2, D-11).
    Do NOT add raw hex to GlassMesh.tsx (all color via constants). Do NOT touch HeroCanvas's reduced-motion/WebGL gate or HeroSection.
  </action>
  <reversibility rating="costly">D-02 transmission material drives lighting/environment/degradation; the strategy locked at the checkpoint governs this task. Rotation speed/axis and frameloop tuning are reversible values (D-04).</reversibility>
  <verify>
    <automated>npx tsc --noEmit && grep -vE '^\s*(//|\*|/\*)' components/hero/GlassMesh.tsx | grep -cE "#[0-9A-Fa-f]{6}" | { read n; test "$n" -eq 0 && echo "no-inline-hex ok" || { echo "FAIL: raw hex in GlassMesh"; exit 1; }; } && npm run build && bash tests/invariants/no-canvas-server-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `components/hero/GlassMesh.tsx` exists: single icosahedron mesh + MeshTransmissionMaterial (drei named import), token-driven lights (ambient/directional/point) with colors ONLY from constants (no raw hex literal in non-comment lines — grep gate above).
    - HeroScene drives `frameloop={isVisible ? 'always' : 'never'}` from IntersectionObserver on the same Canvas (no remount); `PerformanceMonitor` + `AdaptiveDpr` wrap the scene; onDecline toggles the locked degradation path.
    - Y-axis rotation via useFrame at ROTATION_SPEED; roughness ≤ 0.05.
    - `next build` succeeds; `no-canvas-server-bundle.sh` still exits 0 (isolation preserved); `tsc --noEmit` clean.
    - HeroCanvas gate and HeroSection untouched; no new token in styles/tokens.css.
  </acceptance_criteria>
  <done>The frosted-glass centerpiece is live: single rounded solid, token-lit, slowly rotating, offscreen-paused, PerformanceMonitor/AdaptiveDpr active, mobile degradation wired, cross-fading in over the gradient — committed and still isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GPU | The continuous WebGL render loop (`frameloop="always"`) is the only resource-consumption surface; this plan wires the mitigations. |
| server → client bundle | The ssr:false isolation from Plan 01 must be preserved — GlassMesh/HeroScene stay client-only. |

Plan 02 adds no network, input, auth, data, or secret surface — it only expands the client-only scene proven in Plan 01.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | `frameloop="always"` render loop + MeshTransmissionMaterial multi-pass | low | mitigate (M-01) | This plan wires the full mitigation: IntersectionObserver pauses the loop offscreen (`frameloop='never'`) with no remount; `PerformanceMonitor` + `AdaptiveDpr` cap GPU cost with a DPR floor of 1.0; the locked mobile degradation path (samples→2/resolution→32 or non-transmissive fallback) prevents battery/GPU exhaustion on low-end devices. Verified at the Plan 03 perf gate (draw calls < 200, PerformanceMonitor active). |
| T-05-04 | Denial of Service | transmissionSampler assumption (single-mesh scene) | low | accept | `transmissionSampler={true}` on the degraded path assumes a single transmissive mesh (Pitfall 3); documented in GlassMesh JSDoc. Correct for this phase; guard only matters if a second transparent object is ever added. |

**No high-severity threats — Plan 02 only expands the client-only decorative scene; the sole risk (GPU/battery exhaustion from the continuous loop) is fully mitigated by M-01, which this plan implements. No new data/injection/authz surface. ASVS L1 unchanged from Plan 01.**
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `next build` succeeds; `no-canvas-server-bundle.sh` still exits 0.
- No raw hex in non-comment lines of GlassMesh.tsx (inline grep gate).
- `tests/sections/hero.spec.ts` + `tests/motion/reduced.spec.ts` still green vs `next start` (canvas visible happy path; count 0 reduced-motion) — no regression from Plan 01.
- Manual (deferred to Plan 03 gate): draw calls < 200, PerformanceMonitor DPR adaptation on throttle, offscreen pause.
</verification>

<success_criteria>
A single frosted-glass rounded solid rotates slowly behind the hero text, lit entirely by Phase-1 token colors, with the render loop paused offscreen and PerformanceMonitor/AdaptiveDpr driving mobile degradation within budget — all while the ssr:false isolation and reduced-motion gate from Plan 01 remain intact. The scene is ready for the Plan 03 performance + legibility gate.
</success_criteria>

<output>
Create `.planning/phases/05-r3f-hero/05-02-SUMMARY.md` when done.
</output>