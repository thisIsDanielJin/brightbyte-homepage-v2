---
phase: 05-r3f-hero
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - components/hero/HeroCanvas.tsx
  - components/hero/constants.ts
autonomous: true
requirements: [HERO-01, HERO-02]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The .hero-backdrop gradient remains the LCP element; the three.js import + Canvas creation are deferred to browser idle (post-LCP) so the ~1.45s three.js TBT no longer inflates simulated LCP — production Lighthouse mobile (Moto G4, CPU 4x) now reports simulated LCP < 2500ms and CLS = 0 (D-11, D-12, HERO-02, ROADMAP SC #2)"
    - "The D-06 reduced-motion / no-WebGL gate is byte-identical in effect: under prefersReduced || !canUseWebGL() NO Canvas ever mounts — only <HeroFallback /> — because the idle gate sits AFTER that early return and never runs for those users (D-06 one-way locked, HERO-02, ROADMAP SC #3)"
    - "ssr:false isolation preserved: dynamic(() => import('./HeroScene'), { ssr:false }) stays at module top level, three.js never enters the server bundle, and tests/invariants/no-canvas-server-bundle.sh still exits 0 (HERO-01, ROADMAP SC #1)"
    - "CLS = 0 across the deferral: the absolute inset-0 wrapper occupies identical geometry whether or not HeroScene has mounted; the gradient painted by HeroSection is never removed and the deferred Canvas fades IN OVER it (D-11, D-12, ROADMAP SC #2)"
    - "D-11 fade-in intact: once HeroScene mounts on idle and onReady fires (R3F onCreated), the wrapper still cross-fades opacity-0 → opacity-100 over 500ms ease-out — gradient → glass, no pop-in, no spinner (D-11)"
    - "The idle gate fires reliably post-LCP AND has a fallback: requestIdleCallback is the primary signal with a setTimeout fallback (~200ms) when it is undefined (older Safari), so the glass ALWAYS eventually mounts; the idle/timeout handle is cleaned up on unmount (HERO-01, HERO-02)"
    - "No new design tokens and no raw hex added (IDENT-01); the only new value is a local IDLE_MOUNT_TIMEOUT_MS constant, placed in constants.ts to match the established convention of scene/tuning constants living there"
  artifacts:
    - components/hero/HeroCanvas.tsx
  key_links:
    - "HeroCanvas useEffect idle gate → requestIdleCallback (setTimeout ~200ms fallback) → setShouldMount(true) → renders <HeroScene> child ONLY after the D-06 early return (post-LCP mount off the critical path, D-12)"
    - "wrapper <div className='absolute inset-0 opacity-0 [transition...] data-[ready=true]:opacity-100'> unchanged geometry ← HeroScene onReady sets data-ready (D-11 fade-in over the always-painted gradient, CLS = 0)"
    - "IDLE_MOUNT_TIMEOUT_MS (constants.ts) → HeroCanvas setTimeout fallback delay when requestIdleCallback is undefined (idle-gate reliability, HERO-02)"
---

<objective>
Take the three.js chunk fetch + Canvas-creation main-thread cost (~1.45s TBT) OFF the LCP critical path by idle-gating the HeroScene mount, so the `.hero-backdrop` gradient stays the LCP element and the production Lighthouse mobile (Moto G4) simulated LCP drops from 4.5s under the D-12 < 2.5s budget — WITHOUT changing the scene, the D-06 reduced-motion gate, the ssr:false isolation, the CLS=0 guarantee, or the D-11 fade-in.

This closes the single remaining 05-03 gate failure. At the 05-03 perf gate every graded ROADMAP success criterion PASSED except the simulated LCP metric: observed LCP was 1.29s (FCP 0.8s), CLS 0, draw calls 1, DPR floor 1.0, WCAG-AA holds — but Lighthouse's lantern model attributed ~2s of three.js bootup to the simulated LCP (4.5s). The user LOCKED the fix: "Defer canvas mount." Idle-gate the `dynamic({ssr:false})` HeroScene so the three.js import + Canvas creation happen AFTER the gradient paints and the browser goes idle (post-LCP). The gradient remains the LCP element; the glass fades in over it exactly as before.

Purpose: The perf budget (D-12) is the one non-negotiable gate that blocks the hero from shipping. The real paint is already fast — this plan removes the metric-only regression by mounting the decorative Canvas off the LCP path.
Output: An idle-gated `components/hero/HeroCanvas.tsx` (a small `useState`/`useEffect` idle gate around ONLY the `<HeroScene>` child, after the D-06 early return), an optional `IDLE_MOUNT_TIMEOUT_MS` constant in `constants.ts`, and a re-run of the 05-03 Task 2 production Lighthouse gate proving simulated LCP < 2.5s and CLS = 0, with the hero + reduced-motion Playwright specs still green and the isolation invariant still passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-r3f-hero/05-CONTEXT.md
@.planning/phases/05-r3f-hero/05-RESEARCH.md
@.planning/phases/05-r3f-hero/05-UI-SPEC.md
@.planning/phases/05-r3f-hero/05-03-SUMMARY.md
@components/hero/HeroCanvas.tsx
@components/hero/HeroScene.tsx
@components/hero/constants.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Idle-gate the HeroScene mount in HeroCanvas so three.js bootup happens post-LCP, then re-run the production Lighthouse gate</name>
  <files>components/hero/HeroCanvas.tsx, components/hero/constants.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md (lines 66-68 — dynamic() with ssr:false MUST be at module scope in a Client Component, never inside render; the module-level `const HeroScene = dynamic(...)` stays exactly where it is — VERIFIED path exists this session)
    - components/hero/HeroCanvas.tsx (self — the file being edited: module-level dynamic() at L36-38, canUseWebGL() probe L42-53, the D-06 early return `if (prefersReduced || !canUseWebGL()) return <HeroFallback />` at L60, the opacity-0 → data-[ready=true]:opacity-100 wrapper at L62-74 with onReady setting dataset.ready)
    - components/hero/HeroScene.tsx (the dynamically-imported scene — DO NOT edit; confirms onReady wires from R3F onCreated at L94, the DebugHook window.__r3f_hero perf hook at L54-66 the gate reads, and the IntersectionObserver frameloop pause — all unchanged)
    - components/hero/constants.ts (self — HERO_FADE_MS convention at the bottom; scene/tuning constants (GLASS_*, ROTATION_SPEED, CAMERA_*, HERO_FADE_MS) all live here — this is the established home for a new IDLE_MOUNT_TIMEOUT_MS local constant)
    - .planning/phases/05-r3f-hero/05-RESEARCH.md (Pitfall 2 §L386-392 — the .hero-backdrop fallback must remain painted at all times; the Canvas wrapper starts opacity-0 and fades in OVER it; the fallback is never removed → CLS=0; LCP-protection principle §L446-449 in UI-SPEC — the gradient is the LCP element and the Canvas must never block or delay the LCP paint)
    - .planning/phases/05-r3f-hero/05-UI-SPEC.md (Fade-in Transition Contract §L189-229 — 500ms ease-out opacity 0→1, no pop-in, no spinner, fallback always visible; Reduced-Motion Contract §L233-263 — no Canvas element in DOM under reduced-motion; Performance Contract §L433-449 — LCP < 2.5s, CLS = 0 hard gates)
    - .planning/phases/05-r3f-hero/05-03-PLAN.md (Task 2 §L92-117 — the production Lighthouse verify command + the node -e LCP/CLS assertion pattern to reuse verbatim; the human-verify checkpoint §L119-131 that resumes AFTER this plan)
    - playwright.config.ts (webServer.command is `npm run start` — the gate MUST run vs next start, not dev; kill any lingering :3000 dev server first)
  </read_first>
  <behavior>
    - Reduced-motion / no-WebGL (still green): under `reducedMotion: 'reduce'` (and under no-WebGL), NO `<canvas>` ever appears in the DOM — the idle gate must sit AFTER the D-06 early return so these users are never affected by idle timing. Assert `canvas` count 0.
    - Happy path (still green, possibly slightly slower to appear): in de and en at 375 and 1440, the `#hero canvas` DOES eventually become visible — the idle gate defers, it does not skip. The Playwright canvas-visibility assertion may need a more generous `toBeVisible` timeout to accommodate the idle delay, but MUST still assert the canvas appears; do NOT weaken to "canvas may be absent".
    - Perf (the target): production Lighthouse mobile (Moto G4 profile, CPU 4x) reports simulated LCP < 2500ms and CLS === 0 — the three.js bootup no longer counts against the LCP metric because the mount is deferred to idle.
    - CLS: no layout shift is introduced by the deferral — the absolute inset-0 wrapper occupies the same box whether or not HeroScene has mounted.
  </behavior>
  <action>
    Edit `components/hero/HeroCanvas.tsx` to idle-gate ONLY the `<HeroScene>` child mount. Keep the module-level `const HeroScene = dynamic(() => import('./HeroScene').then((m) => m.HeroScene), { ssr: false })` exactly where it is at module top level (Next.js requires dynamic() at module scope — lazy-loading.md:66-68); do NOT move it into render. Keep `canUseWebGL()` and the D-06 early return `if (prefersReduced || !canUseWebGL()) return <HeroFallback />` unchanged and BEFORE any idle logic, so reduced-motion / no-WebGL users never enter the idle path.

    After the early return, add a client-only idle gate: a `const [shouldMount, setShouldMount] = useState(false)` and a `useEffect(() => { ... }, [])` that schedules the mount for browser idle. Inside the effect, if `typeof window.requestIdleCallback === 'function'` use `const id = window.requestIdleCallback(() => setShouldMount(true))`; otherwise fall back to `const id = window.setTimeout(() => setShouldMount(true), IDLE_MOUNT_TIMEOUT_MS)`. Return a cleanup that cancels the pending handle (`window.cancelIdleCallback(id)` in the rIC branch, `window.clearTimeout(id)` in the fallback branch) so nothing fires after unmount. Because `requestIdleCallback` types may be absent, use a narrow local capability check on `window` rather than assuming the global type (keep tsc clean — a minimal typed guard, no `any` if avoidable; a small `declare global` augmentation for requestIdleCallback/cancelIdleCallback is acceptable if needed for tsc).

    Render the wrapper `<div ref={wrapperRef} className="absolute inset-0 opacity-0 [transition:opacity_500ms_cubic-bezier(0,0,0.2,1)] data-[ready=true]:opacity-100" aria-hidden="true">` UNCONDITIONALLY (same geometry always → CLS = 0), and gate ONLY the child: render `<HeroScene onReady={...} />` when `shouldMount` is true, otherwise render nothing (or null) inside the wrapper. The wrapper box is identical whether or not the child has mounted, and the `.hero-backdrop` gradient painted by HeroSection stays underneath the whole time — the glass fades in OVER it (Pitfall 2, D-11). Keep the existing `onReady` callback that sets `wrapperRef.current.dataset.ready = 'true'` so the 500ms ease-out cross-fade still fires from R3F onCreated once the scene mounts (D-11 — do NOT re-implement the fade).

    Add `IDLE_MOUNT_TIMEOUT_MS` (~200) to `components/hero/constants.ts` alongside `HERO_FADE_MS`, with a comment naming it as the setTimeout fallback delay for browsers without requestIdleCallback (matches the established convention of scene/tuning constants living in constants.ts). Do NOT add a new design token to styles/tokens.css and do NOT add raw hex. Do NOT touch HeroScene.tsx, GlassMesh.tsx, HeroSection, the D-06 gate, or the DebugHook.

    Then re-run the SAME production Lighthouse gate from 05-03 Task 2 to prove the deferral worked (see verify). If simulated LCP still misses (unlikely — the whole point is to move bootup off the LCP path), do NOT relax the D-12 budget: the fallback investigation is (a) confirm the gradient is genuinely the LCP element via the `largest-contentful-paint-element` audit, and (b) increase the idle delay / ensure the HeroScene chunk is not being preloaded onto the critical path. The expected outcome is PASS.
  </action>
  <reversibility rating="reversible">The idle delay + gate is a reversible load-strategy tweak — WHEN the existing client-only chunk mounts, not WHETHER it mounts or the architecture. The architectural decision (defer canvas mount) was already locked by the user; this task only implements the deferral timing, which can be tuned or reverted without any scene/isolation change.</reversibility>
  <verify>
    <automated>pkill -f "next.*dev" 2>/dev/null; lsof -ti:3000 | xargs kill -9 2>/dev/null; npx tsc --noEmit && npm run build && bash tests/invariants/no-canvas-server-bundle.sh && (npm run start & SP=$!; sleep 6; npx lighthouse@latest http://localhost:3000/de --only-categories=performance --form-factor=mobile --throttling.cpuSlowdownMultiplier=4 --output=json --output-path=./tests/screenshots/hero-lighthouse-de.json --chrome-flags="--headless" ; node -e "const r=require('./tests/screenshots/hero-lighthouse-de.json');const lcp=r.audits['largest-contentful-paint'].numericValue;const cls=r.audits['cumulative-layout-shift'].numericValue;console.log('LCP(ms)=',lcp,'CLS=',cls);if(lcp>=2500)throw new Error('LCP >= 2.5s: '+lcp);if(cls>0)throw new Error('CLS > 0: '+cls);console.log('perf budget OK')" ; R=$?; npx playwright test tests/sections/hero.spec.ts tests/motion/reduced.spec.ts; PR=$?; kill $SP 2>/dev/null; test $R -eq 0 -a $PR -eq 0)</automated>
  </verify>
  <acceptance_criteria>
    - `components/hero/HeroCanvas.tsx`: module-level `dynamic(() => import('./HeroScene'), { ssr:false })` unchanged; `canUseWebGL()` + the D-06 early return unchanged and BEFORE the idle gate; a `useState`/`useEffect` idle gate using `requestIdleCallback` with a `setTimeout(IDLE_MOUNT_TIMEOUT_MS)` fallback and cleanup on unmount; only the `<HeroScene>` child is gated behind `shouldMount`; the `absolute inset-0` opacity wrapper is rendered unconditionally with unchanged geometry; the `onReady` → `dataset.ready` fade-in is preserved.
    - `components/hero/constants.ts`: `IDLE_MOUNT_TIMEOUT_MS` (~200) added with a source comment; no new tokens.css token; no raw hex added anywhere.
    - Production Lighthouse (`next start`, Moto G4, CPU 4x) at `tests/screenshots/hero-lighthouse-de.json`: simulated LCP < 2500ms AND CLS === 0 (asserted in the verify command). Budget NOT relaxed.
    - `tests/sections/hero.spec.ts` green vs `next start`: canvas eventually visible in de/en at 375 + 1440 (timeout may be more generous to accommodate the idle delay, but the canvas MUST appear).
    - `tests/motion/reduced.spec.ts` green: 0 canvas under `reducedMotion: 'reduce'`.
    - `npx tsc --noEmit` clean; `tests/invariants/no-canvas-server-bundle.sh` exits 0 (ssr:false isolation intact); HeroScene.tsx / GlassMesh.tsx / HeroSection / the D-06 gate / the DebugHook untouched.
  </acceptance_criteria>
  <done>The HeroScene mount is idle-gated post-LCP, the gradient remains the LCP element, and the production Lighthouse mobile gate now PASSES (simulated LCP < 2.5s, CLS = 0) while the reduced-motion gate, ssr:false isolation, CLS=0 wrapper geometry, and the 500ms fade-in are all preserved — committed. Ready to resume the 05-03 human-verify checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client bundle | Preserved unchanged — the idle deferral only changes WHEN the existing client-only HeroScene chunk mounts, not whether three.js can reach the server bundle. `no-canvas-server-bundle.sh` still guards it. |
| browser → GPU | Unchanged — the scene, render loop, PerformanceMonitor, and DPR floor are all untouched; only the mount timing moves later. |

This plan adds no new network, input, auth, data, or secret surface — it defers the mount of an already-existing client-only decorative chunk by a browser-idle tick.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering (regression) | ssr:false isolation boundary | low | mitigate | The deferral keeps the module-level `dynamic(() => import('./HeroScene'), { ssr:false })` intact; `tests/invariants/no-canvas-server-bundle.sh` (registered in `test:invariants` in 05-03) still runs in this plan's verify and fails CI if three.js leaks into the server bundle — the isolation invariant remains CI-guarded across the change. |
| T-05-06 | Denial of Service (self-inflicted) | idle-gate never firing (no rIC + timer cleared) | low | mitigate | The `setTimeout(IDLE_MOUNT_TIMEOUT_MS)` fallback guarantees the glass eventually mounts when `requestIdleCallback` is unavailable (older Safari); cleanup only cancels a still-pending handle on unmount, so a mounted-and-visible hero is never torn down — the Playwright happy-path spec asserts the canvas DOES appear. |

**No high-severity threats — this is a client-only load-strategy tweak over an already-decorative scene; it changes only mount timing, adds no data/injection/authz/network/secret surface, and keeps the isolation invariant CI-guarded. ASVS L1 unchanged.**
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds; `tests/invariants/no-canvas-server-bundle.sh` exits 0 (HERO-01 isolation intact).
- Production Lighthouse (`next start`, Moto G4, CPU 4x): simulated LCP < 2.5s AND CLS = 0 — re-run of the 05-03 Task 2 gate, now passing (D-12, HERO-02).
- LCP element remains the `.hero-backdrop` gradient, not the canvas (D-11 protection) — confirm via the `largest-contentful-paint-element` audit at the resume checkpoint if it emits; otherwise confirm manually (the audit did not emit in the 05-03 JSON).
- `tests/sections/hero.spec.ts` green vs `next start`: canvas eventually visible in de/en at 375 + 1440 (generous timeout for the idle delay; canvas MUST appear).
- `tests/motion/reduced.spec.ts` green: 0 canvas under reduced-motion (D-06 gate untouched).
- No new tokens.css token; no raw hex added (IDENT-01).
</verification>

<success_criteria>
The single remaining 05-03 gate failure is closed: production Lighthouse mobile (Moto G4) simulated LCP is under the D-12 < 2.5s budget and CLS is 0, because the three.js chunk fetch + Canvas creation now happen at browser idle (post-LCP) instead of on the LCP critical path. The `.hero-backdrop` gradient remains the LCP element. The D-06 reduced-motion / no-WebGL gate, the ssr:false isolation (still CI-guarded), the CLS=0 wrapper geometry, and the 500ms D-11 fade-in over the gradient are all preserved byte-identical in effect. The hero is ready to resume the 05-03 human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05-r3f-hero/05-04-SUMMARY.md` when done.
</output>
