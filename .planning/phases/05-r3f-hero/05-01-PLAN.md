---
phase: 05-r3f-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components/hero/HeroFallback.tsx
  - components/hero/constants.ts
  - components/hero/HeroScene.tsx
  - components/hero/HeroCanvas.tsx
  - components/sections/HeroSection.tsx
  - tests/sections/hero.spec.ts
  - tests/motion/reduced.spec.ts
  - tests/invariants/no-canvas-server-bundle.sh
autonomous: true
requirements: [HERO-01, HERO-02]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The R3F hero renders without hydration errors in production; it is loaded exclusively via next/dynamic({ ssr: false }) — importing it in any Server Component causes a build error that is confirmed absent (ROADMAP SC #1, HERO-01)"
    - "When prefers-reduced-motion: reduce is set, no Canvas element is rendered — the static HeroFallback displays instead with the same container dimensions, no layout shift (ROADMAP SC #3, HERO-02, D-06)"
    - "Gradient (.hero-backdrop) is always visible during load; Canvas fades in at opacity 0→1 once onCreated fires — no spinner, no blank state (UI-SPEC loading covered, D-11)"
    - "canUseWebGL() check gates R3F mount; returns HeroFallback on false — Canvas failure is invisible to the user (UI-SPEC error covered)"
    - "HeroFallback fills absolute inset-0 exactly, no overflow possible (UI-SPEC overflow covered)"
    - "Hero CTA links to #contact anchor — no async state, no loading (UI-SPEC CTA loading covered)"
    - { statement: "A headline > 60 chars does not overlap the Canvas at 375px; Phase 5 does not change the text column max-w clamps", verification: backstop }
    - { statement: "The subline (max-w-[560px]) does not overlap the Canvas at 375px", verification: backstop }
  artifacts:
    - components/hero/HeroFallback.tsx
    - components/hero/constants.ts
    - components/hero/HeroScene.tsx
    - components/hero/HeroCanvas.tsx
    - tests/invariants/no-canvas-server-bundle.sh
  key_links:
    - "page.tsx (Server) → HeroSection.tsx ('use client') → HeroCanvas.tsx ('use client') → dynamic(() => import('./HeroScene'), { ssr:false }) — the isolation chain (HERO-01)"
    - "HeroSection backdrop layer swap: line 48 <div hero-backdrop> replaced by <HeroCanvas /> — HeroFallback (.hero-backdrop) always painted first as LCP element"
    - "useReducedMotion() gate in HeroCanvas → HeroFallback when true (D-06 one-way accessibility contract)"
---

<objective>
**As a** local Berlin SMB owner visiting the BrightByte homepage, **I want to** see a refined, quietly stunning hero the instant the page loads, **so that** I immediately trust the studio's craft — while never being made to wait, jarred by layout shift, or forced into motion I've asked my system to suppress.

This tracer plan proves the entire Phase 5 architecture end-to-end on one thin vertical slice: install the R3F stack, scaffold the Wave 0 test/invariant gaps, then wire a minimal Canvas (single unstyled mesh) through the isolation chain (`next/dynamic({ ssr:false })` in a `'use client'` wrapper) behind the existing hero text, gated by reduced-motion + WebGL checks that return the existing gradient fallback. The architectural dead-ends (server-bundle leak, hydration error, CLS) are caught here on one commit, before any transmission-material, lighting, or performance-tuning work in Plan 02.

Purpose: De-risk the isolation boundary, the reduced-motion gate, and the CLS-zero backdrop swap — the three things that break the whole phase if wrong — on the smallest possible scene.
Output: `components/hero/` module (HeroFallback, constants, HeroScene minimal, HeroCanvas), HeroSection backdrop swapped, R3F deps installed, Wave 0 test scaffolds + the `no-canvas-server-bundle.sh` invariant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-r3f-hero/05-CONTEXT.md
@.planning/phases/05-r3f-hero/05-RESEARCH.md
@.planning/phases/05-r3f-hero/05-PATTERNS.md
@.planning/phases/05-r3f-hero/05-UI-SPEC.md
@.planning/phases/05-r3f-hero/05-VALIDATION.md
@AGENTS.md
@.claude/CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols/files created or modified across Phase 5 (source-grounding pass excludes these from drift verification):

**Created (Plan 01):**
- `components/hero/HeroFallback.tsx` — exports `HeroFallback` (static gradient div)
- `components/hero/constants.ts` — exports `ACCENT_HEX`, `SURFACE_HEX`, `SURFACE_SUBTLE`, `SURFACE_DARK_HEX`, `GLASS_IOR`, `GLASS_ROUGHNESS`, `GLASS_THICKNESS`, `GLASS_SAMPLES`, `GLASS_RESOLUTION`, `GLASS_SAMPLES_MOBILE`, `GLASS_RESOLUTION_MOBILE`, `ROTATION_SPEED`, `CAMERA_FOV`, `CAMERA_Z`, `HERO_FADE_MS` (+ `GLASS_SCALE_DESKTOP`, `GLASS_SCALE_MOBILE` added in Plan 02)
- `components/hero/HeroScene.tsx` — exports `HeroScene` (Canvas root; minimal in Plan 01, full scene in Plan 02)
- `components/hero/HeroCanvas.tsx` — exports `HeroCanvas` (gate + dynamic import + fade-in wrapper); local `canUseWebGL()`
- `tests/invariants/no-canvas-server-bundle.sh` — HERO-01 server-bundle invariant gate

**Created (Plan 02):**
- `components/hero/GlassMesh.tsx` — exports `GlassMesh` (mesh + MeshTransmissionMaterial + useFrame rotation + degradation state)

**Modified:**
- `components/sections/HeroSection.tsx` — backdrop `<div>` (line 48) swapped for `<HeroCanvas />`; new import added
- `package.json` / `package-lock.json` — three@0.185.1, @react-three/fiber@9.7.0, @react-three/drei@10.7.8, motion@13.1.0 added (motion promoted from transitive-only to a direct dependency)
- `tests/sections/hero.spec.ts` — canvas visibility + aria-hidden + reduced-motion no-canvas assertions added
- `tests/motion/reduced.spec.ts` — canvas-absence-under-reduced-motion test added
- `package.json` `test:invariants` script — chains `no-canvas-server-bundle.sh` (phase-gate only, Plan 03)

**New deps:** `three` 0.185.1, `@react-three/fiber` 9.7.0, `@react-three/drei` 10.7.8, `motion` 13.1.0 (direct; was transitive via Sanity)

No existing exports are removed or renamed. `styled-components` (unused v1 remnant) is NOT touched.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Install R3F stack, scaffold constants, and create test/invariant gaps</name>
  <files>package.json, package-lock.json, components/hero/constants.ts, tests/invariants/no-canvas-server-bundle.sh, tests/motion/reduced.spec.ts, tests/sections/hero.spec.ts</files>
  <read_first>
    - package.json (self — dependencies block + test:invariants script)
    - .claude/CLAUDE.md (Technology Stack table — pinned versions three 0.185.1 / @react-three/fiber 9.7.0 / @react-three/drei 10.7.8; "drei named imports only")
    - .planning/phases/05-r3f-hero/05-RESEARCH.md (Package Legitimacy Audit — fiber/drei SUS→Approved rationale; Standard Stack install command)
    - .planning/phases/05-r3f-hero/05-PATTERNS.md ("package.json" §L369-388, "constants.ts" §L264-327, "no-canvas-server-bundle.sh" §L459-534, "reduced.spec.ts" §L433-455, "hero.spec.ts" §L391-429)
    - tests/invariants/no-raw-hex.sh (analog for the new bash invariant — set -euo pipefail, SCRIPT_DIR/REPO_ROOT derivation, EXIT_CODE pattern, grep || true, FAIL/PASS blocks)
    - tests/sections/hero.spec.ts (existing locale-loop structure to extend — lines 23-49)
    - tests/motion/reduced.spec.ts (existing reducedMotion:'reduce' context pattern — lines 14-22)
  </read_first>
  <action>
    Run the pinned install: `npm install three@0.185.1 @react-three/fiber@9.7.0 @react-three/drei@10.7.8 motion@13.1.0` (exact versions from .claude/CLAUDE.md; do NOT use caret ranges, do NOT change versions). `motion` MUST be added as a DIRECT dependency here: it is currently only present transitively (via Sanity) and NOT in package.json `dependencies`, yet HeroCanvas.tsx (Task 2) imports `useReducedMotion` from `motion/react` for the D-06 one-way accessibility gate (ROADMAP SC #3) — a transitive-only dependency is not a supported import contract and can vanish on a Sanity bump. Confirm all four deps (three, @react-three/fiber, @react-three/drei, motion) land in package.json dependencies and package-lock.json updates. Do NOT remove styled-components.
    Create `components/hero/constants.ts` per PATTERNS.md §"constants.ts": named exports ACCENT_HEX '#1C39BB', SURFACE_HEX '#FFFFFF', SURFACE_SUBTLE '#FAFAFA', SURFACE_DARK_HEX '#0F0F10' each with a trailing `// from --color-<name>` source-token comment; plus GLASS_IOR 1.5, GLASS_ROUGHNESS 0.05, GLASS_THICKNESS 0.3, GLASS_SAMPLES 6, GLASS_RESOLUTION 256, GLASS_SAMPLES_MOBILE 2, GLASS_RESOLUTION_MOBILE 32, ROTATION_SPEED 0.003, CAMERA_FOV 45, CAMERA_Z 5, HERO_FADE_MS 500. Include the JSDoc header documenting that this is the only file with raw hex outside styles/tokens.css and that no-raw-hex.sh scans app/ only (Assumption A4). Do NOT add any new token to styles/tokens.css.
    Create `tests/invariants/no-canvas-server-bundle.sh` following the no-raw-hex.sh structure: build if `.next/server` absent, then scan `.next/server` for WebGL/three imports and fail if found. Use `grep -rEl` for the WebGL/three/@react-three signature, guard with `|| true`, and gate on a non-empty match variable (never a bare `== 0` on the raw file). FAIL message prefixed `FAIL [HERO-01]:`, PASS message `PASS [HERO-01]:`. Make it executable (`chmod +x`).
    Extend `tests/motion/reduced.spec.ts`: add a test creating a `reducedMotion: 'reduce'` browser context, navigating to `/de`, asserting `page.locator('canvas')` has count 0, then closing the context (D-06 / HERO-02).
    Extend `tests/sections/hero.spec.ts`: inside the existing locale loop add a happy-path canvas-visibility assertion (`#hero canvas` visible + aria-hidden="true") AND a separate reduced-motion test asserting `#hero canvas` count 0. To keep the happy path DETERMINISTIC in headless Playwright (where a WebGL context is not guaranteed), the canvas-visibility assertion MUST first sniff WebGL availability inside the page via `page.evaluate(() => { const c = document.createElement('canvas'); return !!(c.getContext('webgl2') || c.getContext('webgl')); })`; only assert `#hero canvas` visible when that returns true, otherwise assert the `.hero-backdrop` gradient fallback is visible and `#hero canvas` count is 0 (the no-WebGL branch — a legitimate pass, mirrors production canUseWebGL()=false). This branch keeps the suite green regardless of whether the headless browser exposes WebGL. These are the RED scaffolds — they will pass once Task 2/3 wire the Canvas.
    Do NOT yet register no-canvas-server-bundle.sh in the package.json test:invariants script — that wiring lands in Plan 03 (it requires a full build and is a phase-gate check, not a per-commit check).
  </action>
  <precondition>Node 22.12+ available (Sanity v6 prebuild requirement; verify `node -v` ≥ 22.12 before install).</precondition>
  <verify>
    <automated>node -e "const p=require('./package.json');['three','@react-three/fiber','@react-three/drei','motion'].forEach(k=>{if(!p.dependencies[k])throw new Error('missing '+k)});console.log('deps ok')" && test -f components/hero/constants.ts && test -x tests/invariants/no-canvas-server-bundle.sh && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains three@0.185.1, @react-three/fiber@9.7.0, @react-three/drei@10.7.8, and motion@13.1.0 (exact; motion is a NEW direct dependency — previously transitive-only via Sanity — required by HeroCanvas's `motion/react` import); package-lock.json updated.
    - `components/hero/constants.ts` exists, exports all 16 named constants, every hex constant has a `// from --color-*` comment, `tsc --noEmit` passes.
    - `tests/invariants/no-canvas-server-bundle.sh` exists, is executable, and follows the set -euo pipefail / EXIT_CODE / grep||true structure of no-raw-hex.sh.
    - `tests/motion/reduced.spec.ts` and `tests/sections/hero.spec.ts` contain the new canvas-count-0 (reduced-motion) assertions.
    - No new token added to styles/tokens.css; styled-components not removed.
  </acceptance_criteria>
  <done>R3F deps installed at pinned versions; constants module + server-bundle invariant + test scaffolds created; tsc clean.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "3D hero renders behind the text, isolated and reduced-motion-safe" — one minimal path only</name>
  <files>components/hero/HeroFallback.tsx, components/hero/HeroScene.tsx, components/hero/HeroCanvas.tsx, components/sections/HeroSection.tsx</files>
  <read_first>
    - components/sections/HeroSection.tsx (the swap container — line 48 backdrop div, line 51 z-10 text column; 'use client' already present)
    - components/hero/HeroFallback.tsx (to be created — analog HeroSection.tsx:48 verbatim)
    - components/hero/HeroCanvas.tsx (to be created — analog components/sections/ContactSection.tsx 'use client' island + early-return gate pattern, PATTERNS.md §L28-99)
    - components/hero/HeroScene.tsx (to be created — analog components/ui/MotionSection.tsx 'use client'+hook pattern, PATTERNS.md §L102-178)
    - components/ui/MotionSection.tsx (useReducedMotion import from 'motion/react' — lines 20, 29)
    - .planning/phases/05-r3f-hero/05-RESEARCH.md (Pattern 1 dynamic ssr:false isolation §L172-219; Pitfall 1 build error §L374-384; Pitfall 2 gradient-under-canvas §L386-392; HeroCanvas code example §L424-469)
    - .planning/phases/05-r3f-hero/05-UI-SPEC.md (Canvas Layer Contract §L147-186; Reduced-Motion Contract §L233-283; No-WebGL/Pre-Hydration §L286-314; canUseWebGL §L299-311; Loading/State Diagram §L394-415)
    - node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md (Next 16 dynamic ssr:false rules — MANDATORY per AGENTS.md before writing Next code)
  </read_first>
  <behavior>
    - Test (extended tests/sections/hero.spec.ts, mobile-375 + desktop, both locales): after page load, `#hero canvas` is visible and carries aria-hidden="true" (happy path — WebGL available in Playwright Chromium).
    - Test (extended tests/motion/reduced.spec.ts + hero.spec.ts): under `reducedMotion: 'reduce'`, `#hero canvas` count is 0 and the `.hero-backdrop` gradient div is present (D-06 gate — no Canvas at all).
    - Behavior (build): `next build` completes with no error and no three.js/WebGL import in the server bundle (no-canvas-server-bundle.sh passes) — proving the ssr:false isolation chain.
    - Behavior (runtime): no React hydration warning in the `next start` console for the hero route.
    - RED→GREEN boundary: this task's own `<automated>` gate is tsc + `next build` + `no-canvas-server-bundle.sh` (the isolation/build behaviors GREEN here). The Playwright behavior specs above (canvas-visible / canvas-count-0, mobile-375 + reduced-motion) are authored RED in Task 1 and go GREEN in Task 3 against `next start` — the RED→GREEN transition for the runtime behaviors intentionally SPANS Task 2 → Task 3 (Playwright needs the production server Task 3 stands up). This task does not weaken those specs; it wires the code that turns them green in Task 3.
  </behavior>
  <action>
    Create `components/hero/HeroFallback.tsx` (no 'use client' — pure static div, no hooks): named export `HeroFallback` returning exactly `<div className="absolute inset-0 hero-backdrop" aria-hidden="true" />` (verbatim reuse of HeroSection.tsx:48; D-10). JSDoc header per PATTERNS.md §L242-256.
    Create `components/hero/HeroScene.tsx` ('use client', named export `HeroScene({ onReady }: { onReady?: () => void })`): for THIS tracer, render a `<Canvas>` (from '@react-three/fiber') with `dpr={[1,2]}`, `camera={{ fov: CAMERA_FOV, position: [0,0,CAMERA_Z] }}` (from './constants'), `gl={{ antialias: false }}`, `onCreated={onReady}`, and `style={{ position:'absolute', inset:0 }}` (the ONE permitted inline style — R3F Canvas API, documented per PATTERNS.md §L172-178). Inside, render ONLY a minimal `<mesh>` with `<icosahedronGeometry args={[1,4]} />` and a plain `<meshStandardMaterial />` plus one `<ambientLight />` and one `<directionalLight position={[2,3,4]} />`. NO MeshTransmissionMaterial, NO PerformanceMonitor, NO rotation, NO IntersectionObserver yet — those are Plan 02. This is the thinnest mesh that proves the Canvas mounts. Wrap the Canvas in a `<div className="absolute inset-0">`.
    Create `components/hero/HeroCanvas.tsx` ('use client' at line 1, after JSDoc header): `import dynamic from 'next/dynamic'`, `import { useRef } from 'react'`, `import { useReducedMotion } from 'motion/react'`, `import { HeroFallback } from './HeroFallback'`. At MODULE TOP LEVEL (never inside render): `const HeroScene = dynamic(() => import('./HeroScene'), { ssr: false })`. Define local `canUseWebGL()` per UI-SPEC §L299-311. Named export `HeroCanvas()`: call `useReducedMotion()`; if it is truthy OR `!canUseWebGL()` return `<HeroFallback />` (D-06 one-way gate — early return before any Canvas). Otherwise return a wrapper `<div ref={wrapperRef} className="absolute inset-0 opacity-0 [transition:opacity_500ms_cubic-bezier(0,0,0.2,1)] data-[ready=true]:opacity-100" aria-hidden="true">` containing `<HeroScene onReady={() => { if (wrapperRef.current) wrapperRef.current.dataset.ready='true' }} />`. The HeroFallback stays painted underneath via HeroSection (Pitfall 2) — do NOT remove the fallback on mount.
    Modify `components/sections/HeroSection.tsx`: add `import { HeroCanvas } from '@/components/hero/HeroCanvas'` after existing imports (line 24). Replace the backdrop `<div className="absolute inset-0 hero-backdrop" aria-hidden="true" />` at line 48 with BOTH the always-painted fallback `<div className="absolute inset-0 hero-backdrop" aria-hidden="true" />` (LCP element, never removed) AND `<HeroCanvas />` immediately after it (the Canvas fades in over the fallback). Update the header comment's Phase 5 note. DO NOT touch the z-10 text column (line 51+) or any copy.
  </action>
  <reversibility rating="reversible">D-06 (reduced-motion → no Canvas) is a one-way accessibility POLICY already locked upstream in CONTEXT.md at discuss time — no checkpoint is re-opened here. The task's implementation (the early-return gate placement in HeroCanvas) is local and cheap to move, hence reversible; the locked contract is honored, not re-decided.</reversibility>
  <verify>
    <automated>npx tsc --noEmit && npm run build && bash tests/invariants/no-canvas-server-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `next build` succeeds; `no-canvas-server-bundle.sh` exits 0 (no three.js/WebGL in server bundle) — proves HERO-01 ssr:false isolation (ROADMAP SC #1).
    - HeroCanvas.tsx has 'use client' at line 1 (after JSDoc); `dynamic(() => import('./HeroScene'), { ssr:false })` is at module top level, not inside render.
    - HeroCanvas returns `<HeroFallback />` (early return) when `useReducedMotion()` is true OR `canUseWebGL()` is false — no Canvas branch entered (D-06).
    - HeroSection keeps the `.hero-backdrop` fallback div painted AND renders `<HeroCanvas />` after it; z-10 text column and copy untouched.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The single happy path works end-to-end: a minimal Canvas mounts behind the hero text (isolated via ssr:false, no server-bundle leak, no hydration error), and reduced-motion / no-WebGL fall back to the gradient with zero layout shift — committed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify tracer end-to-end against production build (isolation + reduced-motion + CLS-safe swap)</name>
  <files>tests/sections/hero.spec.ts, tests/motion/reduced.spec.ts</files>
  <read_first>
    - tests/sections/hero.spec.ts (the assertions extended in Task 1 — canvas visible + reduced-motion count 0)
    - tests/motion/reduced.spec.ts (canvas-absence assertion)
    - playwright.config.ts (webServer.command uses `npm run start` — Playwright runs vs next start, per RESEARCH §L689)
    - .planning/phases/05-r3f-hero/05-VALIDATION.md (Sampling Rate §L30-37; full suite runs vs next start)
  </read_first>
  <action>
    Run the production Playwright hero + reduced-motion suite against `next start` (NOT dev): build, start, then `npx playwright test tests/sections/hero.spec.ts tests/motion/reduced.spec.ts`. The happy-path canvas-visibility assertion is guarded by the in-page WebGL sniff authored in Task 1 (`page.evaluate` on a probe canvas's `webgl2`/`webgl` context): when the headless Chromium exposes WebGL, assert `#hero canvas` visible + aria-hidden="true"; when it does not, the test asserts the gradient fallback branch instead — either way deterministic and green. Confirm the reduced-motion canvas-count-0 tests are green in both locales at mobile-375 and desktop. Confirm no hydration warning appears in the server console for the hero route. Do NOT tune the scene, add materials, or add performance monitoring — this task only proves the tracer slice is solid before Plan 02 expands it. If any assertion fails, fix the wiring in Task 2's files (not by weakening the test).
  </action>
  <verify>
    <automated>npm run build && (npm run start & SERVER_PID=$!; sleep 6; npx playwright test tests/sections/hero.spec.ts tests/motion/reduced.spec.ts; RESULT=$?; kill $SERVER_PID 2>/dev/null; exit $RESULT)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sections/hero.spec.ts` happy-path: with WebGL available in headless Chromium, `#hero canvas` visible + aria-hidden="true" green in de + en; with WebGL absent, the gradient-fallback branch is green instead (in-page `page.evaluate` WebGL sniff selects the branch — deterministic either way).
    - Reduced-motion tests (both spec files): `canvas` count 0 green.
    - `next start` hero route console shows zero React hydration warnings.
    - No scene material/lighting/perf changes made in this task.
  </acceptance_criteria>
  <done>Tracer slice verified against a production build: isolation, reduced-motion gate, and CLS-safe backdrop swap all hold end-to-end — Plan 02 can safely expand the scene.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client bundle | The ssr:false isolation boundary; three.js/WebGL code must never cross into the server bundle (HERO-01). |
| browser → GPU | The WebGL render loop consumes GPU/CPU; the only resource-consumption surface this phase adds. |

Phase 5 introduces NO new network endpoint, NO user input, NO auth/session/access-control surface, NO stored data, NO secrets. The Canvas is decorative and `aria-hidden`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | WebGL render loop (Canvas) | low | mitigate (M-01) | `frameloop="always"` is offscreen-paused via IntersectionObserver (Plan 02) + `PerformanceMonitor`/`AdaptiveDpr` cap GPU cost + DPR floor 1.0 — prevents unthrottled battery/GPU drain. Plan 01's minimal mesh has negligible cost; the mitigation is fully wired in Plan 02. |
| T-05-02 | Tampering (supply chain) | three / @react-three/fiber / @react-three/drei installs | low | mitigate (M-02) | Pinned exact versions in package.json + package-lock.json; all three from established pmndrs/mrdoob orgs with 3–11M weekly downloads (RESEARCH Package Legitimacy Audit — fiber/drei flagged too-new by seam but Approved, legitimate repos, explicitly locked in .claude/CLAUDE.md). No `[SLOP]`/`[ASSUMED]` packages → no blocking legitimacy checkpoint required. |
| T-05-03 | Information Disclosure | Canvas fingerprinting | low | accept | Canvas is decorative + aria-hidden; collects no user data; irrelevant for a static marketing hero (RESEARCH Security Domain). |

**No high-severity threats — client-only WebGL render, no new data/injection/authz surface; mitigations M-01/M-02 cover resource use and supply chain. ASVS L1: only V7 (error handling) partially applies — WebGL failure silently falls back to HeroFallback, exposing no error detail.**
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `next build` succeeds; `tests/invariants/no-canvas-server-bundle.sh` exits 0 (HERO-01 isolation).
- `tests/sections/hero.spec.ts` + `tests/motion/reduced.spec.ts` green vs `next start` (canvas visible happy path; canvas count 0 under reduced-motion).
- No hydration warning in production console for the hero route.
- `npm run test:invariants` (existing chain) still green — no IDENT-01 regression.
</verification>

<success_criteria>
Minimal Canvas mounts behind the hero text via the ssr:false isolation chain with no server-bundle leak and no hydration error; reduced-motion and no-WebGL both fall back to the existing gradient with zero layout shift; all Wave 0 test/invariant scaffolds exist and pass against a production build. The architecture is proven for Plan 02 to expand.
</success_criteria>

<output>
Create `.planning/phases/05-r3f-hero/05-01-SUMMARY.md` when done.
</output>
