---
context: phase
phase: 05-r3f-hero
task: 3
total_tasks: 3
status: paused
last_updated: 2026-09-03T08:40:35.406Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong Node version | Default shell node is v20.17.0, which FAILS `npm run build` at the Sanity prebuild (`Node.js version >=22.12 required`). | blocking | Prefix every node/npm/npx command with `source ~/.nvm/nvm.sh && nvm use 22.22.0 >/dev/null &&`. Vercel runtime must also be ≥22. |
| Gaming a non-gating metric | 05-05 mounted the hero only after 3s/interaction purely to improve Lighthouse's SIMULATED LCP — a metric already reconciled to non-gating. It cost a visible 3s blank-then-pop-in for no live benefit. | advisory | Don't add UX cost for a metric that isn't a ship gate. Fixed in f734657 (idle mount). |

<current_state>
Phase 5 (R3F Hero) is functionally COMPLETE and marked complete in ROADMAP.md (5/5, 2026-09-03), but NOT yet closed in GSD state: `phase.complete 5` is blocked because the gsd-verifier has not produced `05-VERIFICATION.md`. The 05-03 human-verify checkpoint was run against a production build and signed off as PASS WITH CAVEATS (user decision). Two items deferred to Phase 7 (both logged in ROADMAP). Working tree clean except STATE.md (included in this WIP commit).
</current_state>

<completed_work>

Completed Tasks:
- 05-05 mount-trigger fix — Done (f734657): reverted the 3s interaction-floor trigger to a graceful requestIdleCallback mount (setTimeout fallback). Canvas mounts ~0.4s post-load, no interaction. `HERO_MOUNT_DELAY_MS`→`HERO_IDLE_FALLBACK_MS`. tsc + build green; headless-verified.
- 05-03 human-verify checkpoint — Done (435d784): 4 graded criteria PASS. Step 1 fade FAIL → mount fixed; residual fade hard-cut deferred. 05-03-SUMMARY status blocked→complete, HERO-01/HERO-02 signed off.
- ROADMAP updated — Done (435d784): Phase 5 [x] 5/5 Complete; 05-03 [x]; progress table + plans line fixed; two Phase-7 debt notes added.
</completed_work>

<remaining_work>

- Task 4: Phase-goal verification — run gsd-verifier to produce `.planning/phases/05-r3f-hero/05-VERIFICATION.md`. NOT started.
- Task 5: `phase.complete 5` — blocked until VERIFICATION.md exists. NOT started.
- Task 6: `git push -u origin main` — 7 local commits, no upstream. NOT started.
</remaining_work>

<decisions_made>

- D-12 gates on OBSERVED LCP, not Lighthouse's Lantern-simulated metric (2026-08-26). Simulated residual (~3032ms) is a framework-fixed Next 16 hydration cost.
- Reverted 05-05's interaction-or-3s-floor mount trigger to a plain idle mount — the trigger only protected the now-non-gating simulated LCP.
- Complete Phase 5 with the D-11 fade-in bug AND visual polish deferred to Phase 7 — all 4 graded success criteria pass; neither deferred item is a graded criterion.
</decisions_made>

<blockers>
- phase.complete 5: "No verification report found" → run /gsd-execute-phase 5 in fresh context; it skips wave work (all SUMMARYs exist) and resumes at regression → verify → roadmap-complete.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` Phase 5 section — the two Phase-7 debt notes (fade-in bug + visual polish) and the D-12 reconciliation note.
2. `.planning/phases/05-r3f-hero/05-03-SUMMARY.md` — the "Human-Verify Result (Task 3)" section documenting the pass-with-caveats sign-off.

## Critical Anti-Patterns (do NOT repeat these)
- WRONG NODE: use Node 22.22.0 (`nvm use 22.22.0`) for all build/start/playwright — v20 fails the Sanity prebuild.

## Infrastructure State
- No background servers running (production `next start` was stopped at pause).
- 7 commits local on `main`, no upstream tracking set.

<context>
Phase 5's four graded criteria are all met and evidenced. The only thing between "done in spirit" and "done in GSD state" is the gsd-verifier VERIFICATION.md gate. I deferred running it because context was exhausted (75%), not because of any problem — it's a clean, heavy subagent step best run fresh. The fade-in bug is real but cosmetic; I traced it to the HeroCanvas wrapper rendering with the wrong className at runtime (likely React reusing the adjacent key-less `hero-backdrop` gradient sibling node) and stopped at the user's request to move on. The double-rAF fade attempt is committed (f734657) but does not resolve it.
</context>

<next_action>
Start with: run `/gsd-execute-phase 5` in a fresh session (produces 05-VERIFICATION.md, runs regression, marks phase complete). Then `git push -u origin main`.
</next_action>
