---
phase: 05-r3f-hero
plan: 05
type: execute
wave: 3
depends_on: [05-04]
files_modified:
  - components/hero/HeroCanvas.tsx
  - components/hero/constants.ts
  - next.config.ts
autonomous: true
requirements: [HERO-01, HERO-02]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Lever 1 — the HeroScene mount trigger fires PROVABLY after LCP under Lighthouse 4x-CPU throttle: the bare requestIdleCallback (which fired ~2.3s post-hydration, ~494ms BEFORE observed LCP, pulling the 233KB three.js chunk into Lantern's pre-LCP task graph) is REPLACED by a signal ordering-guaranteed-after-LCP (first user interaction OR a timeout floor set well past the ~2811ms observed-LCP mark), so three.js's ~747ms bootup no longer counts against simulated LCP (D-12, HERO-02, ROADMAP SC #2)"
    - "The mount ALWAYS eventually happens via a guaranteed timeout floor (interaction OR timeout, whichever first): a headless Playwright/Lighthouse run that never interacts still mounts the canvas after the floor elapses, so the #hero canvas is guaranteed-visible AND the mechanism is deterministic under Lantern; the spec is NOT weakened to 'canvas may be absent' (HERO-01, HERO-02)"
    - "Lever 2 — next.config.ts sets experimental.inlineCss: true, inlining the one ~7KB render-blocking CSS chunk (metricSavings FCP 150 / LCP 150) into the <head> so it no longer gates the H1 paint; NextConfig type stays valid and tsc --noEmit is clean (Next 16 doc-supported, production-build only, Tailwind-recommended) (D-12, ROADMAP SC #2)"
    - "Production Lighthouse mobile (next start, Moto G4 profile, CPU 4x) reports simulated LCP < 2500ms AND CLS === 0 with the hero mounted — OR, if after BOTH levers simulated LCP stalls at ~2.6-2.9s (residual framework-fixed Lantern modeling of Next 16 hydration), the executor records the measured number honestly and HALTS (status: halted, human_judgment) surfacing 'reconcile D-12 to observed LCP (~2811ms)' as a user decision; the executor MUST NOT fake a pass, cherry-pick a lighthouse run, or unilaterally relax the budget (D-12 non-negotiable, HERO-02)"
    - "The D-06 reduced-motion / no-WebGL gate is byte-identical in effect: under prefersReduced || !canUseWebGL() NO Canvas ever mounts — only <HeroFallback /> — because the new mount trigger sits AFTER that early return and never runs for those users; tests/motion/reduced.spec.ts asserts 0 canvas (D-06 one-way locked, HERO-02, ROADMAP SC #3)"
    - "ssr:false isolation preserved: dynamic(() => import('./HeroScene'), { ssr:false }) stays at module top level, three.js never enters the server bundle, and tests/invariants/no-canvas-server-bundle.sh still exits 0 (HERO-01, ROADMAP SC #1)"
    - "CLS = 0 across the change: the absolute inset-0 wrapper occupies identical geometry whether or not HeroScene has mounted; the .hero-backdrop gradient painted by HeroSection is never removed and the deferred Canvas fades IN OVER it (D-11, D-12, ROADMAP SC #2)"
    - "D-11 fade-in intact: once HeroScene mounts and onReady fires (R3F onCreated), the wrapper still cross-fades opacity-0 → opacity-100 over 500ms ease-out — gradient → glass, no pop-in, no spinner (D-11)"
    - "No new design tokens and no raw hex added (IDENT-01); the only new/renamed value is a local mount-delay constant in components/hero/constants.ts (replacing/renaming IDLE_MOUNT_TIMEOUT_MS) — no styles/tokens.css token, no text-gray-*/bg-gray-*/border-gray-*"
  artifacts:
    - components/hero/HeroCanvas.tsx
    - next.config.ts
  key_links:
    - "HeroCanvas useEffect mount trigger (first-interaction listener {once,passive} + setTimeout floor, whichever first) → setShouldMount(true) → renders <HeroScene> child ONLY after the D-06 early return (post-LCP mount off Lantern's critical path, D-12)"
    - "next.config.ts experimental.inlineCss:true → the ~7KB render-blocking CSS chunk inlined into <head> → FCP/LCP no longer wait on a CSS <link> round-trip (Lever 2, D-12)"
    - "HERO_MOUNT_DELAY_MS (constants.ts, replaces IDLE_MOUNT_TIMEOUT_MS) → HeroCanvas setTimeout floor delay guaranteeing the canvas always eventually mounts and stays deterministic under Lantern (mount-trigger reliability + Playwright green, HERO-01, HERO-02)"
    - "HeroScene onReady → wrapperRef.current.dataset.ready='true' → 500ms ease-out cross-fade over the always-painted .hero-backdrop gradient (D-11 fade-in, CLS=0)"
---

<objective>
Close the single remaining 05-03 gate failure (D-12 simulated LCP < 2.5s on Moto G4) by attacking the two app-controllable levers the full Lighthouse-trace research (LH 13.4.1) + Next 16 doc review pinned this session — the user chose direction "Fix the real cause (A)". 05-04 correctly took three.js's TBT off the observed critical path (1450ms → ~184ms) but did NOT move simulated LCP, because its idle gate has a bug: a BARE `requestIdleCallback` (no `{timeout}`, no floor) fires ~2.3s post-hydration under 4x-CPU throttle — the main thread goes idle BEFORE observed LCP (~2811ms), so the 233KB three.js chunk loads at ~2317ms (~494ms before LCP) and executes ~747ms of bootup INSIDE Lantern's pre-LCP task graph. That is exactly why observed TBT dropped but simulated LCP stayed ~4.5s.

Lever 1 (the whole ballgame): replace the bare-idle mount trigger with a signal ORDERING-GUARANTEED-AFTER-LCP — first user interaction (pointerdown/scroll/keydown, `{ once: true }` + passive; cannot occur during the headless Lighthouse LCP window) OR a timeout floor set well past the ~2811ms observed-LCP mark, whichever fires first. The floor keeps the canvas guaranteed-visible for headless Playwright (which never interacts) AND deterministic under Lantern. Removing three.js's ~747ms bootup + ~113KB from the pre-LCP graph should collapse simulated LCP toward the observed ~2811ms.

Lever 2 (cheap add): set `experimental.inlineCss: true` in next.config.ts to inline the one ~7KB render-blocking CSS chunk (metricSavings FCP 150 / LCP 150) into the `<head>` — Next 16 doc-supported, production-build only, explicitly recommended for Tailwind/atomic CSS.

Honest ceiling (stated, not hidden): observed LCP is already ~2811ms (~300ms over 2.5s). Simulated < 2.5s is PLAUSIBLE, not guaranteed. If after BOTH levers the simulated LCP stalls at ~2.6-2.9s, the residual is framework-fixed Lantern modeling of Next 16 hydration — do NOT relax the D-12 budget unilaterally; record the measured number honestly and HALT (status: halted, human_judgment), surfacing "reconcile D-12 to observed LCP" as a user decision. Never fake a pass or cherry-pick a run.

Purpose: D-12 is the one non-negotiable gate blocking the hero from shipping; every other graded ROADMAP criterion already passes. This plan removes the two remaining app-controllable contributors to the simulated metric.
Output: a HeroCanvas mount trigger that fires provably post-LCP with a guaranteed timeout floor, `experimental.inlineCss: true` in next.config.ts, a renamed mount-delay constant in constants.ts, and a re-run of the 05-03 Task 2 production Lighthouse gate proving simulated LCP < 2.5s and CLS = 0 — or an honest halt. Once the gate passes, the 05-03 human-verify checkpoint (6 steps in 05-03-PLAN.md Task 3) resumes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-r3f-hero/05-CONTEXT.md
@.planning/phases/05-r3f-hero/05-RESEARCH.md
@.planning/phases/05-r3f-hero/05-04-SUMMARY.md
@components/hero/HeroCanvas.tsx
@components/hero/constants.ts
@next.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix the HeroScene mount trigger to fire provably post-LCP (interaction-or-timeout-floor) + enable experimental.inlineCss, then re-run the production Lighthouse gate</name>
  <files>components/hero/HeroCanvas.tsx, components/hero/constants.ts, next.config.ts</files>
  <read_first>
    - components/hero/HeroCanvas.tsx (self — the file being edited: module-level dynamic() at L49-51, canUseWebGL() probe L55-66, the D-06 early return `if (prefersReduced || !canUseWebGL()) return <HeroFallback />` at L93, the buggy bare `window.requestIdleCallback(() => setShouldMount(true))` idle effect at L82-89 with the setTimeout(IDLE_MOUNT_TIMEOUT_MS) fallback, the opacity-0 → data-[ready=true]:opacity-100 wrapper at L95-112 with onReady setting dataset.ready)
    - components/hero/constants.ts (self — IDLE_MOUNT_TIMEOUT_MS at L70 is the constant to rename/replace; scene/tuning constants — HERO_FADE_MS, GLASS_*, ROTATION_SPEED, CAMERA_* — all live here, the established home for the new mount-delay constant; the IDENT-01 note at L8-12 confirms this file is outside the no-raw-hex scan but still requires source-token comments on hex)
    - next.config.ts (self — the current NextConfig has no `experimental` block; withNextIntl wraps it; the experimental.inlineCss key must be added to a new `experimental` object inside `nextConfig`, keeping `turbopack.root` intact and tsc clean)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/inlineCss.md (lines 9, 43-51, 72 — experimental.inlineCss:true inlines <link> as <style> in <head>; strongest for first-time visitors + atomic CSS/Tailwind; production-build only, not dev — VERIFIED this session)
    - node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md (dynamic() with ssr:false MUST stay at module scope in a Client Component, never inside render; the module-level `const HeroScene = dynamic(...)` does NOT move)
    - .planning/phases/05-r3f-hero/05-03-PLAN.md (Task 2 §L92-117 — the production Lighthouse verify command + the node -e LCP/CLS assertion to REUSE verbatim; the human-verify checkpoint §L119-131 that resumes AFTER this plan passes)
    - .planning/phases/05-r3f-hero/05-04-SUMMARY.md (the halted root-cause record: LCP element is the <h1> not the gradient; canvas-free probe still simulates ~2.8s; bare rIC fired too early — do NOT repeat the 05-04 premise)
    - playwright.config.ts (webServer.command is `npm run start` — the gate MUST run vs next start, not dev; kill any lingering :3000 dev server first)
    - tests/sections/hero.spec.ts (the happy-path spec asserting `#hero canvas` becomes visible in de/en at 375+1440 — with an interaction-gated mount it would NEVER see the canvas without the timeout floor; the floor keeps it green; give the visibility assertion a generous timeout to accommodate the floor delay, but do NOT weaken it to "canvas may be absent")
    - tests/motion/reduced.spec.ts (asserts 0 canvas under reducedMotion:'reduce' — must stay green; the new trigger sits AFTER the D-06 early return)
  </read_first>
  <behavior>
    - Reduced-motion / no-WebGL (still green): under `reducedMotion: 'reduce'` (and under no-WebGL), NO `<canvas>` ever appears in the DOM — the mount trigger sits AFTER the D-06 early return so these users are never affected. Assert canvas count 0 (tests/motion/reduced.spec.ts).
    - Happy path — canvas guaranteed to appear (still green): in de and en at 375 and 1440, the `#hero canvas` DOES eventually become visible via the timeout floor even with zero interaction (headless Playwright does not interact). The visibility assertion may need a more generous `toBeVisible` timeout to cover the floor delay, but MUST still assert the canvas appears.
    - Post-LCP ordering (the target): under 4x-CPU throttle the mount trigger CANNOT fire before observed LCP (~2811ms) — no interaction occurs in a headless LCP window and the timeout floor is set past the observed-LCP mark — so the three.js chunk fetch + ~747ms bootup land in Lantern's POST-LCP task graph and no longer inflate simulated LCP.
    - CSS delivery: production HTML `<head>` carries the inlined `<style>` for the app CSS (experimental.inlineCss:true), not a render-blocking `<link>`, so FCP/LCP no longer wait on a CSS round-trip.
    - Perf (the pass condition): production Lighthouse mobile (Moto G4, CPU 4x) reports simulated LCP < 2500ms AND CLS === 0. If it stalls at ~2.6-2.9s, HALT honestly (see action) — do NOT fake or relax.
    - CLS: no layout shift introduced — the absolute inset-0 wrapper occupies the same box whether or not HeroScene has mounted.
  </behavior>
  <action>
    LEVER 1 — fix the mount trigger in `components/hero/HeroCanvas.tsx`. Keep the module-level `const HeroScene = dynamic(() => import('./HeroScene').then((m) => m.HeroScene), { ssr: false })` exactly where it is at module top level (Next.js requires dynamic() at module scope — lazy-loading.md); do NOT move it into render. Keep `canUseWebGL()` and the D-06 early return `if (prefersReduced || !canUseWebGL()) return <HeroFallback />` unchanged and BEFORE any mount logic. Replace the BUGGY bare `window.requestIdleCallback(() => setShouldMount(true))` effect (which fires ~2.3s post-hydration, BEFORE observed LCP under 4x throttle) with a trigger that is ORDERING-GUARANTEED-AFTER-LCP: in the `useEffect(() => { ... }, [])`, register first-interaction listeners on `window` — `pointerdown`, `scroll`, `keydown` — each with `{ once: true, passive: true }` and a shared handler that calls `setShouldMount(true)`; AND register a `const timerId = window.setTimeout(() => setShouldMount(true), HERO_MOUNT_DELAY_MS)` floor so the canvas ALWAYS eventually mounts even with zero interaction (headless Playwright / Lighthouse never interact). The handler must be idempotent (guard so the first of interaction-or-timeout wins and the rest are no-ops). Return a cleanup that removes all three listeners AND clears the timeout so nothing fires after unmount. Do NOT keep `requestIdleCallback` as the primary signal — bare rIC fires too early under Lantern and is the exact bug being removed. tsc must stay clean (typed event listeners; no `any` if avoidable).

    The `HERO_MOUNT_DELAY_MS` timeout floor MUST sit well past the ~2811ms observed-LCP mark so the floor itself cannot pull the chunk into the pre-LCP graph — set it to 3000 (≥ 2500-3000ms window from research). This is the deterministic-under-Lantern lever: on a real device the glass appears ~3s in if the user never interacts (an acceptable calm reveal for a decorative background), and appears immediately on first pointer/scroll/key.

    LEVER 2 — enable inline CSS in `next.config.ts`. Add an `experimental: { inlineCss: true }` object to the `nextConfig: NextConfig` value (create the `experimental` block; keep the existing `turbopack: { root: projectRoot }` intact). This inlines the one ~7KB render-blocking CSS chunk into the `<head>` as a `<style>` tag (Next 16 doc inlineCss.md:9,43-51 — production-build only, Tailwind-recommended, ~150ms FCP+LCP). Keep the `withNextIntl(nextConfig)` wrapping and the worktree `projectRoot` logic unchanged. tsc --noEmit must stay clean (NextConfig type).

    CONSTANT — in `components/hero/constants.ts`, replace `IDLE_MOUNT_TIMEOUT_MS = 200` with `HERO_MOUNT_DELAY_MS = 3000`, updating the doc comment to name it as the mount-trigger timeout floor (fires the HeroScene mount if no user interaction occurs first; set past the observed-LCP mark so three.js bootup lands post-LCP in Lantern's graph). Update the import + usage in HeroCanvas.tsx accordingly. Do NOT add a new styles/tokens.css design token and do NOT add raw hex anywhere (IDENT-01). Do NOT touch HeroScene.tsx, GlassMesh.tsx, HeroSection, the D-06 gate, the DebugHook, or the onReady → dataset.ready fade-in (D-11 — the wrapper is rendered unconditionally with unchanged `absolute inset-0 opacity-0 [transition:opacity_500ms...] data-[ready=true]:opacity-100` geometry; only the `<HeroScene>` child is gated behind `shouldMount`).

    Then re-run the SAME production Lighthouse gate from 05-03 Task 2 (see verify) — kill any lingering :3000 dev server, build, run the isolation invariant, `next start`, Lighthouse Moto G4 CPU4x, assert simLCP<2500 && CLS===0, then the hero + reduced-motion Playwright specs. If simulated LCP PASSES (< 2500ms) and CLS===0, the plan is complete and the 05-03 human-verify checkpoint resumes. If simulated LCP STALLS at ~2.6-2.9s after both levers, do NOT relax D-12, do NOT cherry-pick a run, do NOT fake a pass: keep the representative (failing) lighthouse JSON, record the measured simLCP/CLS honestly in the SUMMARY, set status: halted with human_judgment, and surface the decision "reconcile D-12 to observed LCP (~2811ms) vs simulated" for the user — the residual is framework-fixed Lantern modeling of Next 16 hydration, not an app-controllable lever.
  </action>
  <precondition>Production build runnable locally (`npm run build && npm run start`); Chrome/Chromium available for Lighthouse (Playwright bundles Chromium; `npx lighthouse` uses system or bundled Chrome). experimental.inlineCss only takes effect in a production build, never dev.</precondition>
  <reversibility rating="reversible">Both levers are trivially revertible and non-architectural. Lever 1 changes only WHEN the existing client-only chunk mounts (mount-trigger timing), not WHETHER it mounts or the ssr:false architecture; the interaction-or-timeout trigger can be re-tuned or reverted to the prior gate without any scene/isolation change. Lever 2 is a single documented experimental config flag that can be removed in one line. The architectural decision (defer canvas mount; fix the real cause) was already locked by the user — this task only implements the concrete mechanism.</reversibility>
  <verify>
    <automated>pkill -f "next.*dev" 2>/dev/null; lsof -ti:3000 | xargs kill -9 2>/dev/null; npx tsc --noEmit && npm run build && bash tests/invariants/no-canvas-server-bundle.sh && (npm run start & SP=$!; sleep 6; npx lighthouse@latest http://localhost:3000/de --only-categories=performance --form-factor=mobile --throttling.cpuSlowdownMultiplier=4 --output=json --output-path=./tests/screenshots/hero-lighthouse-de.json --chrome-flags="--headless" ; node -e "const r=require('./tests/screenshots/hero-lighthouse-de.json');const lcp=r.audits['largest-contentful-paint'].numericValue;const cls=r.audits['cumulative-layout-shift'].numericValue;console.log('LCP(ms)=',lcp,'CLS=',cls);if(lcp>=2500)throw new Error('LCP >= 2.5s: '+lcp);if(cls>0)throw new Error('CLS > 0: '+cls);console.log('perf budget OK')" ; R=$?; npx playwright test tests/sections/hero.spec.ts tests/motion/reduced.spec.ts; PR=$?; kill $SP 2>/dev/null; test $R -eq 0 -a $PR -eq 0)</automated>
  </verify>
  <acceptance_criteria>
    - `components/hero/HeroCanvas.tsx`: module-level `dynamic(() => import('./HeroScene'), { ssr:false })` unchanged; `canUseWebGL()` + the D-06 early return unchanged and BEFORE the mount trigger; the bare `requestIdleCallback` mount signal is REPLACED by first-interaction listeners (`pointerdown`, `scroll`, `keydown`, each `{ once: true, passive: true }`) plus a `setTimeout(HERO_MOUNT_DELAY_MS)` floor, whichever first, with an idempotent handler and cleanup removing all listeners + clearing the timeout on unmount; only the `<HeroScene>` child is gated behind `shouldMount`; the `absolute inset-0` opacity wrapper is rendered unconditionally with unchanged geometry; the `onReady` → `dataset.ready` 500ms fade-in is preserved.
    - `components/hero/constants.ts`: `IDLE_MOUNT_TIMEOUT_MS` replaced by `HERO_MOUNT_DELAY_MS = 3000` with an updated source comment; no new styles/tokens.css token; no raw hex added anywhere; no text-gray-*/bg-gray-*/border-gray-*.
    - `next.config.ts`: `experimental: { inlineCss: true }` present inside `nextConfig`; `turbopack.root` and `withNextIntl` wrapping unchanged; `npx tsc --noEmit` clean (NextConfig type valid).
    - Production Lighthouse (`next start`, Moto G4, CPU 4x) at `tests/screenshots/hero-lighthouse-de.json`: simulated LCP < 2500ms AND CLS === 0 (asserted in the verify command) — OR an honest halt (status: halted, human_judgment) recording the measured simLCP/CLS and surfacing the "reconcile D-12 to observed LCP" decision; budget NOT relaxed, run NOT cherry-picked.
    - `tests/sections/hero.spec.ts` green vs `next start`: canvas eventually visible in de/en at 375 + 1440 via the timeout floor (generous timeout for the floor delay; canvas MUST appear — spec NOT weakened).
    - `tests/motion/reduced.spec.ts` green: 0 canvas under `reducedMotion: 'reduce'`.
    - `npx tsc --noEmit` clean; `tests/invariants/no-canvas-server-bundle.sh` exits 0 (ssr:false isolation intact); HeroScene.tsx / GlassMesh.tsx / HeroSection / the D-06 gate / the DebugHook untouched.
  </acceptance_criteria>
  <done>The HeroScene mount trigger fires provably post-LCP (interaction-or-3000ms-floor, replacing the buggy bare requestIdleCallback), experimental.inlineCss:true inlines the render-blocking CSS, and the production Lighthouse mobile gate PASSES (simulated LCP < 2.5s, CLS = 0) — OR the executor has halted honestly with the measured number and surfaced the D-12/observed-LCP reconciliation decision. In either outcome the D-06 reduced-motion gate, ssr:false isolation (CI-guarded), CLS=0 wrapper geometry, and the 500ms D-11 fade-in are preserved, and the 05-03 human-verify checkpoint is ready to resume on a pass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `components/hero/constants.ts` — the mount-delay constant is RENAMED/REPLACED: `IDLE_MOUNT_TIMEOUT_MS = 200` becomes `HERO_MOUNT_DELAY_MS = 3000` (the mount-trigger timeout floor; fires the HeroScene mount if no user interaction occurs first, set past the observed-LCP mark so three.js bootup lands post-LCP in Lantern's graph). NOT a design token; no raw hex.
- `next.config.ts` — new `experimental.inlineCss: true` key inside `nextConfig` (Lever 2; inlines the render-blocking CSS chunk into `<head>`, production-build only).
- `components/hero/HeroCanvas.tsx` — the mount trigger is rewritten (interaction-or-timeout-floor); no new exported symbol.
- No new styles/tokens.css token; no new file created.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client bundle | Preserved unchanged — Lever 1 only changes WHEN the existing client-only HeroScene chunk mounts (interaction-or-timeout), not whether three.js can reach the server bundle; the module-level `dynamic({ssr:false})` is untouched and `no-canvas-server-bundle.sh` still guards it. |
| browser → GPU | Unchanged — the scene, render loop, PerformanceMonitor, DPR floor, and DebugHook are all untouched; only the mount timing moves later. |

This plan adds NO new network, input, auth, data, or secret surface. Lever 1 defers the mount of an already-existing client-only decorative chunk behind a user-interaction-or-timeout signal. Lever 2 (`experimental.inlineCss`) changes only how the app's own CSS is delivered (inlined `<style>` vs `<link>`) — no external resource, no new origin, no user input. The passive interaction listeners are read-only mount triggers, not data collection.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering (regression) | ssr:false isolation boundary | low | mitigate | The mount-trigger change keeps the module-level `dynamic(() => import('./HeroScene'), { ssr:false })` intact; `tests/invariants/no-canvas-server-bundle.sh` (registered in `test:invariants` in 05-03) runs in this plan's verify and fails CI if three.js leaks into the server bundle — isolation stays CI-guarded across the change. |
| T-05-06 | Denial of Service (self-inflicted) | mount trigger never firing (interaction-gated with no timeout) | low | mitigate | The `setTimeout(HERO_MOUNT_DELAY_MS=3000)` floor GUARANTEES the glass eventually mounts even with zero interaction (headless Playwright / Lighthouse); the idempotent handler + cleanup only cancel a still-pending trigger on unmount, so a mounted-and-visible hero is never torn down — the Playwright happy-path spec asserts the canvas DOES appear via the floor. |

**No high-severity threats — this is a client-only load-strategy tweak (mount timing) plus a documented app-CSS-delivery config flag over an already-decorative scene; it adds no data/injection/authz/network/secret surface and keeps the isolation invariant CI-guarded. ASVS L1 unchanged.**
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds; `tests/invariants/no-canvas-server-bundle.sh` exits 0 (HERO-01 isolation intact).
- `next.config.ts` has `experimental.inlineCss: true` (Lever 2); production HTML `<head>` carries inlined `<style>` for app CSS, not a render-blocking `<link>`.
- HeroCanvas mount trigger is interaction-or-timeout-floor (Lever 1); the bare requestIdleCallback is gone; `HERO_MOUNT_DELAY_MS = 3000` in constants.ts, no new token, no raw hex (IDENT-01).
- Production Lighthouse (`next start`, Moto G4, CPU 4x): simulated LCP < 2.5s AND CLS = 0 — re-run of the 05-03 Task 2 gate — OR an honest halt (status: halted, human_judgment) recording the measured number and surfacing the D-12/observed-LCP reconciliation decision.
- `tests/sections/hero.spec.ts` green vs `next start`: canvas eventually visible in de/en at 375 + 1440 via the floor (generous timeout; canvas MUST appear).
- `tests/motion/reduced.spec.ts` green: 0 canvas under reduced-motion (D-06 gate untouched).
</verification>

<success_criteria>
The single remaining 05-03 gate failure is resolved one of two honest ways: (PASS) production Lighthouse mobile (Moto G4, CPU 4x) simulated LCP is under the D-12 < 2.5s budget and CLS is 0, because the three.js mount now fires provably post-LCP (interaction-or-3000ms-floor, replacing the buggy bare requestIdleCallback) and the render-blocking CSS is inlined via experimental.inlineCss — the 05-03 human-verify checkpoint resumes; OR (HALT) after both levers the simulated LCP stalls at ~2.6-2.9s (framework-fixed Lantern modeling of Next 16 hydration), the measured number is recorded honestly, status is halted with human_judgment, and "reconcile D-12 to observed LCP (~2811ms)" is surfaced as a user decision — no faked pass, no cherry-picked run, no unilateral budget relaxation. In both outcomes the D-06 reduced-motion / no-WebGL gate, the ssr:false isolation (CI-guarded), the CLS=0 wrapper geometry, the D-11 500ms fade-in, and IDENT-01 are all preserved.
</success_criteria>

<output>
Create `.planning/phases/05-r3f-hero/05-05-SUMMARY.md` when done. On a PASS, note that the 05-03 human-verify checkpoint (6 steps, 05-03-PLAN.md Task 3) resumes. On a HALT, surface the "reconcile D-12 to observed LCP (~2811ms) vs simulated" decision for the user.
</output>
