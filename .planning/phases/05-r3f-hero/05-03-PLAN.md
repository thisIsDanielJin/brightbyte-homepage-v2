---
phase: 05-r3f-hero
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - package.json
  - tests/sections/hero.spec.ts
  - components/hero/HeroScene.tsx
  - components/hero/GlassMesh.tsx
autonomous: false
requirements: [HERO-01, HERO-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Lighthouse mobile audit (Moto G4 profile) shows LCP < 2.5s and CLS = 0 with the hero mounted — measured against next start production build, not dev (ROADMAP SC #2, HERO-02, D-12)"
    - "Draw call count stays under 200 in a production build; PerformanceMonitor adaptive DPR scaling is active and verified on a throttled connection (ROADMAP SC #4, D-12)"
    - "WCAG AA (≥ 4.5:1) for headline and subline is verified against the ACTUAL RENDERED glass background at 375 + 1440; CTA (#FFFFFF on #1C39BB = 8.93:1) is already AAA (D-08 hard QA gate, HERO-01)"
    - "The no-canvas-server-bundle.sh invariant is registered in the package.json test:invariants chain so HERO-01 isolation is CI-guarded going forward"
    - "The R3F hero renders with zero hydration errors in production (D-12, ROADMAP SC #1)"
  artifacts: []
  key_links:
    - "package.json test:invariants → chains no-canvas-server-bundle.sh (phase-gate CI guard for HERO-01)"
    - "D-08 legibility: if AA fails against rendered glass, apply UI-SPEC Option A (offset centroid to 65-70%) or Option B (bg-surface/60 scrim) — QA gate is the decision surface"
---

<objective>
Prove the hero meets every graded ROADMAP success criterion against a production build, and close the CI loop so the isolation guarantee cannot silently regress. Plans 01–02 built and wired the scene; this plan is the gate: register the server-bundle invariant, run the Lighthouse mobile (Moto G4) LCP/CLS audit and the draw-call/PerformanceMonitor checks against `next start`, verify WCAG AA of the copy against the actual rendered glass (D-08), and hold a human-verify checkpoint for the manual perf + legibility surfaces that cannot be asserted statically.

Purpose: The perf budget (D-12) and the legibility gate (D-08) are the two things that decide whether the hero ships; both require production measurement and human judgment.
Output: `no-canvas-server-bundle.sh` registered in `test:invariants`, a WCAG-AA-against-rendered-glass Playwright check, and a signed-off human-verify of Lighthouse LCP/CLS + draw calls < 200 + PerformanceMonitor DPR adaptation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-r3f-hero/05-CONTEXT.md
@.planning/phases/05-r3f-hero/05-RESEARCH.md
@.planning/phases/05-r3f-hero/05-UI-SPEC.md
@.planning/phases/05-r3f-hero/05-VALIDATION.md
@.planning/phases/05-r3f-hero/05-01-SUMMARY.md
@.planning/phases/05-r3f-hero/05-02-SUMMARY.md
@tests/sections/hero.spec.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the server-bundle invariant + add the WCAG-AA-against-rendered-glass Playwright check</name>
  <files>package.json, tests/sections/hero.spec.ts</files>
  <read_first>
    - package.json (self — the test:invariants script chain to extend; RESEARCH Wave 0 gap "register in test:invariants" §PATTERNS L532-534)
    - tests/invariants/no-canvas-server-bundle.sh (created in Plan 01 — the invariant to register)
    - tests/invariants/no-raw-hex.sh (analog for how invariants are chained in the script with &&)
    - tests/sections/hero.spec.ts (existing hero suite + the canvas assertions from Plan 01 — extend with the contrast check)
    - tests/a11y/contrast.spec.ts (existing WCAG contrast test pattern — the analog for sampling rendered pixels + computing ratio)
    - tests/a11y/axe.spec.ts (existing axe-playwright pattern for reference)
    - .planning/phases/05-r3f-hero/05-UI-SPEC.md (Legibility scrim contract §L120-145 — Option A offset / Option B scrim; Accessibility Contract §L419-431; targets: headline #18181B ≥ 4.5:1, subline #52525B ≥ 4.5:1, CTA #FFFFFF on #1C39BB = 8.93:1 AAA)
    - .planning/phases/05-r3f-hero/05-VALIDATION.md (Manual-Only Verifications §L61-68 — WCAG AA against rendered glass is screenshot-based)
  </read_first>
  <action>
    Register the invariant: edit the `test:invariants` script in package.json to chain `bash tests/invariants/no-canvas-server-bundle.sh` after the existing four invariants (with `&&`). Because it requires a build, note in the SUMMARY that it is run at the phase gate; the chain still runs it whenever `npm run test:invariants` is invoked after a build exists (the script builds if `.next/server` is absent).
    Add a WCAG-AA-against-rendered-glass check to `tests/sections/hero.spec.ts`, in the existing locale loop, at both mobile-375 and desktop (1440) viewports: after the canvas is visible and the scene has painted (await a short settle so the glass has rendered a frame), take a full-page screenshot of the hero region, sample the rendered pixel color directly behind the headline bounding box and behind the subline bounding box (use `page.locator('#hero h1').boundingBox()` / `#hero p` to locate the region, read pixels from the screenshot buffer at those coordinates), compute the WCAG contrast ratio of the text token color (#18181B headline, #52525B subline) against the sampled composite background, and assert ≥ 4.5:1. The CTA is already AAA (8.93:1) — assert it too for completeness but expect pass. If the assertion fails, the executor applies UI-SPEC Option A (offset glass centroid to 65-70% horizontal via GlassMesh position) or Option B (add a `bg-surface/60` scrim pseudo-element over the copy region on the Canvas wrapper) — Option A preferred, both token-compliant, no raw hex; re-run until AA holds. Do NOT weaken the 4.5:1 threshold. Do NOT change the text token colors (UI-SPEC — type is immutable).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.scripts['test:invariants'].includes('no-canvas-server-bundle.sh'))throw new Error('invariant not registered');console.log('registered ok')" && npm run build && (npm run start & SP=$!; sleep 6; npx playwright test tests/sections/hero.spec.ts; R=$?; kill $SP 2>/dev/null; exit $R)</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `test:invariants` chains `bash tests/invariants/no-canvas-server-bundle.sh`.
    - `tests/sections/hero.spec.ts` contains a WCAG-AA contrast assertion sampling the rendered glass composite behind headline + subline at 375 and 1440, asserting ≥ 4.5:1, in both locales.
    - The contrast check is green (Option A/B applied if it initially failed); threshold not weakened; text token colors unchanged.
    - `next build` + production Playwright run green.
  </acceptance_criteria>
  <done>The isolation invariant is CI-registered and the D-08 legibility gate passes against the real rendered glass at both breakpoints — committed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the production Lighthouse mobile (Moto G4) LCP/CLS audit and capture draw-call + PerformanceMonitor evidence</name>
  <files>tests/screenshots/hero-lighthouse-de.json, components/hero/HeroScene.tsx, components/hero/GlassMesh.tsx</files>
  <read_first>
    - playwright.config.ts (webServer uses `npm run start` — production; the gate MUST run vs next start not dev, RESEARCH §L689, VALIDATION §L25)
    - .planning/phases/05-r3f-hero/05-RESEARCH.md (Pitfall 4 dev-vs-prod draw calls §L402-406; Environment Availability — Lighthouse via `npx lighthouse@latest`, no global install §L668-674; Open Question 1 mobile LCP baseline §L642-646)
    - .planning/phases/05-r3f-hero/05-UI-SPEC.md (Performance Contract §L433-449 — LCP < 2.5s, CLS = 0, draw calls < 200, PerformanceMonitor active, 0 hydration errors, 0 canvas on server)
    - .planning/phases/05-r3f-hero/05-VALIDATION.md (Manual-Only Verifications §L61-68 — Lighthouse Moto G4, renderer.info.render.calls, throttle DPR step-down)
  </read_first>
  <action>
    Build and start the production server (`npm run build && npm run start`). Run Lighthouse mobile against the hero route with a Moto G4-approximating profile: `npx lighthouse@latest http://localhost:3000/de --only-categories=performance --form-factor=mobile --throttling.cpuSlowdownMultiplier=4 --output=json --output-path=./tests/screenshots/hero-lighthouse-de.json --chrome-flags="--headless"`. Extract and record LCP (must be < 2.5s) and CLS (must be 0) from the JSON. Confirm the `.hero-backdrop` gradient is the LCP element (LCP audit `largestContentfulPaintElement` should reference the gradient/hero container, not the canvas) — this proves the D-11 LCP-protection principle.
    Capture draw-call evidence via a concrete debug hook (no prose-only measurement): the R3F scene (built in Plan 02) exposes its renderer for read-only inspection by assigning the fiber store to `window.__r3f_hero` inside the Canvas via a one-line child component that calls `useThree((s) => s.gl)` and, in a `useEffect`, sets `window.__r3f_hero = { calls: () => gl.info.render.calls, dpr: () => gl.getPixelRatio() }`. This hook is unconditional (not production-guarded) so the gate can read it against `next start`; it exposes only two read functions over the already-decorative canvas — no new data or input surface (threat register unchanged). If Plan 02 did not add this hook, add the small child component now as part of this task (scoped edit to `components/hero/HeroScene.tsx` — mount-only, no scene/material change). Then in a throttled Playwright session (CPU 4x) `await page.waitForFunction(() => !!window.__r3f_hero)`, read `page.evaluate(() => window.__r3f_hero.calls())` and assert < 200. Confirm PerformanceMonitor adaptive DPR responds: read `window.__r3f_hero.dpr()` under CPU throttle, observe it step DOWN, and step back UP on release — assert the observed floor never drops below 1.0. Record all measured values (LCP, CLS, draw calls, DPR floor observed) into the SUMMARY for the human-verify checkpoint. Do NOT report dev-server numbers (Pitfall 4). If LCP/CLS/draw-call budget is missed, this is where the locked mobile degradation strategy (Plan 02 checkpoint) is re-tuned — reduce samples/resolution or invoke Tier 2, then re-measure; do NOT relax the budget (D-12 is non-negotiable). Escalation across the wave boundary: if the tier locked at the Plan 02 checkpoint misses the perf budget here at the Plan 03 gate, do NOT re-open Plan 02 — the fix is a scoped edit to `components/hero/GlassMesh.tsx` WITHIN Plan 03, dropping to Tier 2 (already researched — RESEARCH Pattern 5: fewer samples / lower resolution / non-transmissive fallback material). Add `components/hero/GlassMesh.tsx` to this task's edit surface only if that escalation fires. Tier 2's non-transmissive path MUST still pass the D-08 rendered-glass contrast check (Task 1's WCAG-AA-against-rendered-glass assertion) — re-run Task 1's contrast gate after any Tier 2 edit; a Tier 2 that regresses legibility is not acceptable.
  </action>
  <precondition>Production build runnable locally (`npm run build && npm run start`); Chrome/Chromium available for Lighthouse (Playwright bundles Chromium; `npx lighthouse` uses system or bundled Chrome).</precondition>
  <verify>
    <automated>npm run build && (npm run start & SP=$!; sleep 6; npx lighthouse@latest http://localhost:3000/de --only-categories=performance --form-factor=mobile --throttling.cpuSlowdownMultiplier=4 --output=json --output-path=./tests/screenshots/hero-lighthouse-de.json --chrome-flags="--headless" ; R=$?; kill $SP 2>/dev/null; node -e "const r=require('./tests/screenshots/hero-lighthouse-de.json');const lcp=r.audits['largest-contentful-paint'].numericValue;const cls=r.audits['cumulative-layout-shift'].numericValue;console.log('LCP(ms)=',lcp,'CLS=',cls);if(lcp>=2500)throw new Error('LCP >= 2.5s: '+lcp);if(cls>0)throw new Error('CLS > 0: '+cls);console.log('perf budget OK')"; exit $R)</automated>
  </verify>
  <acceptance_criteria>
    - Lighthouse JSON captured at `tests/screenshots/hero-lighthouse-de.json` from a `next start` run (not dev).
    - LCP < 2.5s and CLS = 0 with the hero mounted (assertion in verify command).
    - LCP element is the gradient/hero container, not the canvas (D-11 protection confirmed).
    - Draw calls < 200 read via `window.__r3f_hero.calls()` in a throttled Playwright `page.evaluate`, and PerformanceMonitor DPR step-down read via `window.__r3f_hero.dpr()` (floor ≥ 1.0) — both recorded in SUMMARY for the checkpoint.
    - No dev-server numbers reported.
  </acceptance_criteria>
  <done>Production Lighthouse LCP < 2.5s, CLS = 0, draw calls < 200, and PerformanceMonitor DPR adaptation are measured and recorded against next start — the D-12 budget is met (or degradation re-tuned until it is).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify the hero taste bar + all four ROADMAP success criteria</name>
  <what-built>The complete R3F frosted-glass hero: isolated via next/dynamic({ssr:false}), token-lit single rotating glass solid behind the hero text, offscreen-paused, PerformanceMonitor/AdaptiveDpr active, reduced-motion → gradient fallback. Task 1 registered the isolation invariant and passed the WCAG-AA-against-rendered-glass gate; Task 2 captured production Lighthouse LCP/CLS + draw calls + DPR adaptation.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run start`, open http://localhost:3000/de — confirm the glass fades in over the gradient (no pop-in, no spinner), rotates slowly and calmly, and reads as refined/quietly-stunning, NOT a flashy tech demo (D-01 taste bar; PROJECT.md "never startup-flashy to an SMB owner").
    2. Read the headline/subline over the glass at desktop AND resize to ~375px — confirm the copy is comfortably legible (D-08); the Task 1 automated AA check backs this but confirm it looks right to the eye.
    3. Scroll the hero out of view and back — confirm no flash/remount and (via DevTools Performance or the recorded evidence) that the loop pauses offscreen (D-05).
    4. Open DevTools, set CPU 4x throttle — confirm the scene stays smooth as PerformanceMonitor steps DPR down (D-12 / ROADMAP SC #4); review the recorded draw-call count (< 200) and Lighthouse LCP (< 2.5s) / CLS (0) from Task 2's SUMMARY.
    5. Toggle OS "reduce motion" (or emulate in DevTools rendering) and reload — confirm NO canvas renders, only the gradient, with zero layout shift (D-06 / ROADMAP SC #3).
    6. Confirm the browser console shows zero hydration warnings on the hero route (ROADMAP SC #1).
  </how-to-verify>
  <resume-signal>Type "approved" if the hero meets the taste bar and all four ROADMAP success criteria hold, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client bundle | This plan CI-registers the invariant that guards it (no three.js/WebGL in the server bundle — HERO-01). |
| browser → GPU | This plan measures and signs off the resource-consumption mitigations (frameloop pause, PerformanceMonitor DPR) under throttling. |

Plan 03 is verification + CI wiring only; it adds no new network, input, auth, data, or secret surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | WebGL render loop (GPU/battery) | low | mitigate (M-01) | This plan VERIFIES the mitigation under throttling: PerformanceMonitor DPR step-down confirmed, draw calls < 200 confirmed, offscreen pause confirmed at the human-verify checkpoint. |
| T-05-05 | Tampering (regression) | ssr:false isolation boundary | low | mitigate (M-03) | `no-canvas-server-bundle.sh` registered in `test:invariants` — future changes that leak Canvas/WebGL into the server bundle fail CI, preventing silent HERO-01 regression. |

**No high-severity threats — this is a verification/gate plan over a client-only decorative scene; it closes the CI loop (M-03) and confirms M-01 empirically. No new data/injection/authz surface. ASVS L1 unchanged.**
</threat_model>

<verification>
- `test:invariants` includes `no-canvas-server-bundle.sh` (HERO-01 CI guard).
- WCAG-AA-against-rendered-glass check green at 375 + 1440, both locales (D-08).
- Production Lighthouse: LCP < 2.5s, CLS = 0, LCP element is the gradient (D-11/D-12).
- Draw calls < 200; PerformanceMonitor DPR step-down under throttle; offscreen pause — recorded and human-verified.
- Zero hydration warnings in production console.
- Full production Playwright suite (`npm run test:sections` vs next start) green; accepted pre-existing mobile-375 language-switcher failure remains accepted (not introduced by this phase).
</verification>

<success_criteria>
All four ROADMAP Phase 5 success criteria are proven against a production build: (1) ssr:false isolation with zero hydration errors, now CI-guarded; (2) LCP < 2.5s + CLS = 0 on Moto G4; (3) reduced-motion → no Canvas, no shift; (4) draw calls < 200 + PerformanceMonitor adaptive DPR active. Legibility (D-08) holds against the rendered glass. The hero meets the calm-editorial taste bar at the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05-r3f-hero/05-03-SUMMARY.md` when done.
</output>
