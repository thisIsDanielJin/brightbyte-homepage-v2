---
phase: 04-2d-marketing-sections
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/[locale]/page.tsx
  - components/sections/ServicesSection.tsx
  - components/sections/PricingSection.tsx
  - components/sections/WorkSection.tsx
  - components/sections/TestimonialsSection.tsx
  - components/sections/AboutSection.tsx
  - lib/sanity/queries.ts
  - sanity/schemaTypes/siteSettings.ts
  - sanity.types.ts
  - messages/de.json
  - messages/en.json
  - tests/sections/services.spec.ts
  - tests/sections/pricing.spec.ts
  - tests/sections/work.spec.ts
  - tests/sections/testimonials.spec.ts
  - tests/sections/about.spec.ts
autonomous: false
requirements: [SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-10, SEC-11, QA-01, QA-02, QA-03]

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Services renders the 2 seeded tiers in plain language; 1-col at 375px, 2-col at 1440px; hidden entirely when 0 services (SEC-02)."
    - "Pricing shows tier prices sourced ONLY from Sanity (priceFrom -> 'ab €X'/'from €X'; priceOnRequest -> 'Auf Anfrage'/'On request') — never hardcoded, never old v1 figures (SEC-03)."
    - "Work grid renders 4-6 project cards (image + name + outcome note) with a subtle hover lift, no links; auto-wraps all projects; shows the empty-state copy when 0 (SEC-04, D-11)."
    - "Testimonials renders exactly 3 cards with the outcome metric as its own visual field above the quote (SEC-05); hidden when 0."
    - "About renders name + solo-studio framing + body; photo when present, else the 'DJ' initials fallback — no broken layout (SEC-06)."
    - "All five sections render correctly in DE+EN at 375px and 1440px with no overflow/CLS (SEC-10)."
    - "Each section uses the whisper-quiet entrance fade and is disabled under prefers-reduced-motion (SEC-11); axe zero violations (QA-03)."
  artifacts:
    - "components/sections/ServicesSection.tsx"
    - "components/sections/PricingSection.tsx"
    - "components/sections/WorkSection.tsx"
    - "components/sections/TestimonialsSection.tsx"
    - "components/sections/AboutSection.tsx"
    - "tests/sections/services.spec.ts"
    - "tests/sections/pricing.spec.ts"
    - "tests/sections/work.spec.ts"
    - "tests/sections/testimonials.spec.ts"
    - "tests/sections/about.spec.ts"
  key_links:
    - "app/[locale]/page.tsx Promise.all fetches getServices/getProjects/getTestimonials/getSiteSettings(locale) and passes data as props (single page-level fetch, no per-section fetch — Pattern 2)."
    - "Sections mount in D-05 DOM order between Hero and Contact: #services -> #pricing -> #work -> #testimonials -> #about."
    - "Pricing reads from the services price object (price.amount/currency/label/priceFrom + priceOnRequest)."
    - "WorkSection images via urlFor() from lib/sanity/image.ts with reserved 4/3 aspect box."
---

<objective>
**As a** Berlin SMB owner scrolling the home page,
**I want to** see plain-language services, transparent fixed pricing, real project outcomes, verified testimonials, and a human "you work directly with me" about section,
**so that** my main questions (what, how much, does it work, who) are answered before I reach the contact form.

Purpose: Replicate the proven 04-01 vertical (Sanity read -> RSC -> token section -> motion -> QA) across the five remaining content sections, in the D-05 order.
Output: Five finished sections composed into the home page, each through the per-section QA loop, plus the About photo-field schema addition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-2d-marketing-sections/04-CONTEXT.md
@.planning/phases/04-2d-marketing-sections/04-RESEARCH.md
@.planning/phases/04-2d-marketing-sections/04-UI-SPEC.md
@.planning/phases/04-2d-marketing-sections/04-VALIDATION.md
@.planning/phases/04-2d-marketing-sections/04-01-SUMMARY.md
@AGENTS.md
@lib/sanity/queries.ts
@lib/sanity/image.ts
@styles/tokens.css
@sanity/schemaTypes/siteSettings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ServicesSection + PricingSection (from services data) + compose into page</name>
  <files>components/sections/ServicesSection.tsx, components/sections/PricingSection.tsx, app/[locale]/page.tsx, messages/de.json, messages/en.json, tests/sections/services.spec.ts, tests/sections/pricing.spec.ts</files>
  <behavior>
    - Services: 2 seeded tiers render as cards (title, blurb, includes list with ✓ prefix, price); 1-col at 375px / 2-col at 1440px; section hidden when 0 services.
    - Pricing: renders the two tiers as pricing cards; priceFrom -> 'ab €X'/'from €X' at text-5xl; priceOnRequest -> 'Auf Anfrage'/'On request' (no numeric); values ONLY from Sanity price object; hidden when 0.
    - Both sections render DE+EN at 375px+1440px with no overflow.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for Next 16 APIs before writing code (AGENTS.md).
    Edit app/[locale]/page.tsx: add a page-level Promise.all fetching getServices(locale), getProjects(locale), getTestimonials(locale) alongside the existing getSiteSettings(locale) (Pattern 2 — single page-level fetch, pass as props). Locale from awaited params only.
    Build components/sections/ServicesSection.tsx (RSC content wrapped by MotionSection) per UI-SPEC "Services Section": section id="services", eyebrow + heading, service cards (bg-surface border-border, p-6, title text-2xl semibold, blurb text-base secondary, includes list text-sm secondary with a ✓ Unicode prefix in text-accent — no icon library). Price display per UI-SPEC (priceFrom prepends "ab "/"from "; priceOnRequest shows "Auf Anfrage"/"On request"). Return null / render nothing when services.length === 0 (UI Considerations row 9). No hover, no links.
    Build components/sections/PricingSection.tsx (RSC + MotionSection) per UI-SPEC "Pricing Section": section id="pricing", bg-surface-subtle, 2-col max-width 900px centered at 1440 / stacked at 375. Price text-5xl bold from Sanity price.amount+price.currency; priceFrom renders "ab €X"; priceOnRequest renders the on-request label. Per-tier outlined CTA (border border-accent text-accent, hover bg-accent text-surface, href="#contact"). Hidden when 0 tiers.
    NEVER hardcode a price; NEVER reintroduce old v1 pricing (€35/h, €450, Freundespreis) — figures come exclusively from Sanity (freelancer.md). Add pricing/services i18n section headings + eyebrow labels to messages per Copywriting Contract.
    Scaffold tests/sections/services.spec.ts and tests/sections/pricing.spec.ts (de+en x 375/1440 screenshot + visibility + price-format assertions per 04-RESEARCH.md). Zero raw hex / no gray-* (IDENT-01).
    Per-section QA loop (D-02): capture screenshots, iterate, run ui-skills critique per section (see human-check), confirm axe.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:invariants && npx playwright test tests/sections/services.spec.ts tests/sections/pricing.spec.ts tests/a11y/axe.spec.ts</automated>
    <human-check>QA-02: `npx ui-skills start` -> load the smallest relevant pricing/card skill -> critique Services + Pricing desktop+mobile screenshots (de+en); iterate until calm-editorial. Confirm NO hardcoded/old pricing appears — every figure traces to Sanity.</human-check>
  </verify>
  <done>Services + Pricing render finished in DE+EN at both viewports from Sanity; prices are Sanity-only with correct priceFrom/priceOnRequest formatting; sections hide on 0 data; specs + axe green; ui-skills critique passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkSection (image grid, hover lift, empty state) + TestimonialsSection (metric-above-quote)</name>
  <files>components/sections/WorkSection.tsx, components/sections/TestimonialsSection.tsx, app/[locale]/page.tsx, messages/de.json, messages/en.json, tests/sections/work.spec.ts, tests/sections/testimonials.spec.ts</files>
  <behavior>
    - Work: 4-6 cards (image via urlFor with reserved 4/3 aspect box, name text-base semibold, outcome note text-sm secondary); grid-cols-2 lg:grid-cols-3 auto-wrapping all projects; hover lift translate-y-[-4px] shadow-md (CSS transition, cursor-default, no links); missing image -> surface-muted block same aspect; empty state copy when 0 projects.
    - Testimonials: exactly 3 cards; outcomeValue rendered as text-4xl bold above the quote (own visual field), outcomeLabel below, then rule, quote (italic secondary), author, company; 3-col at 1440 / stacked at 375; hidden when 0.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for the Next 16 next/image API before writing code (AGENTS.md).
    Build components/sections/WorkSection.tsx (RSC + MotionSection) per UI-SPEC "Work Grid" + D-11: section id="work", grid-cols-2 lg:grid-cols-3 gap-6 (auto-wrap, show all — UI Considerations row 30, no max cap). Each card: bg-surface-muted, next/image with fill inside an aspect-[4/3] relative wrapper, src from urlFor(project.image).width(800).height(600).fit('crop').auto('format').url(), sizes per UI-SPEC; missing image -> surface-muted block. Name + outcome note below. Hover lift via CSS transition (duration-standard ease-standard), cursor-default, NO links (D-11). When projects.length === 0 render the "Projekte folgen in Kürze"/"Projects coming soon" empty state (Copywriting Contract, UI Considerations row 25).
    Build components/sections/TestimonialsSection.tsx (RSC + MotionSection) per UI-SPEC "Testimonials Section": section id="testimonials", 3-col grid at 1440 / stacked at 375. Card: outcomeValue text-4xl bold text-primary as its OWN field above the quote, outcomeLabel text-sm secondary below it, border-t rule, quote text-base secondary italic, author text-sm semibold, company text-sm secondary. Hidden when 0 (no "coming soon" for social proof — row 32).
    Edit app/[locale]/page.tsx to mount <WorkSection projects={projects} /> and <TestimonialsSection testimonials={testimonials} /> in D-05 order (after Pricing, before About). Add work/testimonials headings + empty-state copy to messages. Zero raw hex / no gray-* (IDENT-01).
    Scaffold tests/sections/work.spec.ts and tests/sections/testimonials.spec.ts (de+en x 375/1440; work: 4+ cards + outcome notes present + long-quote column equalization backstop row 39; testimonials: metric rendered separately from quote). Per-section QA loop (D-02) + ui-skills critique + axe.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:invariants && npx playwright test tests/sections/work.spec.ts tests/sections/testimonials.spec.ts tests/a11y/axe.spec.ts</automated>
    <human-check>QA-02: `npx ui-skills start` -> load the smallest relevant grid/card skill -> critique Work + Testimonials desktop+mobile screenshots (de+en); confirm the outcome metric reads as its own callout above each quote and the work hover lift is subtle.</human-check>
  </verify>
  <done>Work grid renders all projects with hover lift + empty state + no links; Testimonials render 3 cards with the metric as its own field; both DE+EN at both viewports; specs + axe green; ui-skills critique passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AboutSection (photo-or-initials fallback) + aboutPhoto schema field + compose</name>
  <files>components/sections/AboutSection.tsx, app/[locale]/page.tsx, sanity/schemaTypes/siteSettings.ts, lib/sanity/queries.ts, sanity.types.ts, messages/de.json, messages/en.json, tests/sections/about.spec.ts</files>
  <behavior>
    - About renders heading (name) + solo-studio subline + body; photo-above-text at 375px, 40/60 two-column at 1440px.
    - When aboutPhoto is present -> next/image rounded-full in a reserved 1/1 aspect box; when absent -> circular surface-muted container with "DJ" initials (text-2xl semibold secondary). No broken layout either way.
    - Body "you work directly with me" framing comes from Sanity siteSettings with a next-intl fallback.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for the Next 16 next/image API before writing code.
    Add an optional `aboutPhoto` image field (hotspot) to sanity/schemaTypes/siteSettings.ts per 04-RESEARCH.md "Flag SEC-06" (optional — initials shown if absent). Append `aboutPhoto` to SITE_SETTINGS_QUERY projection in lib/sanity/queries.ts (do not add a second client / token). Run `npm run types:sanity` to regenerate sanity.types.ts. This is additive; no re-seed required to ship (fallback covers absence) — Daniel can upload a photo later with no code change (deferred asset, CONTEXT.md).
    Build components/sections/AboutSection.tsx (RSC + MotionSection) per UI-SPEC "About Section" using the exact photo/fallback pattern in 04-RESEARCH.md Flag SEC-06: photo present -> next/image object-cover rounded-full 120px/160px in a relative 1/1 box; absent -> circular bg-surface-muted with "DJ" initials. Heading "Daniel Jin Wodke", subline solo-studio framing, body from siteSettings with next-intl fallback. No social feed links (anti-feature). 375px photo-above-text, 1440px 40/60 two-column.
    Edit app/[locale]/page.tsx to mount <AboutSection settings={settings} /> in D-05 order (after Testimonials, before the Contact placeholder that 04-03 fills). Add about heading/subline i18n keys per Copywriting Contract. Zero raw hex / no gray-* (IDENT-01).
    Scaffold tests/sections/about.spec.ts (de+en x 375/1440; assert initials fallback renders and no layout break when photo absent — the ship-now state). Per-section QA loop (D-02) + ui-skills critique + axe.
  </action>
  <verify>
    <automated>npm run types:sanity && npx tsc --noEmit && npm run test:invariants && npx playwright test tests/sections/about.spec.ts tests/a11y/axe.spec.ts</automated>
    <human-check>QA-02: `npx ui-skills start` -> load the smallest relevant profile/about skill -> critique About desktop+mobile screenshots (de+en); confirm the initials fallback reads intentional (not broken) and the "you work directly with me" framing is warm and human.</human-check>
  </verify>
  <done>About renders finished with the initials fallback (ship-now) in DE+EN at both viewports; aboutPhoto field + projection added; when a photo is later uploaded it renders with no code change; specs + axe green; ui-skills critique passed. All five content sections now compose the home page in D-05 order.</done>
  <reversibility rating="reversible">aboutPhoto is an optional additive field; the initials fallback is the durable ship-now path.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity dataset -> RSC read | Public tokenless read of services/projects/testimonials/siteSettings (D-03) |
| Sanity image asset -> next/image | urlFor()-built CDN image URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | Pricing figures | high | mitigate | Prices rendered ONLY from Sanity price object; no hardcoded literals; old v1 pricing never authored. QA human-check asserts every figure traces to Sanity. |
| T-04-05 | Information Disclosure | Sanity read client | high | mitigate | Reuse the single tokenless stega:false client; `sanity-single-client.sh` + `no-stega-in-build.sh` gates stay green. |
| T-04-06 | Tampering | Sanity image URL injection | low | accept | urlFor() builds URLs from asset refs; images are public marketing assets; React escapes alt/name text. Low impact. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run test:invariants` green (IDENT-01, stega, locale, single-client).
- `npm run types:sanity` regenerates sanity.types.ts cleanly (aboutPhoto typed).
- `npx playwright test tests/sections/*.spec.ts tests/a11y/axe.spec.ts` green in de+en at 375px+1440px.
- ui-skills critique (QA-02) passed for each of the five sections.
</verification>

<success_criteria>
- Services, Pricing, Work, Testimonials, About all render finished in DE+EN at 375px+1440px.
- Pricing is Sanity-only; no hardcoded or old v1 figures.
- Work auto-wraps all projects with hover lift + empty state; Testimonials show the metric as its own field.
- About ships with the initials fallback; photo swaps in later with no code change.
- Entrance motion whisper-quiet, disabled under reduced-motion; axe zero violations.
</success_criteria>

<output>
Create `.planning/phases/04-2d-marketing-sections/04-02-SUMMARY.md` when done.
</output>
