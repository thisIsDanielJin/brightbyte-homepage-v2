---
phase: 04-2d-marketing-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - sanity/schemaTypes/siteSettings.ts
  - lib/sanity/queries.ts
  - sanity.types.ts
  - messages/de.json
  - messages/en.json
  - app/[locale]/layout.tsx
  - app/[locale]/page.tsx
  - app/globals.css
  - components/layout/Header.tsx
  - components/layout/Footer.tsx
  - components/ui/MotionSection.tsx
  - components/sections/HeroSection.tsx
  - tests/sections/hero.spec.ts
  - tests/layout/header.spec.ts
  - tests/a11y/axe.spec.ts
  - tests/motion/reduced.spec.ts
autonomous: false
requirements: [SEC-01, SEC-08, SEC-11, QA-01, QA-02, QA-03]
user_setup:
  - service: sanity
    why: "D-03: Sanity production dataset must be publicly readable for tokenless build-time reads to return content. No token is added."
    dashboard_config:
      - task: "Set the `production` dataset visibility to Public"
        location: "sanity.io/manage/project/ddrca30s/datasets -> production -> Public"
  - service: sanity-studio
    why: "D-07: Hero headline/subline are new siteSettings fields. After the schema push, both DE and EN siteSettings documents must be authored with heroHeadline + heroSubline, or the hero renders next-intl fallback copy."
    dashboard_config:
      - task: "Author heroHeadline + heroSubline on both DE and EN siteSettings documents"
        location: "Sanity Studio (/studio) -> Site Settings (DE) and (EN)"

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "`/de` and `/en` render a real header (logo + nav), a fully-built Hero section, and a real dark-surface footer — end to end, from live Sanity siteSettings copy."
    - "Hero headline + subline come from Sanity siteSettings (D-07); when absent, next-intl fallback copy renders (no crash)."
    - "Hero sits in a `min-h-svh relative` container so Phase 5 can swap the backdrop for an R3F canvas with zero CLS (D-06)."
    - "Header is sticky, shows a frosted backdrop on scroll, collapses to a hamburger overlay at 375px, and highlights the active anchor section (SEC-08)."
    - "Footer renders on `--color-surface-dark` with `--color-on-dark` text — the intended dark accent moment (D-13)."
    - "Section entrance uses a whisper-quiet fade+8-12px rise once on view; no animation runs under prefers-reduced-motion (SEC-11, D-14)."
    - "axe-playwright reports zero violations on `/de` and `/en`; all interactive elements have :focus-visible (QA-03)."
  artifacts:
    - "components/layout/Header.tsx"
    - "components/layout/Footer.tsx"
    - "components/ui/MotionSection.tsx"
    - "components/sections/HeroSection.tsx"
    - "playwright.config.ts"
    - "tests/sections/hero.spec.ts"
    - "tests/layout/header.spec.ts"
    - "tests/motion/reduced.spec.ts"
    - "tests/a11y/axe.spec.ts"
  key_links:
    - "app/[locale]/page.tsx fetches getSiteSettings(locale) and passes heroHeadline/heroSubline to HeroSection."
    - "SITE_SETTINGS_QUERY projection includes heroHeadline, heroSubline (and later impressumBody/datenschutzBody in 04-04)."
    - "app/[locale]/layout.tsx mounts Header (top) and Footer (bottom) around {children}, replacing the switcher-only header."
    - "MotionSection wraps section content and zeroes the transition under useReducedMotion()."
---

<objective>
**As a** Berlin small-business owner landing on brightbyte.berlin,
**I want to** immediately see a calm, confident hero with a clear positioning statement, a real navigation header, and a professional footer,
**so that** I trust the studio's craft within the first few seconds.

This is the phase TRACER: it wires the entire vertical — Sanity read -> Server Component -> token-styled section -> motion -> per-section QA loop — end to end, on exactly ONE fully-built section (Hero), plus the real header/footer shell that every other section will live inside. It proves the architecture before 04-02 replicates it across the remaining six sections.

Purpose: De-risk the whole phase on one slice. If the Sanity-read -> RSC -> token -> motion -> axe path works for Hero, it works for every section.
Output: Wave 0 test/dependency scaffolds; siteSettings schema additions (heroHeadline/heroSubline) + re-seed; the real Header + Footer + MotionSection; a finished Hero section; and a green per-section QA loop (screenshots + axe) for Hero and the shell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-2d-marketing-sections/04-CONTEXT.md
@.planning/phases/04-2d-marketing-sections/04-RESEARCH.md
@.planning/phases/04-2d-marketing-sections/04-UI-SPEC.md
@.planning/phases/04-2d-marketing-sections/04-VALIDATION.md
@AGENTS.md
@lib/sanity/queries.ts
@lib/sanity/client.ts
@app/[locale]/layout.tsx
@app/[locale]/page.tsx
@components/LocaleSwitcher.tsx
@i18n/navigation.ts
@styles/tokens.css
@sanity/schemaTypes/siteSettings.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: USER ACTION — enable Sanity public read + confirm Resend key availability</name>
  <instructions>
    This is the external precondition for every Sanity-fed section (D-03). No code is built here.
    1. Set the `production` dataset to Public at sanity.io/manage/project/ddrca30s/datasets -> production -> Public.
    2. Confirm a Resend account exists and a `RESEND_API_KEY` is available (needed in 04-03, not now) — add it to `.env.local` and the Vercel project env when ready.
    3. After the toggle, run `npm run test:content` (anonymous) and confirm it returns Sanity documents (not empty arrays).
  </instructions>
  <action>USER ACTION only — no automated implementation. The executor pauses until the dataset is Public and anonymous reads return content.</action>
  <verification>Run `npm run test:content`; it must return live Sanity documents (not empty arrays), confirming the D-03 public-read toggle is applied.</verification>
  <verify>
    <automated>npm run test:content</automated>
  </verify>
  <done>The `production` dataset is Public; `npm run test:content` returns live Sanity content anonymously; a RESEND_API_KEY is known to be available for 04-03.</done>
  <resume-signal>Type "approved" once the dataset is Public and `npm run test:content` returns content. If still empty, report the anon read result.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — install resend, extend Playwright config, scaffold section/a11y/motion specs</name>
  <precondition>user_setup: Task 1 approved — `npm run test:content` returns live Sanity content anonymously.</precondition>
  <files>package.json, playwright.config.ts, tests/sections/hero.spec.ts, tests/layout/header.spec.ts, tests/a11y/axe.spec.ts, tests/motion/reduced.spec.ts</files>
  <action>
    Read node_modules/next/dist/docs/ for any Next 16 API touched here before writing code (AGENTS.md).
    Run `npm install resend` (RESEARCH "Must Install Before Implementation" — the only missing runtime dep; needed by 04-03, install now so Wave 0 is complete).
    Create playwright.config.ts if absent (extend if present) with two viewport projects — 375x812 and 1440x900 — and a baseURL for the dev/preview server covering both `/de` and `/en`. Use the projects+screenshot patterns from 04-RESEARCH.md "Validation Architecture". No watch-mode flags.
    Scaffold the Wave 0 spec files this plan needs (per 04-VALIDATION.md Wave 0 Requirements — the remaining specs are scaffolded by their owning plans):
      - tests/sections/hero.spec.ts: for each locale in [de,en] x each viewport, goto /$locale, wait for load, assert #hero visible, screenshot #hero. Assert the hero headline text node is non-empty.
      - tests/layout/header.spec.ts: header visible at both viewports; at 375px the desktop nav links are hidden and a hamburger control is present; at 1440px the anchor nav links (#services,#pricing,#work,#contact) are present; after scrolling, the sticky backdrop class is applied.
      - tests/a11y/axe.spec.ts: AxeBuilder with tags ['wcag2a','wcag2aa'] on /de and /en; expect zero violations (pattern from 04-RESEARCH.md). This is the QA-03 gate reused by every later plan.
      - tests/motion/reduced.spec.ts: newContext({ reducedMotion:'reduce' }); goto /de; assert #hero visible immediately and its computed transform is 'none' (SEC-11 / D-14).
    Wire an npm script so `npm run test` (or a new `test:sections`) runs the invariants + axe + section specs; keep existing `test:invariants` and `test:content` untouched (regression gate). Do NOT weaken existing invariants.
  </action>
  <verify>
    <automated>npm install resend >/dev/null 2>&1 && npx tsc --noEmit && npm run test:invariants && npx playwright test tests/a11y/axe.spec.ts --list</automated>
  </verify>
  <done>resend is in package.json; playwright.config.ts has 375px + 1440px projects; the four spec files exist and are discovered by Playwright; tsc clean; invariants still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: siteSettings schema additions (heroHeadline/heroSubline) + query projection + typegen + re-seed</name>
  <files>sanity/schemaTypes/siteSettings.ts, lib/sanity/queries.ts, sanity.types.ts</files>
  <behavior>
    - After schema push + typegen, `SITE_SETTINGS_QUERY` result type includes `heroHeadline: string | null` and `heroSubline: string | null`.
    - `getSiteSettings('de')` and `getSiteSettings('en')` return the authored hero copy once documents are seeded.
    - `npm run test:content` still passes its 9 checks (no regression); `tsc --noEmit` clean.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ only if touching a Next API (this task is Sanity/schema — no Next API).
    In sanity/schemaTypes/siteSettings.ts add two document-i18n fields after `footerText` per 04-RESEARCH.md "Flag D-07" (verbatim shape): `heroHeadline` (string, required, max 80) and `heroSubline` (text, rows 2, max 200). Keep them plain string/text — no Portable Text (Phase 3 D-04 convention).
    In lib/sanity/queries.ts append `, heroHeadline, heroSubline` to the SITE_SETTINGS_QUERY projection ONLY. Do not add a second client, do not add any token, do not add stega config here (D-03; stega:false lives on the client).
    Run `npm run types:sanity` (schema extract --force + typegen) to regenerate sanity.types.ts; commit sanity.types.ts (schema.json stays gitignored per prior-phase decision).
    Deploy the schema so Studio exposes the fields, then author heroHeadline + heroSubline on BOTH the DE and EN siteSettings documents (D-07 seed path; DE/EN suggestions in 04-RESEARCH.md are Claude's discretion — content is Sanity-controlled). The hero renders next-intl fallback copy if a field is null, so seeding is a content step, not a code blocker.
  </action>
  <verify>
    <automated>npm run types:sanity && npx tsc --noEmit && npm run test:content</automated>
  </verify>
  <done>siteSettings has heroHeadline + heroSubline; SITE_SETTINGS_QUERY projects them; sanity.types.ts regenerated; test:content green; DE+EN hero copy authored in Studio.</done>
  <reversibility rating="reversible">Schema fields and query projections are additive and can be removed; content is public marketing copy.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Real Header + Footer + MotionSection wrapper; mount in layout; scroll-margin base rule</name>
  <files>components/layout/Header.tsx, components/layout/Footer.tsx, components/ui/MotionSection.tsx, app/[locale]/layout.tsx, app/globals.css, messages/de.json, messages/en.json</files>
  <behavior>
    - Header renders at both viewports: 1440px shows logo + anchor nav (#services,#pricing,#work,#contact) + LocaleSwitcher; 375px shows logo + hamburger that toggles a full-width overlay of the same links.
    - On scroll > 0, Header gains the frosted backdrop classes (bg-surface-subtle/90 backdrop-blur-sm shadow-sm); at top it is transparent.
    - Active section anchor is highlighted via border-b-2 border-accent, driven by IntersectionObserver (not scroll listeners).
    - Footer renders on bg-surface-dark with text-on-dark; contains logo (dark variant), anchor nav, legal links (/impressum,/datenschutz via i18n Link), contact email + copyright from siteSettings with getFullYear() fallback.
    - MotionSection fades opacity 0->1 and rises y:10->0 once on view (viewport once, amount 0.15, duration 0.5, ease [0,0,0.2,1]); under useReducedMotion() the transition duration is 0.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for the Next 16 image/dynamic/'use client' APIs before writing code (AGENTS.md).
    Build components/ui/MotionSection.tsx exactly per 04-RESEARCH.md Pattern 3 ('use client', motion + useReducedMotion imported from 'motion/react' NOT framer-motion; renders motion.section receiving children so RSC sections stay async — Pitfall 4). Props: children, id, className.
    Build components/layout/Header.tsx ('use client' — IntersectionObserver + mobile nav state + scroll backdrop) per UI-SPEC "Header" and "Navigation — Sticky Header": sticky top-0 h-16, logo as next/image with explicit width/height (no CLS), anchor links as plain <a href="#..."> (same-page anchors), legal/route links via i18n Link, LocaleSwitcher folded in. IntersectionObserver rootMargin '-30% 0px -60% 0px'. Fold in components/LocaleSwitcher.tsx.
    Build components/layout/Footer.tsx (RSC; siteSettings passed as props from layout or page) per UI-SPEC "Footer" + D-13: bg-surface-dark, text-on-dark / text-muted-on-dark, dark-variant logo, 3-col at 1440 / stacked at 375, email + copyright from siteSettings (getFullYear() fallback).
    Edit app/[locale]/layout.tsx: fetch getSiteSettings(locale) (locale from awaited params — URL only, never client state), replace the switcher-only <header> with <Header locale={locale} settings={settings} /> above {children} and <Footer settings={settings} /> below. Keep NextIntlClientProvider, the Plus Jakarta Sans bridge, and the body token classes intact.
    Add to app/globals.css @layer base: a rule giving anchor-target elements scroll-margin-top of 80px so anchored section headings clear the 64px sticky header (RESEARCH Pitfall 3). Add/confirm the :focus-visible ring treatment (ring-2 ring-accent ring-offset-2) per UI-SPEC "Focus-Visible".
    Add the nav/footer/hero i18n message keys to messages/de.json and messages/en.json per the UI-SPEC Copywriting Contract (nav labels, CTA "Projekt anfragen"/"Start a project", footer tagline, hero fallback copy). Use ONLY token utilities — zero raw hex, no text-gray-*/bg-gray-* (IDENT-01). Locale from params only (no useState for locale — Pitfall 7).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:invariants && npx playwright test tests/layout/header.spec.ts</automated>
  </verify>
  <done>Header + Footer render in the layout at both viewports/locales; sticky backdrop + hamburger overlay + active-anchor highlight work; footer is on the dark surface; MotionSection zeroes motion under reduced-motion; invariants + header spec green.</done>
  <reversibility rating="costly">D-04 single-page anchor nav is baked into the Header; splitting sections into routes later reworks nav + metadata + section mounting.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 5: HeroSection (D-06 swap container) wired to Sanity copy; compose into page; per-section QA loop</name>
  <files>components/sections/HeroSection.tsx, app/[locale]/page.tsx, tests/sections/hero.spec.ts</files>
  <behavior>
    - HeroSection renders headline (text-4xl -> md:text-5xl, weight 700, text-primary) + subline (text-base, text-secondary, max-w 560px) + a primary CTA (bg-accent text-surface, href="#contact") over a static token-based radial-gradient backdrop.
    - Headline/subline come from props (Sanity siteSettings heroHeadline/heroSubline); when a prop is null, next-intl fallback copy renders — never an empty heading.
    - The section is `<section id="hero" className="relative min-h-svh flex items-center">` so Phase 5 inserts an absolute-inset Canvas with zero CLS (D-06).
    - Wrapped in MotionSection behavior (whisper-quiet fade); nothing animates under reduced-motion.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for Next 16 image/metadata APIs before writing code.
    Build components/sections/HeroSection.tsx per UI-SPEC "Hero Shell": min-h-svh relative flex container, static CSS radial-gradient backdrop using --color-surface / --color-surface-subtle tokens (NO image, NO JS, NO 3D — the gradient is the Phase 5 swap target), text content in a relative child (50% column at 1440, centered at 375). CTA button per UI-SPEC (bg-accent text-surface, px-6 py-3, rounded-sm, hover bg-accent-hover, focus-visible ring-2 ring-accent ring-offset-2, href="#contact"). Use MotionSection (or its motion.section pattern) for the entrance fade. Props: headline?: string|null, subline?: string|null; fall back to next-intl messages when null.
    Edit app/[locale]/page.tsx: make it an async RSC that awaits params.locale (URL only), fetches getSiteSettings(locale), and renders <HeroSection headline={settings?.heroHeadline} subline={settings?.heroSubline} />. Remove the placeholder centered <h1>. Keep generateMetadata + buildHreflangAlternates('/') intact. Do not fetch other sections yet — 04-02 adds them.
    Zero raw hex / no gray-* utilities (IDENT-01). stega:false stays on the client; never pass Sanity strings through a second client.
    QA loop (D-02, per-section): run tests/sections/hero.spec.ts to capture desktop+mobile screenshots for de+en; review each screenshot; iterate the section until it reads calm/editorial and the long-headline case wraps without pushing the CTA below the fold at 375px (UI Considerations row 8 backstop). Then run the ui-skills critique (QA-02, manual — see verify human-check). Confirm axe zero violations.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:invariants && npx playwright test tests/sections/hero.spec.ts tests/motion/reduced.spec.ts tests/a11y/axe.spec.ts</automated>
    <human-check>QA-02: run `npx ui-skills start` -> load the smallest relevant hero/landing skill -> critique the Hero desktop+mobile screenshots (de+en) against calm-editorial identity; iterate until it passes. Confirm the hero reads finished on its own (D-06) and the CTA stays above the fold at 375px with a long headline.</human-check>
  </verify>
  <done>Hero renders finished in DE+EN at 375px+1440px from Sanity copy inside the min-h-svh swap container; entrance fade respects reduced-motion; hero + motion + axe specs green; ui-skills critique passed. The full vertical (Sanity -> RSC -> token section -> motion -> QA) is proven end to end.</done>
  <reversibility rating="reversible">D-06: the min-h-svh container contract is the durable part; the gradient backdrop is a Phase 5 swap-in.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity dataset -> build/request-time read | Public dataset read (D-03); tokenless, published perspective, stega:false |
| npm registry -> node_modules | `resend` install (Wave 0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Sanity read client | high | mitigate | No read/write token in client bundle; single tokenless stega:false client (D-03). Invariant `tests/invariants/sanity-single-client.sh` stays green. |
| T-04-02 | Tampering | stega tokens in hero headline / page title | medium | mitigate | Only lib/sanity/client.ts (stega:false global); never a second client. `tests/invariants/no-stega-in-build.sh` gate. |
| T-04-03 | Tampering | Locale injection via params.locale | low | mitigate | next-intl routing constrains locale to ['de','en']; locale never from client state (`no-locale-from-state.sh`). |
| T-04-SC | Tampering | `npm install resend` | high | mitigate | resend audited in 04-RESEARCH.md "Package Legitimacy Audit" (official Resend SDK, multi-million weekly downloads) — verdict OK; not [ASSUMED]/[SUS]/[SLOP], so no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run test:invariants` (no-raw-hex, no-locale-from-state, sanity-single-client, no-stega-in-build) green.
- `npm run test:content` returns live Sanity content anonymously (D-03 confirmed).
- `npx playwright test tests/sections/hero.spec.ts tests/layout/header.spec.ts tests/motion/reduced.spec.ts tests/a11y/axe.spec.ts` green in de+en at 375px+1440px.
- ui-skills critique (QA-02) passed for Hero + header/footer shell.
</verification>

<success_criteria>
- `/de` and `/en` render real Header + finished Hero + dark Footer end to end from live Sanity copy.
- Hero is inside the min-h-svh swap container (D-06); zero CLS contract established for Phase 5.
- Entrance motion is whisper-quiet and fully disabled under prefers-reduced-motion (SEC-11/D-14).
- axe zero violations; :focus-visible present (QA-03).
- The full vertical is proven; 04-02 replicates it across the remaining sections.
</success_criteria>

<output>
Create `.planning/phases/04-2d-marketing-sections/04-01-SUMMARY.md` when done.
</output>
