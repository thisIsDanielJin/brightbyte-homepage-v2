---
phase: 04-2d-marketing-sections
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - sanity/schemaTypes/siteSettings.ts
  - lib/sanity/queries.ts
  - sanity.types.ts
  - app/[locale]/impressum/page.tsx
  - app/[locale]/datenschutz/page.tsx
  - messages/de.json
  - messages/en.json
  - tests/legal/pages.spec.ts
  - tests/motion/reduced.spec.ts
  - tests/a11y/axe.spec.ts
autonomous: false
requirements: [SEC-09, SEC-10, SEC-11, QA-01, QA-02, QA-03]
user_setup:
  - service: sanity-studio
    why: "D-12: Datenschutz body is a new siteSettings field; the DSGVO privacy text (and optional impressumBody) must be authored in DE and EN in Studio. Legal copy is Daniel's content deliverable, not code."
    dashboard_config:
      - task: "Author datenschutzBody (DSGVO privacy policy) + optional impressumBody on both DE and EN siteSettings documents"
        location: "Sanity Studio (/studio) -> Site Settings (DE) and (EN)"

estimate:
  tokens: 85000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/de/impressum and /en/impressum render address + Steuernummer + §19 UStG note from Sanity siteSettings (SEC-09)."
    - "/de/datenschutz and /en/datenschutz render the DSGVO privacy body from Sanity siteSettings (SEC-09)."
    - "Both legal routes emit correct bidirectional hreflang via buildHreflangAlternates and render inside the full site header/footer shell."
    - "The footer dark-surface moment is verified across the whole site; footer legal links reach the legal routes (D-13)."
    - "Final sweep: all 9 sections + 2 legal pages render in DE+EN at 375px+1440px with no overflow/CLS; axe zero violations everywhere; :focus-visible on every interactive element (SEC-10/QA-03)."
    - "No entrance animation plays anywhere under prefers-reduced-motion (SEC-11)."
  artifacts:
    - "app/[locale]/impressum/page.tsx"
    - "app/[locale]/datenschutz/page.tsx"
    - "tests/legal/pages.spec.ts"
  key_links:
    - "SITE_SETTINGS_QUERY projects impressumBody + datenschutzBody (added here) alongside the address/steuernummer/vatNote seeded in Phase 3."
    - "Legal page generateMetadata calls buildHreflangAlternates('/impressum' | '/datenschutz')."
    - "Footer legal links (from 04-01) resolve to these routes via i18n Link."
---

<objective>
**As a** German site visitor (and legal-compliance requirement),
**I want to** reach a complete Impressum and Datenschutzerklärung in my locale from the footer,
**so that** the site meets German legal obligations and reads as trustworthy and finished.

Purpose: Close the last requirement (SEC-09 legal routes), verify the footer dark-surface moment site-wide, and run the final phase-level a11y/mobile/motion sweep across all 9 sections + 2 legal pages (SEC-10, SEC-11, QA-03) — the single phase verification gate (D-01).
Output: Impressum + Datenschutz routes from Sanity, the datenschutzBody schema addition + re-seed, and a green full-suite phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-2d-marketing-sections/04-CONTEXT.md
@.planning/phases/04-2d-marketing-sections/04-RESEARCH.md
@.planning/phases/04-2d-marketing-sections/04-UI-SPEC.md
@.planning/phases/04-2d-marketing-sections/04-VALIDATION.md
@.planning/phases/04-2d-marketing-sections/04-01-SUMMARY.md
@.planning/phases/04-2d-marketing-sections/04-02-SUMMARY.md
@.planning/phases/04-2d-marketing-sections/04-03-SUMMARY.md
@AGENTS.md
@lib/sanity/queries.ts
@lib/i18n/metadata.ts
@i18n/navigation.ts
@sanity/schemaTypes/siteSettings.ts
@styles/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: impressumBody/datenschutzBody schema + projection + typegen + re-seed; Impressum + Datenschutz routes</name>
  <files>sanity/schemaTypes/siteSettings.ts, lib/sanity/queries.ts, sanity.types.ts, app/[locale]/impressum/page.tsx, app/[locale]/datenschutz/page.tsx, messages/de.json, messages/en.json, tests/legal/pages.spec.ts</files>
  <behavior>
    - /de/impressum + /en/impressum render address + Steuernummer + §19 UStG note (from Sanity) inside the site shell; page title "Impressum"/"Legal Notice".
    - /de/datenschutz + /en/datenschutz render the datenschutzBody prose (whitespace-pre-wrap); page title "Datenschutzerklärung"/"Privacy Policy".
    - Both routes emit correct bidirectional hreflang; SITE_SETTINGS_QUERY result type includes impressumBody + datenschutzBody.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ for the Next 16 page/generateMetadata/async-params API before writing code (AGENTS.md).
    Add `impressumBody` (text, optional) and `datenschutzBody` (text, required) fields to sanity/schemaTypes/siteSettings.ts per 04-RESEARCH.md "Flag D-12" (plain text, no Portable Text — Phase 3 convention). Append `impressumBody, datenschutzBody` to SITE_SETTINGS_QUERY projection in lib/sanity/queries.ts (single tokenless client; no token; stega:false stays on the client). Run `npm run types:sanity` to regenerate sanity.types.ts.
    Deploy schema, then author DE + EN datenschutzBody (standard DSGVO Datenschutzerklärung for a solo German freelancer site — Verantwortlicher, Kontaktdaten, Datenerhebung, Kontaktformular, Betroffenenrechte, Hosting/Vercel) and optional impressumBody in Studio (D-12 seed path — Daniel's legal content, not a code deliverable). datenschutzBody is required, so the page needs it seeded to render meaningfully.
    Build app/[locale]/impressum/page.tsx and app/[locale]/datenschutz/page.tsx (RSC) per 04-RESEARCH.md Pattern 6 + UI-SPEC "Impressum + Datenschutz Pages": max-w-720px editorial column, py-16 px-4 mobile / py-24 px-8 desktop, page title text-4xl bold, section headings text-2xl semibold, body text-base secondary leading-relaxed with whitespace-pre-wrap, address text-sm secondary font-mono. Fetch getSiteSettings(locale) (locale from awaited params — URL only). generateMetadata returns the localized title + `alternates: buildHreflangAlternates('/impressum'|'/datenschutz')`. Both routes render inside the existing [locale] layout shell (Header + Footer). Add impressum/datenschutz title i18n keys per Copywriting Contract. Zero raw hex / no gray-* (IDENT-01).
    Scaffold tests/legal/pages.spec.ts (de+en x 375/1440: impressum shows address + Steuernummer; datenschutz shows body; both wrapped by header/footer). Per-page QA loop (D-02) + ui-skills critique + axe.
  </action>
  <verify>
    <automated>npm run types:sanity && npx tsc --noEmit && npm run test:invariants && npx playwright test tests/legal/pages.spec.ts tests/a11y/axe.spec.ts</automated>
    <human-check>QA-02: `npx ui-skills start` -> load the smallest relevant long-form/editorial skill -> critique Impressum + Datenschutz desktop+mobile screenshots (de+en); confirm the legal prose reads clean at 720px measure and address monospace wraps correctly.</human-check>
  </verify>
  <done>Impressum + Datenschutz render from Sanity in DE+EN at both viewports inside the site shell with correct hreflang; impressumBody/datenschutzBody schema + projection + types added and seeded; legal + axe specs green; ui-skills critique passed. SEC-09 complete.</done>
  <reversibility rating="reversible">Legal fields are additive; text plain-text now, migratable to Portable Text in a later phase if rich formatting is needed.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final phase gate — full-suite a11y/mobile/motion sweep + footer dark-surface verification across all sections</name>
  <what-built>
    The full run of the phase verification gate (D-01: one phase, one gate). This task builds no new UI — it runs the complete suite across all 9 sections + 2 legal pages and confirms the cross-cutting criteria that no single earlier plan owns end to end.
  </what-built>
  <how-to-verify>
    1. Run the full automated gate:
       - `npx tsc --noEmit`
       - `npm run test:invariants` (IDENT-01 no-raw-hex, no-locale-from-state, sanity-single-client, no-stega-in-build)
       - `npm run test:content` (9 checks, anonymous — D-03 public read)
       - `npx playwright test` (all section, contact, legal, header, motion, a11y specs) in de+en at 375px + 1440px
       - Confirm `tests/a11y/axe.spec.ts` reports zero violations on `/de`, `/en`, `/de/impressum`, `/de/datenschutz` (and EN equivalents).
       - Confirm `tests/motion/reduced.spec.ts` proves NO entrance animation runs under prefers-reduced-motion across sections (SEC-11).
    2. Manually verify at 375px and 1440px in both locales:
       - Section order is Hero -> Services -> Pricing -> Work -> Testimonials -> About -> Contact, wrapped by header/footer (D-05).
       - No horizontal overflow, no layout shift; anchor nav lands each section below the sticky header (scroll-margin).
       - The footer renders on the dark surface (--color-surface-dark / --color-on-dark) on every page — the intended dark-accent moment (D-13) — and its legal links reach /impressum and /datenschutz.
       - Every interactive element (nav links, CTAs, form fields, submit, footer links) shows a :focus-visible ring (QA-03).
    3. Confirm the per-section ui-skills critiques (QA-02) from 04-01..04-03 and Task 1 are all recorded as passed.
  </how-to-verify>
  <resume-signal>Type "approved" when the full suite is green in both locales at both breakpoints, axe is zero-violation site-wide, reduced-motion is honored everywhere, and the footer dark moment + legal links are confirmed. Otherwise list the failing checks.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity dataset -> RSC read | Public tokenless read of legal siteSettings fields (D-03) |
| Locale segment -> legal route | params.locale drives which locale's legal text renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Information Disclosure | Sanity read client (legal fields) | high | mitigate | Reuse the single tokenless stega:false client; no token; `sanity-single-client.sh` + `no-stega-in-build.sh` gates stay green. |
| T-04-13 | Tampering | stega tokens in legal page titles | medium | mitigate | Titles come from i18n messages, not raw Sanity strings passed to metadata; body rendered as escaped text. `no-stega-in-build.sh` gate. |
| T-04-14 | Tampering | Locale injection via params.locale on legal routes | low | mitigate | next-intl routing constrains locale to ['de','en']; `no-locale-from-state.sh` gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run test:invariants` + `npm run test:content` green.
- `npx playwright test` (full suite) green in de+en at 375px+1440px.
- axe zero violations on home + both legal routes in both locales.
- reduced-motion honored across all sections.
- Footer dark-surface moment + legal-link routing verified manually.
- All per-section ui-skills critiques (QA-02) recorded as passed.
</verification>

<success_criteria>
- Impressum + Datenschutz render from Sanity in DE+EN inside the site shell with correct hreflang (SEC-09).
- All 9 sections + 2 legal pages pass the full a11y/mobile/motion sweep (SEC-10, SEC-11, QA-03).
- Footer dark-surface moment confirmed site-wide; legal links reach the legal routes (D-13).
- The single phase verification gate (D-01) is green — Phase 4 is ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/04-2d-marketing-sections/04-04-SUMMARY.md` when done.
</output>
