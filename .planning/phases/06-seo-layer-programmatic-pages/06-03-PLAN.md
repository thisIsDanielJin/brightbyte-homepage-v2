---
phase: 06-seo-layer-programmatic-pages
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false
requirements: [SEO-01, SEO-02, SEO-03]
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: low
user_setup:
  - service: vercel
    why: "SC #2/#3/#4 verify on a real public preview URL (Google Rich Results Test + sitemap validator + curl hreflang require a public deploy, not localhost)"
    env_vars:
      - name: SANITY_API_READ_TOKEN
        source: "Sanity Manage → API → Tokens (Viewer). Carried-over pre-deploy TODO — must be present in the Vercel env (all environments) so build-time reads succeed."

must_haves:
  truths:
    - "A Vercel preview deploy of the branch exists with a public URL"
    - "Google Rich Results Test reports 0 errors for the SEO tracer page (WebPage + FAQPage + BreadcrumbList), the homepage (ProfessionalService/LocalBusiness), and a section page (WebPage)"
    - "curl of a DE SEO page on the preview lists hreflang de, en, x-default with x-default → the /de URL; the EN counterpart page lists the reciprocal set (bidirectional)"
    - "The deployed /sitemap.xml validates in a sitemap validator: all 50 SEO URLs + alternates parse cleanly"
    - "robots.txt on the preview allows all public routes and references the sitemap"
  artifacts: []
  key_links:
    - "Vercel preview URL → Google Rich Results Test (SC #2)"
    - "Vercel preview URL → curl hreflang bidirectionality (SC #4)"
    - "Vercel preview /sitemap.xml → sitemap validator (SC #3)"
---

<objective>
Phase gate: verify the four ROADMAP success criteria on a real deployed environment (not the dev server). SC #2 (JSON-LD via Google Rich Results Test), SC #3 (sitemap validation), and SC #4 (bidirectional hreflang via curl) are all Manual-Only per 06-VALIDATION.md — they require a public preview URL. This plan deploys to a Vercel preview and confirms each SC, closing the phase.

Purpose: prove SEO correctness on the real deployed HTML the way Google will see it.
Output: a verified Vercel preview + human sign-off on all four success criteria.

**Requirements:** SEO-01, SEO-02, SEO-03 (final deployed verification).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-seo-layer-programmatic-pages/06-VALIDATION.md
@.planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel preview + automated curl hreflang cross-check</name>
  <precondition>SANITY_API_READ_TOKEN is present in the Vercel project env (all environments) so build-time Sanity reads succeed on the preview — carried-over pre-deploy TODO (STATE.md).</precondition>
  <read_first>
    - .planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md § Verification Architecture (SC #4 curl commands, SC #3 validators, § Environment Availability — Vercel preview / ngrok fallback)
    - .planning/phases/06-seo-layer-programmatic-pages/06-VALIDATION.md § Manual-Only Verifications
    - .planning/STATE.md — pre-deploy TODOs (SANITY_API_READ_TOKEN in Vercel env; verify Node ≥22.12 runtime)
  </read_first>
  <action>
    Trigger a Vercel preview deploy of the phase branch (git push to the branch; Vercel auto-builds a preview, or `npx vercel` if the CLI is authenticated). Capture the public preview URL. Run the SC #4 curl cross-check against the preview: for the DE tracer page grep `hreflang="[^"]*"` and the alternate hrefs, confirm de/en/x-default present and x-default → /de/s/webentwickler-berlin; repeat for the EN counterpart and confirm reciprocity (DE page references EN page and vice versa). Fetch /sitemap.xml and /robots.txt from the preview and confirm 200 + expected content. If Vercel is not available, fall back to `next start` tunneled via `npx localtunnel` (RESEARCH § Environment Availability) and note the fallback in the SUMMARY.
  </action>
  <verify>
    <automated>echo "Manual/CLI deploy step — verify command is the curl block in the acceptance criteria run against the captured PREVIEW_URL"</automated>
  </verify>
  <acceptance_criteria>
    - A preview URL is captured and reachable (curl returns 200 for /de/s/webentwickler-berlin and its EN counterpart).
    - `curl -s <PREVIEW>/de/s/webentwickler-berlin | grep -o 'hreflang="[^"]*"' | sort` → de, en, x-default; x-default href → /de/s/webentwickler-berlin.
    - The EN page lists the reciprocal set (bidirectional; DE↔EN reference each other).
    - `curl -s <PREVIEW>/sitemap.xml` returns 200 and contains the SEO URLs; `curl -s <PREVIEW>/robots.txt` returns 200 with the sitemap reference.
  </acceptance_criteria>
  <done>Preview deployed; curl confirms bidirectional hreflang + x-default → /de and sitemap/robots served correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify SC #2/#3/#4 on the Vercel preview</name>
  <what-built>The full bilingual SEO layer deployed to a Vercel preview: 25 keyword pages × 2 locales, JSON-LD on every page type, complete sitemap + robots, paired hreflang. Automated curl checks (Task 1) already confirmed hreflang + sitemap/robots serving.</what-built>
  <how-to-verify>
    SC #2 — Google Rich Results Test (https://search.google.com/test/rich-results):
      1. Paste `<PREVIEW>/de/s/webentwickler-berlin` → confirm 0 errors; WebPage + FAQPage (all Q&A) + BreadcrumbList detected.
      2. Paste `<PREVIEW>/en/<en-slug>` → same result for the EN page.
      3. Paste `<PREVIEW>/de` (homepage) → confirm ProfessionalService/LocalBusiness detected, 0 errors.
      4. Paste `<PREVIEW>/de/impressum` (a section page) → confirm WebPage detected.
    SC #3 — Sitemap validation:
      5. Submit `<PREVIEW>/sitemap.xml` to a sitemap validator (e.g. xml-sitemaps.com/validate, or `xmllint --noout` locally against the fetched XML) → confirm all 50 SEO URLs + alternates parse with no errors.
    SC #4 — Spot-check hreflang visually in the Rich Results / URL Inspection rendered HTML for one DE and one EN page (Task 1's curl already automated the cross-check).
  </how-to-verify>
  <resume-signal>Type "approved" once all four success criteria pass on the preview, or describe any errors the validators reported.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public preview URL → external validators | deployed HTML inspected by Google + sitemap validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Information Disclosure | deployed build-time reads | high | mitigate | stega:false invariant already enforced in code + CI; preview build uses the same client. No draft leakage (perspective: published). |
| T-06-09 | Information Disclosure | SANITY_API_READ_TOKEN in Vercel env | medium | accept | Server-only viewer token in Vercel env (all environments); never exposed to client bundle. Pre-deploy TODO tracked in STATE.md. |
</threat_model>

<verification>
- Vercel preview reachable; curl hreflang + sitemap + robots confirmed (Task 1).
- Human sign-off that Google Rich Results Test = 0 errors on all page types and the sitemap validates (SC #2/#3).
</verification>

<success_criteria>
All four ROADMAP success criteria verified on a real Vercel preview: (1) all `/s/[slug]` render both locales no 404, (2) valid JSON-LD per page via Rich Results Test, (3) sitemap + robots validate, (4) bidirectional hreflang with x-default → /de confirmed via curl. Phase 6 complete — the site is launchable.
</success_criteria>

<output>
Create `.planning/phases/06-seo-layer-programmatic-pages/06-03-SUMMARY.md` when done.
</output>
