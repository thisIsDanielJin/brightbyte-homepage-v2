---
phase: 06-seo-layer-programmatic-pages
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - content/seo-pages.ndjson
  - lib/jsonld/seoPage.ts
  - lib/jsonld/organization.ts
  - app/[locale]/page.tsx
  - app/[locale]/impressum/page.tsx
  - app/[locale]/datenschutz/page.tsx
  - tests/seo/seo-pages.spec.ts
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]
estimate:
  tokens: 95000
  raw_tokens: 47000
  tasks: 3
  confidence: low
user_setup: []

must_haves:
  truths:
    - "All 50 seoPage docs (25 DE + 25 EN) + 25 translation.metadata links are imported into Sanity"
    - "generateStaticParams returns exactly 50 {locale, slug} entries"
    - "Every one of the 25 DE slugs and 25 EN slugs renders 200 (not 404) in its locale against next start"
    - "The homepage (/de and /en) emits ProfessionalService/LocalBusiness JSON-LD sourced from siteSettings (email, address, areaServed Berlin) with no hardcoded copy"
    - "Section/legal pages emit WebPage JSON-LD localized per locale"
    - "sitemap.xml contains all 50 SEO-page URLs, each with alternates.languages using the correct differing DE/EN slug"
    - "Every DE SEO slug is preserved verbatim from v1 (no renamed or 'improved' slugs)"
    - "JSON-LD across all page types uses JSON.stringify (no unescaped CMS string interpolation)"
  artifacts:
    - content/seo-pages.ndjson
    - lib/jsonld/organization.ts
    - app/[locale]/page.tsx
  key_links:
    - "50 imported docs → generateStaticParams → 50 prerendered SEO pages, zero 404s"
    - "siteSettings singleton → buildLocalBusinessLd → homepage JSON-LD"
    - "SEO_SLUG_PAIRS_QUERY over all 25 DE docs → full sitemap with paired alternates"
---

<objective>
Expansion slice: with the tracer proven in Plan 01, unlock the remaining 24 SEO slugs (all 50 docs + 25 links imported), add site-wide + section JSON-LD (D-05 — ProfessionalService/LocalBusiness on home, WebPage on sections/legal), and confirm the full sitemap covers all 50 URLs with correct paired hreflang. Every `/s/[slug]` renders in both locales with no 404 (SEO-01), every page type has valid JSON-LD (SEO-02), and the sitemap is complete (SEO-03).

Purpose: complete the SEO surface across all 25 keyword pages and every public route's structured data.
Output: full Sanity import, homepage/section JSON-LD, complete sitemap, all-slug render coverage.

**Requirements:** SEO-01 (all 50 params render), SEO-02 (JSON-LD on home + sections), SEO-03 (full sitemap).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-layer-programmatic-pages/06-CONTEXT.md
@.planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md
@.planning/phases/06-seo-layer-programmatic-pages/06-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import all 50 docs + 25 links; verify all 25 slugs render both locales</name>
  <precondition>Plan 01 completed: schema enriched, migration script + content/seo-pages.ndjson generated, EN slugs approved. The full NDJSON already exists from 06-01 Task 3.</precondition>
  <read_first>
    - content/seo-pages.ndjson — the full generated NDJSON (50 docs + 25 links) from Plan 01
    - scripts/migrate-seo-pages.ts — re-run to regenerate if needed
    - app/[locale]/s/[slug]/page.tsx — generateStaticParams (should already loop de+en × getSeoPages)
    - .planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md § Pattern 7 (import command), Pitfall 7 (verbatim DE slugs), § Verification Architecture
    - tests/seo/seo-pages.spec.ts — the "static params" spec (update the expected count to 50)
  </read_first>
  <action>
    Run `npx sanity dataset import content/seo-pages.ndjson production --replace` to import all 50 docs + 25 translation.metadata links. Update the tests/seo/seo-pages.spec.ts "static params" assertion to expect exactly 50 entries (25 slugs × 2 locales). Verify via `npx sanity documents query 'count(*[_type=="seoPage"])'` == 50 and `count(*[_type=="translation.metadata" && "seoPage" in schemaTypes])` == 25. Build and confirm all 25 DE slugs + 25 EN slugs prerender (spot-check the sample set: webentwickler-berlin, website-fuer-aerzte, webdesign-neukoelln plus their EN counterparts). No DE slug is renamed.
  </action>
  <verify>
    <automated>npm run build && (npm run start &) && sleep 6 && npx playwright test tests/seo/seo-pages.spec.ts --project=chromium; kill %1 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx sanity documents query 'count(*[_type=="seoPage"])'` returns 50.
    - Build log shows 50 prerendered `/[locale]/s/[slug]` routes; zero build errors.
    - The "static params" spec asserts 50 entries and passes.
    - All 25 DE slugs match the v1-locked list verbatim (Pitfall 7).
  </acceptance_criteria>
  <done>All 50 docs imported; all 25 slugs render in both locales; static-params count is 50.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Homepage ProfessionalService/LocalBusiness JSON-LD + section/legal WebPage JSON-LD</name>
  <read_first>
    - app/[locale]/page.tsx — homepage RSC + existing generateMetadata
    - app/[locale]/impressum/page.tsx, app/[locale]/datenschutz/page.tsx — section/legal pages
    - lib/sanity/queries.ts — SITE_SETTINGS_QUERY (contactEmail, address) + getSiteSettings(locale)
    - lib/jsonld/seoPage.ts — existing emitters (buildWebPageLd) from Plan 01
    - .planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md § Pattern 4 (JSON-LD in RSC), D-05 (home = ProfessionalService/LocalBusiness with Berlin areaServed, hello@brightbyte-berlin.com, Karl-Marx-Allee 118, 10243 Berlin)
    - .planning/phases/06-seo-layer-programmatic-pages/06-CONTEXT.md § D-05
  </read_first>
  <behavior>
    - /de and /en homepage each emit one ProfessionalService (or LocalBusiness) JSON-LD script with name BrightByte Berlin, areaServed Berlin, email + postal address from siteSettings, inLanguage localized.
    - impressum + datenschutz each emit a WebPage JSON-LD localized per locale.
    - All values sourced from Sanity/constants — no hardcoded marketing copy in the LD.
  </behavior>
  <action>
    Create lib/jsonld/organization.ts with buildLocalBusinessLd(locale, siteSettings) returning `@type: 'ProfessionalService'` (schema.org) with name, url, areaServed {@type City, name Berlin}, email + address (streetAddress Karl-Marx-Allee 118, postalCode 10243, addressLocality Berlin, addressCountry DE), inLanguage de-DE|en-US. In app/[locale]/page.tsx render the script via `<script type="application/ld+json" dangerouslySetInnerHTML={{__html: JSON.stringify(ld)}} />`, sourcing contact/address from getSiteSettings(locale) (fall back to the D-05 constants where a siteSettings field is absent). In impressum + datenschutz pages, emit a WebPage LD via the existing buildWebPageLd. Extend tests/seo/seo-pages.spec.ts with a grep-tagged check "home JSON-LD" asserting the homepage has a ProfessionalService/LocalBusiness ld+json script.
  </action>
  <verify>
    <automated>npm run build && (npm run start &) && sleep 6 && npx playwright test tests/seo/seo-pages.spec.ts --project=chromium; kill %1 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Against next start: the homepage DE + EN each contain exactly one `application/ld+json` script whose `@type` is ProfessionalService or LocalBusiness with `areaServed` Berlin and the siteSettings email.
    - impressum + datenschutz each contain a WebPage ld+json script.
    - `grep -rn "dangerouslySetInnerHTML" app/[locale]/page.tsx lib/jsonld/` shows JSON-LD only (JSON.stringify output).
  </acceptance_criteria>
  <done>Home emits ProfessionalService/LocalBusiness LD; sections/legal emit WebPage LD; all localized and Sanity-sourced.</done>
</task>

<task type="auto">
  <name>Task 3: Full sitemap coverage (all 50 URLs) + full-suite green</name>
  <read_first>
    - app/sitemap.ts — the SEO-page block added in Plan 01 (SEO_SLUG_PAIRS_QUERY)
    - lib/sanity/queries.ts — SEO_SLUG_PAIRS_QUERY
    - tests/seo/sitemap.spec.ts — extend to assert all 50 SEO URLs present
    - .planning/phases/06-seo-layer-programmatic-pages/06-RESEARCH.md § Pattern 5 (sitemap), Pitfall 4 (differing slugs), Pitfall 8 (no next/headers)
    - .planning/phases/06-seo-layer-programmatic-pages/06-VALIDATION.md § Sampling Rate + Wave 0 (next start discipline)
  </read_first>
  <action>
    Confirm app/sitemap.ts SEO block now emits all 25 pairs (50 URLs) from the full dataset — no code change expected beyond Plan 01 (the query reads all DE docs), but verify count. Extend tests/seo/sitemap.spec.ts to assert the sitemap contains 50 SEO-page `<url>` entries (plus the 2 locale roots) and that a location-category slug (e.g. webdesign-neukoelln) has an xhtml:link en alternate with the differing EN slug. Run the full Playwright suite against next start and the invariant suite; all green before phase gate (Plan 03).
  </action>
  <verify>
    <automated>npm run build && (npm run start &) && sleep 6 && npx playwright test tests/seo/ --project=chromium && npm run test:invariants; kill %1 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - The sitemap spec asserts ≥50 SEO-page URL entries and passes.
    - `webdesign-neukoelln` DE entry has an en xhtml:link alternate using its differing EN slug.
    - `npm run test:invariants` passes (stega, single-client, no-raw-hex, etc.).
    - Full `npx playwright test tests/seo/ --project=chromium` is green.
  </acceptance_criteria>
  <done>Sitemap covers all 50 SEO URLs with correct paired alternates; full seo/ suite + invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity → build-time JSON-LD (home + 50 pages) | CMS/settings strings enter HTML at build |
| full slug set → generateStaticParams | only 50 seeded slugs resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering (XSS) | homepage/section JSON-LD scripts | high | mitigate | JSON.stringify-only serialization in dangerouslySetInnerHTML; siteSettings/CMS strings never interpolated raw into HTML. |
| T-06-06 | Information Disclosure | all new build-time reads | high | mitigate | Reuse the single stega:false client; verified by test:invariants (sanity-single-client, no-stega-in-build). |
| T-06-07 | Tampering | bulk NDJSON import | medium | mitigate | `--replace` idempotent; import verified by document count == 50 / 25 before proceeding. |
</threat_model>

<verification>
- `npx sanity documents query 'count(*[_type=="seoPage"])'` == 50.
- `npm run build` prerenders 50 SEO routes, zero errors.
- Full `npx playwright test tests/seo/ --project=chromium` green against next start.
- `npm run test:invariants` green.
</verification>

<success_criteria>
All 25 keyword pages render in both locales (50 routes, zero 404s), the homepage and section/legal pages emit valid localized JSON-LD, and the sitemap covers all 50 SEO URLs with correct differing-slug alternates. Full seo/ suite + invariants green — ready for the deployed-preview phase gate.
</success_criteria>

<output>
Create `.planning/phases/06-seo-layer-programmatic-pages/06-02-SUMMARY.md` when done.
</output>
