# Phase 6 — External API Coverage Declaration

No external API integration: phase consumes the existing Sanity read client (`lib/sanity/client.ts`, `stega: false`) + the `@sanity/document-internationalization` `translation.metadata` link established in Phase 3; no new SDK/service integrated. `sanity dataset import` (D-07) is a build-tooling CLI content-landing step, not a runtime API integration. The API-coverage matrix does not apply.
