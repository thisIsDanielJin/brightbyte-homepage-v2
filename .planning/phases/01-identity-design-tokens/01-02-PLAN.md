---
phase: 01-identity-design-tokens
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - public/logo-light.svg
  - public/logo-dark.svg
  - docs/brand-voice.md
autonomous: true
requirements: [IDENT-03, IDENT-04]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "public/logo-light.svg and public/logo-dark.svg exist, contain zero <text> nodes and zero font-family attributes (text converted to paths) — grep -l '<text' public/logo-*.svg returns empty (IDENT-03)"
    - "Both SVGs contain zero <script> elements and no external xlink:href/use references (svgo-stripped, safe static assets)"
    - "logo-light.svg fills use #18181B (BrightByte) and #52525B (Berlin); logo-dark.svg fills use #F4F4F5 and #A1A1AA — matching the UI-SPEC logo contract and tokens.css values"
    - "docs/brand-voice.md exists, is ~1 page, and captures tone, sentence rule, DE forbidden words (innovativ, maßgeschneidert, ganzheitlich, Lösung, Synergien, state-of-the-art) and EN forbidden words (innovative, bespoke, tailored, holistic, solution, cutting-edge) plus the approved copy elements table (IDENT-04, D-12)"
    - { statement: "The wordmark renders legibly on both #FFFFFF and #0F0F10 surfaces at min-width 120px and reads as calm/editorial per the cool-architectural direction", verification: backstop }
  artifacts:
    - public/logo-light.svg
    - public/logo-dark.svg
    - docs/brand-voice.md
  key_links:
    - "logo fill hex values trace to tokens.css color tokens (text-primary #18181B, text-secondary #52525B, text-on-dark #F4F4F5, text-muted-on-dark #A1A1AA)"
    - "docs/brand-voice.md forbidden-words + copy-elements table is the source Phase 4 copy is written against (D-12)"
---

<objective>
Produce the two remaining resolved identity artifacts that expand out from the proven token skeleton: the wordmark logo (IDENT-03) and the 1-page brand voice guide (IDENT-04). The logo is a text-as-paths SVG in two variants (light + dark surfaces) matching the UI-SPEC logo contract exactly; the voice guide captures the plain-warm tone, sentence rules, bilingual forbidden-word lists, and approved copy elements that all Phase 4+ copy is written against.

Purpose: Complete the resolved identity so Phases 2-7 have a single logo asset and a single voice reference — closing v1's ~14-experiment logo spiral and its jargon-drift.
Output: public/logo-light.svg, public/logo-dark.svg, docs/brand-voice.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-identity-design-tokens/01-UI-SPEC.md
@.planning/phases/01-identity-design-tokens/01-RESEARCH.md
@styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wordmark logo — light + dark text-as-paths SVGs</name>
  <files>public/logo-light.svg, public/logo-dark.svg</files>
  <read_first>
    - .planning/phases/01-identity-design-tokens/01-UI-SPEC.md (Logo Contract — exact weights, tracking, fills, min-width, clear space)
    - .planning/phases/01-identity-design-tokens/01-RESEARCH.md (Logo Production Workflow — text-as-paths export, svgo, SVG structure; Security Domain SVG note)
    - styles/tokens.css (the canonical hex values the fills must match)
  </read_first>
  <action>
    Produce two SVG wordmarks for "BrightByte Berlin" set in Plus Jakarta Sans, text converted to outline paths (no font dependency at render). Per the UI-SPEC logo contract: "BrightByte" in weight 700, tracking -0.03em; "Berlin" in weight 400, tracking 0.12em, positioned as a subordinate descriptor. Light variant fills: BrightByte #18181B, Berlin #52525B. Dark variant fills: BrightByte #F4F4F5, Berlin #A1A1AA. Design the viewBox so the wordmark holds legibility at 120px wide (the enforced minimum — applied as a CSS rule in the consuming component in Phase 4, not encoded in the SVG). Leave clear space equal to the cap-height of the "B" on all four sides within the viewBox.

    Generate the outlined paths using whatever text-to-path route is available in-environment (design-tool export, or a code/CLI route that converts the font glyphs to path data). After generating, run both files through svgo to optimize and strip scripts/metadata, targeting under 5KB each. The resulting SVGs must contain only svg, path, and structural elements: zero text nodes, zero font-family attributes, zero script, no external xlink:href or use references. Add role="img" and aria-label="BrightByte Berlin" on each svg root.
  </action>
  <verify>
    <automated>test -f public/logo-light.svg && test -f public/logo-dark.svg && ! grep -q '<text' public/logo-light.svg public/logo-dark.svg && ! grep -q '<script' public/logo-light.svg public/logo-dark.svg && grep -q '#18181B' public/logo-light.svg && grep -q '#F4F4F5' public/logo-dark.svg</automated>
  </verify>
  <acceptance_criteria>
    - public/logo-light.svg and public/logo-dark.svg both exist and are under 5KB.
    - Neither file contains a text node, a font-family attribute, a script element, or an external xlink:href/use reference.
    - logo-light.svg contains fills #18181B and #52525B; logo-dark.svg contains fills #F4F4F5 and #A1A1AA.
    - Each svg root has role="img" and aria-label="BrightByte Berlin".
  </acceptance_criteria>
  <done>Both wordmark variants exist as safe, optimized, text-as-paths SVGs with the correct token-matched fills. Committed.</done>
  <reversibility rating="reversible">Wordmark-only (D-11) with two token values as fills; regenerating from a different tracking/weight is cheap. Reversible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Brand voice guide — 1-page IDENT-04 deliverable</name>
  <files>docs/brand-voice.md</files>
  <read_first>
    - .planning/phases/01-identity-design-tokens/01-UI-SPEC.md (Copywriting Contract — Brand Voice Rules table and Copy Elements table, verbatim source)
    - .planning/phases/01-identity-design-tokens/01-RESEARCH.md (Brand Voice Guide Format — file location and structure)
    - .planning/phases/01-identity-design-tokens/01-CONTEXT.md (D-12 — plain & warm, German-first, facts-led credibility)
  </read_first>
  <action>
    Author docs/brand-voice.md as a scannable ~1-page guide per D-12 and the UI-SPEC Copywriting Contract. Open with a one-sentence essence ("Plain, warm, direct — speaks to a florist as a person, credibility through facts"). Include a Rules table lifting the UI-SPEC Brand Voice Rules verbatim: tone (plain/warm/direct, not agency-formal), language register (German-first canonical, EN mirrors register not a formal translation), sentence length (max 2 clauses, one idea per sentence), the DE forbidden-words list (innovativ, maßgeschneidert, ganzheitlich, Lösung, Synergien, state-of-the-art), the EN forbidden-words list (innovative, bespoke, tailored, holistic, solution, cutting-edge), credibility vehicle (facts only — fixed price, code ownership, 24h reply), headline rule (declarative benefit not clever), and CTA rule (action-verb first; never submit/send/click here). Then include the Approved Copy Elements table verbatim from the UI-SPEC (primary CTAs, nav CTA, empty state, form success/error copy in both DE and EN). Keep it to one page — it is a reference card, not an essay.
  </action>
  <verify>
    <automated>test -f docs/brand-voice.md && grep -q 'maßgeschneidert' docs/brand-voice.md && grep -q 'cutting-edge' docs/brand-voice.md && grep -qi 'Projekt anfragen' docs/brand-voice.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/brand-voice.md exists and fits ~1 page.
    - Contains the full DE forbidden-words list (including maßgeschneidert, Synergien) and the full EN list (including bespoke, cutting-edge).
    - Contains the sentence-length rule, credibility-vehicle rule, and CTA rule.
    - Contains the Approved Copy Elements table with both DE and EN columns (e.g. "Projekt anfragen" / "Start a project").
  </acceptance_criteria>
  <done>The 1-page brand voice guide exists capturing tone, sentence style, bilingual forbidden patterns, and approved copy elements — the reference all v2 copy is written against. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG → public/ static asset | SVGs committed to public/ are served as static files. If they contained script or external references they could be an XSS vector when inlined. |
| (none at runtime) | Voice guide is a markdown doc; logos are static assets. No user input, no auth, no runtime network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Elevation of Privilege | logo SVG served from public/ | low | mitigate | Text-as-paths only; svgo strips script/metadata; verify asserts zero script and no external xlink:href/use. Paths are authored, not sourced from untrusted input. |
| T-01-03 | Tampering | svgo (npx) tool used to optimize SVGs | low | accept | Dev-time-only optimization run; output inspected by the verify grep gate. No runtime exposure. |

No high-severity threats. Static asset + markdown phase; ASVS L1 surface minimal (no user input, no auth, no runtime network).
</threat_model>

<verification>
- Both logo SVGs exist, are text-as-paths, script-free, with token-matched fills (verify grep gates).
- docs/brand-voice.md exists with the full bilingual forbidden-word lists and the copy-elements table.
</verification>

<success_criteria>
- IDENT-03: a single resolved wordmark logo (light + dark) exists as safe SVG, replacing v1's inline SVG + abandoned experiments.
- IDENT-04: a 1-page brand voice guide exists, applied to all v2 copy.
</success_criteria>

<output>
Create `.planning/phases/01-identity-design-tokens/01-02-SUMMARY.md` when done.
</output>
