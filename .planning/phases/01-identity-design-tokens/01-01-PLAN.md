---
phase: 01-identity-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - postcss.config.mjs
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/globals.css
  - app/token-audit/page.tsx
  - styles/tokens.css
  - playwright.config.ts
  - tests/a11y/contrast.spec.ts
  - tests/invariants/no-raw-hex.sh
autonomous: true
requirements: [IDENT-01, IDENT-02]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "styles/tokens.css contains exactly one @theme block and it is the only @theme block in the project (grep -rl '@theme' finds only styles/tokens.css and the single @theme inline bridge in app/globals.css)"
    - "styles/tokens.css defines --color-accent: #1C39BB, --color-text-primary: #18181B, --color-surface: #FFFFFF and every palette/type/spacing/motion token from the UI-SPEC token tables (IDENT-01)"
    - "The font resolves to Plus Jakarta Sans via the @theme inline bridge --font-sans: var(--font-plus-jakarta-sans) — document.fonts.check('16px \"Plus Jakarta Sans\"') returns true, not the system fallback (IDENT-02, RESEARCH Pitfall 1)"
    - "The /token-audit page renders text in every foreground/background token pair and @axe-core/playwright color-contrast audit reports zero violations, confirming accent #1C39BB on #FFFFFF passes AA (computed 8.93:1) (IDENT-02)"
    - "tests/invariants/no-raw-hex.sh exits 0 (zero raw hex outside styles/tokens.css, zero text-gray-*/bg-gray-*/border-gray- in app/) — the enforceable IDENT-01 invariant later phases inherit"
    - { statement: "Text at all token sizes reads as calm, editorial, and professional per the UI-SPEC typography contract", verification: backstop }
  artifacts:
    - styles/tokens.css
    - app/globals.css
    - app/layout.tsx
    - app/token-audit/page.tsx
    - postcss.config.mjs
    - playwright.config.ts
    - tests/a11y/contrast.spec.ts
    - tests/invariants/no-raw-hex.sh
    - package.json
  key_links:
    - "app/globals.css @import chain: @import \"tailwindcss\" then @import \"../styles/tokens.css\" then @theme inline font bridge"
    - "app/layout.tsx applies Plus_Jakarta_Sans({ variable: '--font-plus-jakarta-sans' }).variable className to <html> — the runtime half of the font bridge"
    - "@theme inline in globals.css resolves --font-sans eagerly at build so font-sans utility works (RESEARCH Pitfall 1)"
---

<objective>
Establish the Walking Skeleton for BrightByte Berlin v2: the thinnest end-to-end proof that the Tailwind v4 `@theme` design-token system WORKS through the full stack. This is the phase tracer — it scaffolds the Next.js 16 project, authors `styles/tokens.css` (the single canonical `@theme` block every later phase consumes), wires Plus Jakarta Sans via `next/font` and the `@theme inline` bridge, renders ONE token-audit page that consumes named tokens, and stands up the Wave 0 validation infrastructure (Playwright + @axe-core contrast audit + raw-hex grep gate) that proves the tokens are correct and AA-compliant before any component work begins.

Purpose: Identity-first hard gate (prevents v1's per-section improvisation). The token file is the integration point for Phases 2-7 — IDENT-01 requires zero raw hex / zero `text-gray-*` in any component file, enforced from this phase forward.
Output: Next.js scaffold, `styles/tokens.css`, font wiring, `/token-audit` page, and green Wave 0 test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-design-tokens/01-UI-SPEC.md
@.planning/phases/01-identity-design-tokens/01-RESEARCH.md
@.planning/phases/01-identity-design-tokens/SKELETON.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Scaffold Next.js + tokens.css + font bridge — one token-consuming page end-to-end</name>
  <files>package.json, postcss.config.mjs, next.config.ts, tsconfig.json, app/layout.tsx, app/globals.css, styles/tokens.css, app/token-audit/page.tsx</files>
  <read_first>
    - .planning/phases/01-identity-design-tokens/01-UI-SPEC.md (§Color, §Typography, §Spacing Scale, §Motion Tokens, §Token File Contract — the exact hex, sizes, and token names)
    - .planning/phases/01-identity-design-tokens/01-RESEARCH.md (§Architecture Patterns Pattern 1-3, §Common Pitfalls Pitfall 1 — the @theme inline bridge; §Standard Stack for exact versions)
    - /Users/I750579/Documents/brightbyte-homepage-v2/.claude/CLAUDE.md (Tailwind v4 @theme is THE styling approach — no tailwind.config.js, no SCSS, no inline styles)
  </read_first>
  <behavior>
    - Building the project compiles styles/tokens.css through @tailwindcss/postcss with no CSS errors.
    - The /token-audit page uses utility classes derived from named tokens (e.g. bg-surface, text-primary, text-accent, font-sans) — no raw hex, no text-gray-*.
    - document.fonts.check('16px "Plus Jakarta Sans"') returns true after load (font bridge works, not system fallback).
  </behavior>
  <action>
    Scaffold a Next.js 16.3.0 App Router + React 19.2.x + TypeScript project (greenfield — no scaffolder assumptions; create package.json, tsconfig.json, next.config.ts by hand or via create-next-app then prune). Install exact versions from RESEARCH §Standard Stack: next@16.3.0, react@19.2.x, tailwindcss@4.3.3, @tailwindcss/postcss@4.3.3, sharp@0.35.3. Create postcss.config.mjs registering the @tailwindcss/postcss plugin (NOT the old tailwindcss plugin — v4 split it; wrong plugin fails silently).

    Author styles/tokens.css as a SINGLE @theme {} block containing every token from the UI-SPEC token tables verbatim: colors (--color-surface #FFFFFF, --color-surface-subtle #FAFAFA, --color-surface-muted #F4F4F5, --color-border #E4E4E7, --color-text-muted #A1A1AA, --color-text-secondary #52525B, --color-text-primary #18181B, --color-accent #1C39BB, --color-accent-hover #1630A0, --color-destructive #DC2626, --color-surface-dark #0F0F10, --color-text-on-dark #F4F4F5, --color-text-muted-on-dark #A1A1AA); type scale (--text-sm 0.875rem, --text-base 1rem, --text-2xl 1.5rem, --text-4xl 2rem, --text-5xl 3rem); spacing (--spacing-1 through --spacing-32 per the table); motion (--ease-out, --ease-standard, --ease-in cubic-beziers and --duration-micro/short/standard/entrance). Add a header comment noting --ease-* and --duration-* do NOT auto-generate utilities (consumed via var()/arbitrary values — RESEARCH Pitfall 2). To force all gray through named tokens, set Tailwind's default gray scale to initial in the @theme block so text-gray-* utilities cannot silently bypass semantic tokens (RESEARCH Pitfall 3).

    Wire app/globals.css: @import "tailwindcss"; then @import "../styles/tokens.css"; then a single @theme inline block bridging --font-sans to var(--font-plus-jakarta-sans) (the inline modifier resolves eagerly at build — RESEARCH Pitfall 1); then the prefers-reduced-motion reduce media query zeroing animation/transition durations.

    Wire app/layout.tsx: import Plus_Jakarta_Sans from 'next/font/google' with subsets ['latin'], display 'swap', variable '--font-plus-jakarta-sans' (no weight — variable font covers 200-800). Import './globals.css'. Apply the font .variable className to <html lang="de"> (the runtime half of the bridge).

    Create app/token-audit/page.tsx: a Server Component rendering one block of text per foreground/background token pair from the UI-SPEC WCAG table (text-primary on surface, text-secondary on surface, accent on surface, text-on-dark on surface-dark, etc.), each using only named-token utilities and font-sans. This is both the tracer's proof element and the fixture the contrast audit (Task 3) targets. Do NOT render Text Muted (#A1A1AA) as body copy — it is the known AA exception restricted to decorative/disabled per D-06/UI-SPEC; if shown, label it decorative and exclude from the readable-copy pairs.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && test -f styles/tokens.css && grep -q -- '--color-accent: #1C39BB' styles/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` completes without CSS or type errors.
    - `styles/tokens.css` contains `--color-accent: #1C39BB`, `--color-text-primary: #18181B`, `--color-surface: #FFFFFF` and every token named in the UI-SPEC tables.
    - `grep -c '@theme' styles/tokens.css` returns 1; the only other `@theme` in the project is the single inline bridge in `app/globals.css`.
    - `app/globals.css` @import order is tailwindcss, then tokens.css, then the @theme inline font bridge.
    - `app/token-audit/page.tsx` uses only named-token utilities (verified by Task 2 grep gate).
  </acceptance_criteria>
  <done>The project builds; tokens.css is the single @theme source of truth; the font bridge is wired both halves; /token-audit renders text in every token pair using named tokens only. Committed.</done>
  <reversibility rating="costly">Light-first palette (D-05) and cool-architectural direction (D-01) are baked into every token; changing after Phase 4 means a palette rebuild. Costly, not one-way — no hard gate. Accent hex (D-06) is one token value (reversible).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — install Playwright + @axe-core, author raw-hex invariant grep gate</name>
  <files>package.json, playwright.config.ts, tests/invariants/no-raw-hex.sh</files>
  <read_first>
    - .planning/phases/01-identity-design-tokens/01-RESEARCH.md (§Validation Architecture — the grep commands, Wave 0 gaps, package legitimacy audit for @axe-core/playwright)
    - .planning/phases/01-identity-design-tokens/01-VALIDATION.md (Wave 0 Requirements — this task fills them)
    - package.json (the file being modified — add devDeps and scripts)
  </read_first>
  <action>
    Install devDependencies: @playwright/test (latest) and @axe-core/playwright@4.12.1 (RESEARCH package-legitimacy: OK/Approved, Deque Systems, 8.15M weekly downloads, no postinstall). Run the Playwright browser install for chromium only.

    Create playwright.config.ts with baseURL http://localhost:3000, a webServer entry that runs the dev/start command so the /token-audit route is reachable, testDir ./tests, and chromium as the sole project. No watch-mode flags.

    Create tests/invariants/no-raw-hex.sh: a bash script enforcing the IDENT-01 invariant. It greps app/ for raw hex outside styles/tokens.css and for Tailwind default gray utilities. Filter comment lines before counting so header prose is not self-invalidating (pipe through grep -v of comment-prefixed lines). Script (a) fails with a printed offender list and exit 1 if any raw hex is found in app/ *.tsx/*.ts/*.css excluding styles/tokens.css, and (b) fails if any text-gray-/bg-gray-/border-gray- appears in app/. Exit 0 when clean. Make it executable.

    Add package.json scripts: "test:a11y" running the contrast spec, "test:invariants" running the grep gate, and "test" running both.
  </action>
  <verify>
    <automated>test -x tests/invariants/no-raw-hex.sh && bash tests/invariants/no-raw-hex.sh && npx playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - `@playwright/test` and `@axe-core/playwright@4.12.1` present in package.json devDependencies.
    - `playwright.config.ts` exists with `baseURL: 'http://localhost:3000'` and a webServer entry; no watch-mode flags.
    - `bash tests/invariants/no-raw-hex.sh` exits 0 against the current app/ (token-audit page uses named tokens only).
    - `package.json` has `test`, `test:a11y`, `test:invariants` scripts.
  </acceptance_criteria>
  <done>Playwright + axe installed; the raw-hex/gray invariant gate is executable and green against the token-audit page; test scripts wired. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contrast audit spec — prove every token pair passes WCAG AA green</name>
  <files>tests/a11y/contrast.spec.ts</files>
  <read_first>
    - .planning/phases/01-identity-design-tokens/01-RESEARCH.md (§Color Contrast Verification — computed ratios incl. accent 8.93:1; §Validation Architecture Contrast Audit Pattern)
    - .planning/phases/01-identity-design-tokens/01-UI-SPEC.md (§Color WCAG AA table — the pairs and the Text Muted exception)
    - app/token-audit/page.tsx (the fixture the spec navigates to)
    - playwright.config.ts (baseURL + webServer)
  </read_first>
  <behavior>
    - Navigating to /token-audit and running @axe-core/playwright with the color-contrast rule returns zero violations for all readable token pairs.
    - The decorative Text Muted (#A1A1AA on #FFFFFF, 2.56:1) region is excluded/annotated so it does not fail the readable-copy audit (it is the known intentional exception).
  </behavior>
  <action>
    Create tests/a11y/contrast.spec.ts using @playwright/test and AxeBuilder from @axe-core/playwright. The test navigates to /token-audit, runs AxeBuilder scoped withRules(['color-contrast']), and asserts results.violations has length 0. Exclude the decorative Text Muted region from the scan (via an .exclude selector on the labeled decorative block) since #A1A1AA on white intentionally fails AA and is restricted to decorative/disabled use per D-06 / UI-SPEC — never body copy. Keep runtime under ~30s (chromium only, single page).
  </action>
  <verify>
    <automated>npx playwright test tests/a11y/contrast.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/a11y/contrast.spec.ts` passes with zero color-contrast violations.
    - The accent pair (#1C39BB on #FFFFFF) is present in the audited page and passes (computed 8.93:1, AA and AAA).
    - The decorative Text Muted region is explicitly excluded from the readable-copy assertion, not silently passing.
  </acceptance_criteria>
  <done>The contrast audit runs green against the live /token-audit page, proving every readable foreground/background token pair meets WCAG AA before any component is built. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party packages (tailwindcss, next, @axe-core/playwright) enter the build. Only realistic surface this phase. |
| (none at runtime) | Phase 1 output is static CSS + a build-time font asset. No user input, no auth, no network calls at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm devDependency install (@axe-core/playwright, playwright) | low | mitigate | RESEARCH Package Legitimacy Audit verified @axe-core/playwright as OK/Approved (Deque Systems, 8.15M wk downloads, no postinstall). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking human checkpoint required. |
| T-01-01 | Tampering | font supply chain (Plus Jakarta Sans via next/font) | low | accept | Build-time asset from Google Fonts via Next.js official next/font; SIL OFL. No runtime fetch. Accepted — no high-severity path. |
| T-01-02 | Elevation | SVG logo XSS if inlined from untrusted input | low | accept | Not applicable this plan (logos are authored in Plan 02, text-as-paths, no scripts, svgo-stripped). No untrusted input source exists. |

No high-severity threats. ASVS L1 surface for a static token/asset phase is minimal (no user input, no auth, no runtime network).
</threat_model>

<verification>
- `npx next build` succeeds; `styles/tokens.css` is the single `@theme` block.
- `bash tests/invariants/no-raw-hex.sh` exits 0.
- `npx playwright test tests/a11y/contrast.spec.ts` passes (zero color-contrast violations; accent 8.93:1 confirmed).
- Font bridge verified: `document.fonts.check('16px "Plus Jakarta Sans"')` true (not system fallback).
</verification>

<success_criteria>
- The token system is proven end-to-end: scaffold compiles tokens.css, font resolves via the @theme inline bridge, /token-audit consumes named tokens, and the contrast audit is green.
- IDENT-01 invariant (zero raw hex / zero gray utilities) is enforceable and green.
- IDENT-02 palette + typeface confirmed WCAG AA before any component work.
</success_criteria>

<output>
Create `.planning/phases/01-identity-design-tokens/01-01-SUMMARY.md` when done.
</output>
