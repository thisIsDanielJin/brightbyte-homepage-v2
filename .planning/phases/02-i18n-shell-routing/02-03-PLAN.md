---
phase: 02-i18n-shell-routing
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/LocaleSwitcher.tsx
  - app/[locale]/layout.tsx
autonomous: true
requirements: [I18N-03]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The language switcher navigates `/de/...` ↔ `/en/...` preserving the current path segment (I18N-03, ROADMAP criterion 3)"
    - "The switcher is pure `<Link>` navigation with NO client-side state mutation — no useState, no localStorage, no useRouter().replace() (I18N-03, D-09)"
    - "The switcher lives in a minimal, unstyled header inside `[locale]/layout.tsx`, holding ONLY the switcher (D-06)"
  artifacts:
    - "components/LocaleSwitcher.tsx — 'use client' component using @/i18n/navigation Link + usePathname"
    - "app/[locale]/layout.tsx — minimal <header> mounting LocaleSwitcher"
  key_links:
    - "usePathname MUST come from @/i18n/navigation, not next/navigation (Pitfall 4) — it returns the locale-stripped path so the <Link locale> re-prefixes exactly once, never producing /de/de/..."
    - "the switcher must read current locale via useLocale() from next-intl (URL-derived), never from state"
    - "the switcher header must use @theme token utilities only — no raw hex, no text-gray-* (IDENT-01)"
  prohibitions:
    - statement: "Locale switching MUST NOT produce a double-prefix URL like `/de/de/...` or `/en/en/...` — path preservation re-prefixes exactly once (I18N-03, RESEARCH Pitfall 4)"
      status: enforced
      verification: "tests/i18n/smoke.spec.ts switcher test asserts the post-switch URL matches `/en/...` (single prefix) with no `/de/de/` or `/en/en/` segment"
    - statement: "The switcher MUST NOT hold or derive locale from component state, localStorage, or useRouter().replace() — pure <Link> only (I18N-03, D-09)"
      status: enforced
      verification: "bash tests/invariants/no-locale-from-state.sh exits 0; components/LocaleSwitcher.tsx contains no useState/useReducer/localStorage"
    - statement: "MUST NOT import Link or usePathname from next/link / next/navigation in the switcher — use @/i18n/navigation (Pitfall 4)"
      status: enforced
      verification: "components/LocaleSwitcher.tsx imports Link and usePathname from @/i18n/navigation"
    - statement: "MUST NOT add footer/nav chrome — the header holds ONLY the switcher this phase (D-06; full chrome is Phase 4)"
      status: enforced
      verification: "the [locale]/layout.tsx header contains only the LocaleSwitcher, no nav/footer sections"
    - statement: "MUST NOT introduce raw hex or text-gray-* in the switcher/header markup (IDENT-01)"
      status: enforced
      verification: "bash tests/invariants/no-raw-hex.sh exits 0"
---

<user_story>
**As a** bilingual Berlin visitor reading a page in one language,
**I want to** switch to the other language and stay on the same page,
**so that** I can read the exact content I was viewing in my preferred language without losing my place.
</user_story>

<objective>
Expansion slice on the proven tracer: add the path-preserving language switcher and mount it in the minimal `[locale]` header. Implemented as pure `<Link>` navigation (next-intl `createNavigation` Link + `usePathname`) with zero client-side state — the structural guard against v1's localStorage/state i18n anti-pattern (I18N-03, D-06, D-09).

Purpose: I18N-03 completes the user-facing proof that routing works end-to-end — a real person can flip locales and stay on their page. The switcher's minimal header is the mount point Phase 4 replaces with full identity-driven chrome.

Output: `components/LocaleSwitcher.tsx` + a minimal header in `[locale]/layout.tsx`, with smoke test (6) flipping GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-i18n-shell-routing/02-CONTEXT.md
@.planning/phases/02-i18n-shell-routing/02-RESEARCH.md
@AGENTS.md
@.planning/phases/02-i18n-shell-routing/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path-preserving LocaleSwitcher (pure Link, no state)</name>
  <files>components/LocaleSwitcher.tsx</files>
  <read_first>
    - AGENTS.md
    - https://next-intl.dev/docs/routing/navigation (createNavigation Link with locale prop + usePathname)
    - .planning/phases/02-i18n-shell-routing/02-RESEARCH.md §Pattern 5 (switcher), §Pattern 6 (navigation exports), §Pitfall 4 (usePathname source — must be @/i18n/navigation)
    - .planning/phases/02-i18n-shell-routing/02-CONTEXT.md I18N-03 wording, D-09 (no client state), D-02 (switcher label from messages)
    - i18n/navigation.ts (from Plan 01 — Link + usePathname source)
    - i18n/routing.ts (from Plan 01 — routing.locales to iterate)
    - messages/de.json + messages/en.json (from Plan 01 — LocaleSwitcher label/locale-name keys)
    - styles/tokens.css (token utilities available; consume only — no raw hex)
  </read_first>
  <behavior>
    - Test: on `/de`, clicking the EN switch link navigates to `/en` preserving the current path segment (root → /en; /de/foo → /en/foo).
    - Test: on `/en`, clicking the DE link navigates to `/de` preserving the path.
    - Test: no `/de/de/` or `/en/en/` double-prefix URL is ever produced.
    - This is smoke test (6) in tests/i18n/smoke.spec.ts flipping RED→GREEN.
  </behavior>
  <action>
    Create `components/LocaleSwitcher.tsx` as a `'use client'` component per RESEARCH §Pattern 5: import `Link` and `usePathname` from `@/i18n/navigation` (NOT next/navigation — Pitfall 4), `useLocale` from `next-intl`, and `routing` from `@/i18n/routing`. Read the locale-stripped `usePathname()`, map over `routing.locales` rendering a `<Link href={pathname} locale={locale}>` per locale, marking the active one with `aria-current`. Use the switcher label/locale names from messages (D-02) via `useTranslations`. NO useState, NO useReducer, NO localStorage, NO useRouter().replace() — pure `<Link>` navigation (I18N-03, D-09). Style with `@theme` token utility classes only — no raw hex, no `text-gray-*` (IDENT-01). Give the nav an `aria-label`.
  </action>
  <verify>
    <automated>test -f components/LocaleSwitcher.tsx && grep -q "@/i18n/navigation" components/LocaleSwitcher.tsx && ! grep -Eq "useState|useReducer|localStorage|useRouter" components/LocaleSwitcher.tsx && bash tests/invariants/no-locale-from-state.sh && bash tests/invariants/no-raw-hex.sh && npx tsc --noEmit && echo SWITCHER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/LocaleSwitcher.tsx` is `'use client'` and imports `Link` + `usePathname` from `@/i18n/navigation`.
    - It renders one `<Link href={pathname} locale={locale}>` per `routing.locales` with `aria-current` on the active locale.
    - It contains no `useState`, `useReducer`, `localStorage`, or `useRouter` usage.
    - The switcher label/locale names come from message files (not hardcoded).
    - `bash tests/invariants/no-locale-from-state.sh` and `bash tests/invariants/no-raw-hex.sh` both exit 0.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Pure `<Link>` switcher exists, iterates locales, preserves path, holds no client state, uses token utilities only.</done>
</task>

<task type="auto">
  <name>Task 2: Mount switcher in minimal [locale] header</name>
  <files>app/[locale]/layout.tsx</files>
  <read_first>
    - AGENTS.md
    - app/[locale]/layout.tsx (from Plan 01 — add the header; preserve font bridge, body classes, provider, generateStaticParams)
    - .planning/phases/02-i18n-shell-routing/02-CONTEXT.md D-06 (minimal unstyled header, switcher ONLY — no footer/nav)
    - .planning/phases/02-i18n-shell-routing/02-RESEARCH.md §Pattern 4 (header placement inside provider)
    - components/LocaleSwitcher.tsx (from Task 1 — the component to mount)
    - styles/tokens.css (token utilities — consume only)
  </read_first>
  <action>
    In `app/[locale]/layout.tsx`, add a minimal `<header>` INSIDE `NextIntlClientProvider` (so the switcher's `useTranslations`/`useLocale` hooks resolve) and before `{children}`, containing ONLY `<LocaleSwitcher />` (D-06). No footer, no nav links, no branding — that chrome is Phase 4. Keep the header markup minimal and use only `@theme` token utility classes for any spacing/layout (no raw hex, no text-gray-*). Preserve everything Plan 01 established: the Plus Jakarta Sans variable on `<html>`, the body token classes, the provider, and `generateStaticParams`.
  </action>
  <verify>
    <automated>grep -q "LocaleSwitcher" app/[locale]/layout.tsx && grep -q "NextIntlClientProvider" app/[locale]/layout.tsx && bash tests/invariants/no-raw-hex.sh && npx tsc --noEmit && npm run build && npx playwright test tests/i18n/smoke.spec.ts && echo HEADER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/[locale]/layout.tsx` renders `<LocaleSwitcher />` inside a `<header>` within `NextIntlClientProvider`, before `children`.
    - The header contains only the switcher (no footer/nav/branding).
    - Plan 01 layout invariants preserved: font-bridge variable, body token classes, provider, `generateStaticParams`.
    - `bash tests/invariants/no-raw-hex.sh` exits 0.
    - `npm run build` succeeds; the full `tests/i18n/smoke.spec.ts` suite passes (all 6 tests GREEN).
  </acceptance_criteria>
  <done>Switcher mounted in a minimal header; full i18n smoke suite green; routing proven end-to-end for a real user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → client switcher | User click triggers `<Link>` navigation; pathname read from router context (URL), not stored state |

## STRIDE Threat Register

ASVS Level 1 · block on: high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | Information Disclosure / Tampering | LocaleSwitcher locale source | medium | mitigate | Locale is read via `useLocale()` (URL-derived) and navigation is pure `<Link>` — no localStorage/state holds locale, so there is no client-side locale value to tamper with or leak. Structurally enforced by tests/invariants/no-locale-from-state.sh (D-09). |
| T-2-07 | Tampering | usePathname source | low | mitigate | `usePathname` imported from `@/i18n/navigation` returns the locale-stripped path; the `<Link locale>` re-prefixes exactly once — no attacker path can force a `/de/de/` traversal (Pitfall 4). |
</threat_model>

<verification>
- `npm run build` succeeds with the switcher mounted.
- `bash tests/invariants/no-locale-from-state.sh` exits 0 (switcher holds no locale state).
- `bash tests/invariants/no-raw-hex.sh` exits 0 (IDENT-01 preserved in header/switcher).
- `npx playwright test tests/i18n/smoke.spec.ts` — all 6 tests GREEN (redirect, html lang de/en, hreflang de/en, switcher navigation).
</verification>

<success_criteria>
- The switcher navigates `/de/...` ↔ `/en/...` preserving the path, as pure `<Link>` navigation with no client state (ROADMAP criterion 3, I18N-03).
- The CI smoke suite catches any client-side locale leak; locale is URL-only (ROADMAP criterion 4, D-09).
- The full Phase 2 i18n smoke suite is green end-to-end.
</success_criteria>

<output>
Create `.planning/phases/02-i18n-shell-routing/02-03-SUMMARY.md` when done.
</output>
